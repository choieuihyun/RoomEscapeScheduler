# Floduler — 작업명세서

작성일: 2026-08-20
문서 역할: **무엇을 어떻게 만들었는가 / 무엇을 검증했는가.** 왜 그렇게 정했는지는 [기획.md](기획.md).

---

## 1. 산출물

| 파일 | 역할 | 비고 |
|---|---|---|
| `index.html` | 앱 전체 (마크업 + 스타일 + 로직) | 단일 파일, 외부 라이브러리 0개 |
| `serve.mjs` | 개발 서버 (정적 서빙 전용) | Node 내장 모듈만 사용, 의존성 0개 |
| `vendor/onnxruntime-web/`, `vendor/ppu-paddle-ocr/`, `vendor/ppu-ocv/`, `vendor/paddle-models/` | 이미지 인식 엔진 PaddleOCR (약 18MB) | 저장소에 포함 — CDN 없이 오프라인 동작 (기획 §4.25) |
| `test/acceptance.mjs` | 인수 테스트 | `index.html` 의 로직을 직접 실행 |
| `package.json` | 실행 스크립트 | `dependencies` 없음 |
| `.nojekyll` | 빈 파일 | GitHub Pages 가 `vendor/` 를 걸러내지 않게 한다 (§2.2) |
| `cloud.js` | 계정·일정 보관 (Firebase Auth + Firestore) | 로그인 버튼을 눌러야 내려온다 |
| `firebase-config.js` | Firebase 접속 정보 | 비밀 아님. 비우면 계정 기능만 꺼진다 |
| `firestore.rules` | 접근 규칙 | Firebase 콘솔에 붙여넣는 원본 |
| `기획.md` | 배경·목표·설계 근거 | |
| `작업명세서.md` | 이 문서 | |
| `requirements.md` | 최초 요구사항 원본 (보관) | |

웹 폰트(Pretendard, jsDelivr)만 네트워크에서 가져오며, 차단되면 시스템 폰트로 떨어져 정상 동작한다.
맥·iOS 는 `Apple SD Gothic Neo`, 윈도우는 `Malgun Gothic` 으로 대체된다.
인식 엔진은 저장소 안에 있어 외부 요청이 없다.

---

## 2. 실행

```bash
npm run dev            # http://localhost:5173
npm test               # 인수 테스트
```

`index.html` 을 브라우저에서 그냥 열어도 조합 계산은 전부 동작한다 (OCR만 비활성).
정적 호스팅(GitHub Pages, Netlify 등)도 파일을 그대로 올리면 되며, 이 경우에도 OCR만 빠진다.

### 2.1 접속 범위

개발 서버는 **`127.0.0.1` 에만 바인딩한다.** 공용 와이파이처럼 남과 공유하는 망에서 쓰는 것을 전제로 한 기본값이다.

Node 의 `listen(PORT)` 는 호스트를 생략하면 `0.0.0.0`(모든 인터페이스)에 열려, 같은 망의 아무나 `http://<내IP>:5173` 으로 들어올 수 있다. 개발 중인 화면을 공용 와이파이에 무심코 띄우지 않도록 호스트를 명시적으로 고정했다.

| 실행 | 바인딩 | 접속 가능 범위 |
|---|---|---|
| `npm run dev` | `127.0.0.1:5173` + `[::1]:5173` | 이 컴퓨터만 |
| `HOST=0.0.0.0 npm run dev` | `0.0.0.0:5173` | 같은 네트워크의 모든 기기 (기동 시 경고 출력) |
| `HOST=<주소> npm run dev` | 그 주소 하나 | 지정한 대로 |

**루프백 두 개에 묶는 이유**: `localhost` 는 시스템에 따라 `::1`(IPv6) 로도, `127.0.0.1`(IPv4) 로도 풀린다. 한쪽에만 묶으면 반대쪽으로 붙는 클라이언트가 "연결 거부"를 만난다 — 대부분은 자동 폴백하지만 전부는 아니다. 양쪽에 묶어도 이 컴퓨터 밖으로는 열리지 않는다. IPv6 가 없는 환경에서 `::1` 바인딩이 실패하면 무시하고 IPv4 로만 뜬다.

검증 (실측):

| 접속 경로 | 결과 |
|---|---|
| `localhost` / `127.0.0.1` / `::1` | 200 |
| `http://<LAN IP>:5173` (같은 망의 다른 기기) | 연결 거부 |
| `lsof` LISTEN 주소 | `127.0.0.1:5173`, `[::1]:5173` — 외부 인터페이스 없음 |
| `HOST=0.0.0.0` 로 실행 후 LAN IP | 200 (의도적 노출, 경고 출력됨) |

**다른 환경에서 쓸 때**

- 다른 컴퓨터에 받아서 각자 실행하는 것은 전혀 영향 없다. 루프백은 기기마다 따로 존재한다.
- VM · Docker 컨테이너 · WSL · 원격 서버 **안에서 띄우고 바깥 브라우저로 붙는** 경우에는 루프백 바인딩이 막는다. 이때는 `HOST=0.0.0.0` 으로 열고 포트를 게시하거나(컨테이너), SSH 포트 포워딩(`ssh -L 5173:localhost:5173`)을 쓴다. 공용 망이라면 포트 포워딩 쪽이 안전하다.

---

### 2.2 배포

앱은 **완전한 정적 사이트**다. 인식까지 브라우저에서 돌므로 서버·DB·계정이 필요 없다.
`index.html` 과 `vendor/` 를 그대로 올리면 끝이고, 절대경로가 없어 하위 경로(`/RoomEscapeScheduler/`)에서도 동작한다.

**GitHub Pages 권장** — 저장소가 이미 공개이고 코드도 거기 있어 추가 준비가 없다.
Settings → Pages → Deploy from a branch → `main` / `(root)`.

| | GitHub Pages | Firebase Hosting |
|---|---|---|
| 준비 | 없음 (저장소 있음) | 구글 계정 + CLI + init |
| 배포 | `git push` | `firebase deploy` |
| 무료 대역폭 | 월 100GB | **하루 360MB** |
| 필요한 기능 | 정적 파일뿐 | 인증·DB·함수 — 안 씀 |

**대역폭 걱정은 없다.** 인식 엔진(약 5MB)은 **이미지를 첨부할 때만** 내려간다.
시간만 입력하는 방문자는 `index.html` 52KB 만 받는다.


#### `.nojekyll` 은 선택이 아니다

저장소 루트의 빈 파일 `.nojekyll` 은 **지우면 앱이 반쯤 망가진다.**
GitHub Pages 는 기본적으로 올라온 파일을 Jekyll 로 한 번 걸러 내보내는데,
Jekyll 의 기본 제외 목록에 `vendor/` 가 들어 있다. 인식 엔진이 전부 그 아래 있으므로
그대로 두면 **이미지 첨부 기능만 조용히 404 가 된다** — 조합 계산은 멀쩡히 돌기 때문에 눈치채기 어렵다.

#### 배포 후 확인할 것

로컬 `serve.mjs` 는 MIME 과 캐시 헤더를 이쪽이 정하지만, Pages 는 정해 주지 않는다.
환경이 달라져 결과가 달라질 수 있는 지점은 **정적 자산 서빙뿐**이므로 확인도 거기까지다.

| 확인 | 방법 | 기대 |
|---|---|---|
| 앱이 뜨는가 | 주소 접속 | 빈 화면(§4.22) — `테스트 데이터 넣기` 로 확인 가능 |
| `vendor/` 가 살아 있는가 | `curl -sI <url>/vendor/onnxruntime-web/ort-wasm-simd-threaded.wasm` | `200` (`.nojekyll` 검증) |
| 모델 파일이 그대로 서빙되는가 | `curl -sI <url>/vendor/paddle-models/PP-OCRv6_tiny_det.ort` | `Content-Length` 가 1.8MB 근처 (LFS 포인터로 바뀌어 있지 않은지) |
| 인식이 실제로 되는가 | 실제 기기에서 캡처 1장 첨부 | 회차 채워짐 |
| 링크 공유가 살아 남는가 | 단톡방에 링크 붙여넣고 다른 폰에서 열기 | 입력값 재현 |

### 2.3 다중 사용자에 대해

"여러 사람이 쓰게 하고 싶다" 는 요구는 실제로 세 가지가 섞여 있고, 필요한 것이 서로 다르다.

| 원하는 것 | 필요한 것 | 상태 |
|---|---|---|
| 여러 명이 각자 사용 | 없음 — 정적 파일이라 각자 독립 | 이미 됨 |
| 짠 시간표를 남에게 보내기 | 링크 (상태를 URL 에 직렬화) | 이미 됨 |
| 내 브라우저에서 이어서 하기 | `localStorage` | 미구현 |
| 기기를 넘나드는 "내 것" | **DB + 로그인** | 미구현 |
| 남들이 올린 것을 찾아보기 / 같이 고치기 | **DB** | 미구현 |

**DB 가 필요해지는 경계는 "내 것" 이라는 개념이 기기를 넘어갈 때**다. 그 전까지는 링크와 파일이
DB 역할을 대신한다 — 사용자가 이미 쓰는 카톡·메모·북마크에 저장을 외주 주는 셈이다.
링크 길이는 문제가 되지 않는다 (실측: 테마 5개 × 회차 10개 → 793자, 테마 7개 × 회차 12개 → 1,175자).

### 2.4 계정 설정 (F-12, 완료)

계정 기능은 코드가 다 들어가 있고 **`firebase-config.js` 만 비어 있다.**
비어 있는 동안에는 계정 줄에 "일정 저장 꺼짐" 만 뜨고 나머지는 전부 정상 동작한다.

| 단계 | 콘솔 위치 | 비고 |
|---|---|---|
| 1 | 프로젝트 만들기 | 애널리틱스 불필요 |
| 2 | Authentication → 이메일/비밀번호 → 사용 설정 | 이걸 안 켜면 `operation-not-allowed` |
| 3 | Firestore Database → 만들기 (`asia-northeast3`) | |
| 4 | Firestore → **규칙** → `firestore.rules` 붙여넣고 게시 | **건너뛰면 남의 문서가 열린다** |
| 5 | 프로젝트 설정 → 웹 앱 추가 → 값을 `firebase-config.js` 로 | 이 값들은 비밀이 아니다 |
| 6 | Authentication → Settings → 승인된 도메인에 `choieuihyun.github.io` | |

**요금.** Firebase Spark 는 카드 등록이 없고, 한도(하루 읽기 5만·쓰기 2만·저장 1GiB)를 넘기면
**청구가 아니라 그날치 차단**이다. 동호회 규모에서 닿을 숫자가 아니다.
일정 하나가 1KB 남짓이므로 1GiB 는 사실상 백만 건이다.

**왜 Supabase 가 아닌가.** 무료 등급이 **7일 무활동 시 프로젝트를 정지**시킨다.
띄엄띄엄 쓰는 동호회 도구에서는 2주 만에 들어온 사람이 오류를 보게 된다. Firebase 는 정지가 없다.

---

## 3. 기능 요구사항

| ID | 항목 | 내용 | 상태 |
|---|---|---|---|
| F-01 | 테마 입력 | 이름 / 소요시간(분) / 회차 시간 목록 | 완료 |
| F-02 | 테마 개수 | 제한 없음 (동적 추가·삭제). 2~5연방 등 실제 사용 형태에 대응 | 완료 |
| F-03 | 조합 탐색 | 겹치지 않는 모든 순서·회차 조합 계산 | 완료 |
| F-04 | 결과 정렬 | 다중 기준 선택 (5종) | 완료 |
| F-05 | 이미지 입력 | 예약 화면 캡처에서 회차 시간 자동 추출 | 완료 — 브라우저 동작 확인 (§9.4) |
| F-06 | 텍스트 입력 | 회차 시간 직접 입력 / 수정 | 완료 |
| F-07 | 조건 필터 | 시작 하한, 종료 상한, 최소·최대 공백 | 완료 |
| F-09 | 식사 공백 | 지정 시간대에 N분 이상 공백을 **요구** | 완료 |
| F-10 | 순서 제약 | 카드를 끌어 옮긴 순서가 곧 조합 순서(전면 변경, 기획 §4.14 후기) — 도구는 더 이상 순서를 탐색하지 않는다 | 완료 |
| F-11 | 결과 공유 | 상태를 담은 링크 + 사람이 읽는 텍스트 복사 | 완료 |
| F-08 | 부분 조합 | 일부 테마만 넣은 조합. 테마 수·테마 묶음별로 나눠서 본다 | 완료 (계산은 기본 끔) |
| F-12 | 계정·일정 보관 | 아이디/비번 로그인, 짠 일정을 `하고 싶음`/`했음` 으로 저장 | 완료 — 설정 대기 (§2.4) |
| F-13 | 매장 간 이동시간 | 테마별 매장명 + 기본값, **매장 쌍마다 덮어쓰기**. 탐색·표시 양쪽에 반영 | 완료 |
| F-14 | 순차 배정 | 여러 팀이 같은 테마를 나눠 돌 때, 확정한 팀의 회차를 다음 팀 계산에서 제외 | 완료 — 기본 접힘 (기획.md §4.29) |
| F-15 | 서버에서 불러오기 | 지점·날짜·테마를 고르면 회차 + **매진 여부**를 받아 카드를 채운다 | 완료 — §4.4 (기획.md §4.30) |
| F-16 | 취소표 감시·알림 | 매진 회차에 **"이 자리 감시"** 를 걸고, 자리가 나면 알림을 받는다 | **미착수** — §4.5 |

**입력 경로는 세 가지다** — F-05(사진), F-06(직접 입력), F-15(불러오기).

~~둘로 고정한다~~ 는 결정([기획.md](기획.md) §4.10)은 *"예약 사이트를 주기적으로 긁으려면 서버가 필요한데
그게 없다"* 가 이유였다. 그 서버가 생겨 전제가 사라졌으므로 번복했다 — [기획.md](기획.md) §4.30.

F-15 는 **선택 경로다.** 서버가 없거나 멎어도 F-05·F-06 은 그대로 동작한다 (§2.4 의 계정과 같은 취급).

---

## 4. 입력 사양

### 4.1 회차 시간 파싱 (`parseSessions`)

아래 형태를 모두 처리한다.

| 입력 | 해석 |
|---|---|
| `오전 10:30 매진 오후 12:00 매진` | 예약 화면 복사 형태 |
| `10:30, 12:00, 13:30, 15:00` | 구분자는 공백·쉼표·`·`·`|`·줄바꿈 |
| `10:30, 12:00, 1:30, 3:00` | → `10:30, 12:00, 13:30, 15:00` (아래 보정 규칙) |

규칙:

1. `오전`/`오후`/`AM`/`PM` 표기는 24시간제로 변환. 단독 토큰으로 와도 다음 시각에 적용된다.
2. **오전/오후 표기가 한 번도 없으면** 오름차순으로 가정하고, 이전 회차보다 작거나 같은 값에 12시간을 더해 보정한다.
3. `매진` / `마감` / `SOLD` 토큰은 **직전** 시각에 매진 플래그를 붙인다. `1매`, `2매` 등은 잔여석이므로 예약 가능으로 본다.
4. 같은 시각이 두 번 잡히면 하나로 접고, 매진 플래그는 OR로 합친다. **이 단계는 12시간 보정보다 먼저 수행한다** (§9.2).

### 4.2 입력 결과를 보여주는 방식

두 입력 경로(직접 입력 / 사진)를 화면에서 구분하고, **해석 결과를 계산 전에 확인**할 수 있게 한다.

| 표시 | 의미 |
|---|---|
| 헤더의 `4연방` 배지 | 지금 몇 개를 짜고 있는지. 추가·삭제가 가능하다는 신호이기도 하다 |
| `직접 입력` / `사진에서 읽음` / `사진 → 수정함` | 이 회차가 어디서 왔는지 |
| 입력칸 아래 시각 칩 | 입력을 실제로 어떻게 해석했는지 (입력 즉시 갱신) |
| 황동색 칩 | 12시간 보정된 값 (`1:10` → `13:10`) |
| 붉은 `다음날 01:10` + 경고 | 자정을 넘긴 회차 — 순서가 섞였다는 신호 |
| 취소선 친 칩 + 안내 | `매진` 으로 읽힌 회차. 계산에서 빠진다 |

**보정을 숨기지 않는 이유**: 오전/오후 표기가 없으면 오름차순으로 가정해 12시간을 더하는데,
이 가정은 입력 순서가 섞이면 조용히 엉뚱한 값을 만든다 (`15:40 10:00 13:10` → `15:40 22:00 익일 01:10`).
계산을 누른 뒤에야 이상을 알아채면 원인을 찾기 어렵다. 그래서 보정 사실과 자정 넘김을 입력 시점에 드러낸다.

### 4.3 이미지에서 추출하는 것

**회차 시간만** 읽는다. 매진 여부·테마명은 읽지 않는다 (§6.3).

그래서 **매진 회차는 사용자가 빼고 넣는다.** 화면에도 그렇게 안내한다 — 기획 §4.13.
`매진`·`마감` 이 텍스트에 함께 들어온 경우(예약 페이지를 통째로 복사한 경우)에만
자동으로 걸러지며, 걸러진 회차는 취소선으로 보여 준다.

- 시각은 콜론이 있는 토큰만 인정 → 달력의 날짜 숫자는 자연히 제외된다
- 오전/오후는 읽지 않고 오름차순 12시간 보정으로 복원한다 (§4.1-2)

사진으로 채운 뒤에도 텍스트가 그대로 남아 **손으로 고칠 수 있다**. 고치면 출처가 `사진 → 수정함` 으로 바뀐다.

### 4.4 서버에서 불러오기 (F-15)

**사진과 다른 점은 매진 여부가 함께 온다는 것이다.** §4.3 이 못 읽는 그 값이다.

#### 화면 흐름

```
지점 고르기  →  날짜 고르기  →  테마 목록에서 고르기  →  고른 것만 카드 생성
  (호출 1)      (호출 1의 응답)        (호출 2)
```

호출은 **두 번**이면 끝난다. 지점 목록에 날짜까지 들어 있어 날짜 선택에 추가 호출이 없다.

#### 서버 계약

**배포 주소: `https://floduler.duckdns.org`** (2026-08-28~, 오라클 도쿄).
`server.js` 의 `DEFAULT_BASE` 가 이 값이고, 로컬 서버를 볼 때만 콘솔에서 덮어쓴다:
`localStorage.setItem('flod.server', 'http://127.0.0.1:8083')`

별도 프로젝트(로컬 `~/IdeaProjects/Server`, Kotlin/Spring)가 제공한다. 저장소는 합치지 않는다 —
Pages 는 루트 배포가 필요하고 툴체인도 배포 주기도 겹치지 않는다. 잇는 것은 **API 계약**뿐이다.

```
GET /api/branches
  → [ { id:"play33-konkuk", store:"플레이33", branch:"건대점",
        dates:["2026-08-26","2026-08-27","2026-09-01"],   // 고를 수 있는 날짜. 비면 수집 전
        checkedAt:"2026-08-26T05:49:43Z" } , ... ]

GET /api/schedule?branch=play33-konkuk&date=2026-09-01
  → { store:"플레이33", branch:"건대점", date:"2026-09-01",
      checkedAt:"2026-08-26T05:31:48Z",
      themes:[{
        id:"play33-konkuk:18",          // 안정적인 키. 선택 유지와 (나중에) 감시 등록에 쓴다
        name:"목격자",
        place:"플레이33 건대점",
        dur:65,
        genre:"드라마/스릴러", capacity:"2~3인", minPeople:2, maxPeople:3,
        horrorLevel:1, difficulty:2,          // 0.5 단위 — 2.5 · 0.5 가 온다
        posterUrl:"https://play33.kr/storage/theme/…",
        sessions:[ {id:172, t:635, soldout:false}, {id:173, t:1095, soldout:true}, … ]
        //          └ 감시를 걸 때 이 값을 POST /api/watches 에 넣는다
      }] }
```

수집 전인 지점·날짜는 **404** 와 사유 문구를 준다. 빈 배열이 아니라 404 인 이유는
"회차가 없는 날" 과 "아직 안 본 날" 이 화면에서 다른 말이어야 하기 때문이다.

**여기까지는 로그인이 필요 없다.** 아래 감시 API 만 로그인을 요구한다.

지원 매장은 서버가 정한다 — 2026-08-31 기준 **플레이33 4지점 · 키이스케이프 11지점 · 토끼굴 1지점 · 지구별 3지점** (4매장 19지점).
`branch` 응답에 `store` 가 있어서 화면은 `키이스케이프 홍대점` 처럼 매장명을 붙여 보여준다
(세 브랜드가 다 홍대에 있어서 지점명만으로는 구분이 안 된다).

#### 감시 API — "이 자리가 풀리면 알려줘"

```
Authorization: Bearer <Firebase ID 토큰>          ← 이 세 개에만 필요

POST   /api/watches      { slotId: 172 }
  → 201 { id:7, branch:"대전점", theme:"우울해서 빵 샀어",
          date:"2026-08-28", t:600, available:false, createdAt:"…" }
  → 404 그런 회차가 없습니다 / 400 이미 지난 회차입니다
  두 번 걸어도 하나다 (같은 감시를 돌려준다). 새로 만들면 알림이 두 번 간다

GET    /api/watches      → [ WatchDto, … ]   지난 날짜는 빠진다
DELETE /api/watches/{id} → 204                남의 것이면 404 (있다는 사실도 알려 주지 않는다)
```

토큰은 **Floduler 가 이미 쓰는 Firebase Auth 의 ID 토큰 그대로**다.
`cloud.js` 의 로그인 세션에서 꺼내 쓰면 되고, 서버는 구글 공개키로 서명만 검증한다
(서버 저장소 `아키텍처.md` D12).

알림은 같은 자리에 대해 **1시간에 한 번**까지만 간다 (D11).

#### 매핑

| 서버 | 카드 | 비고 |
|---|---|---|
| `name` | `theme.name` | |
| `place` | `theme.place` | 이동시간(F-13)이 이 문자열로 매칭한다. 같은 지점끼리는 값이 같아 **자동으로 0분** |
| `dur` | `theme.dur` | **홈페이지에 적힌 소요시간을 그대로.** 회차 간격으로 보정하지 않는다 (기획 §4.30) |
| `t` | `session.t` | **분 단위 정수.** 카드 내부 형식과 같아 변환이 없다 |
| `session.id` | — | 카드에는 안 들어간다. **감시를 걸 때만** 쓰는 자리 식별자 |
| `soldout` | `session.soldout` | **모델에 이미 있던 필드.** 화면·계산 코드 변경 불필요 |
| `id` / `genre` / `capacity` / `minPeople` / `maxPeople` / `horrorLevel` / `difficulty` / `posterUrl` | 테마 **선택 목록**용 | 카드에는 안 들어가도 된다. 고를 때 보여 주는 값 |

`dur` 은 수동 입력 기본값 70 보다 짧은 값(60~65)이 들어오므로, 불러온 조합은 지금보다 다소 빡빡하게 나온다.

#### 테마 선택 목록에서 보여 줄 것

| 값 | 왜 |
|---|---|
| **`minPeople` ~ `maxPeople`** | ⭐ 우리 인원이 안 맞으면 **애초에 못 가는 테마**다. 회색 처리하거나 걸러낸다 |
| `posterUrl` | 눈으로 고르는 게 이름으로 고르는 것보다 빠르다 |
| `difficulty` (1~5) · `horrorLevel` (0~5) · `genre` | 조합을 고를 때의 판단 근거. **둘 다 0.5 단위 실수다** — `2.5`, `0.5` 가 실제로 온다 |
| 남은 회차 수 (`sessions` 중 `!soldout`) | 매진된 테마를 목록에서 바로 알 수 있다 |

#### 매진 회차는 화면에 남긴다 (§4.13 처리 방식 변경)

§4.13 은 매진 회차를 **조용히 제외**했다. 매진 여부가 데이터에 없어서였다. 이제 들어온다.

**계산에서 빼는 것과 화면에서 지우는 것은 다르다.** 계산에서는 그대로 빼되,
타임라인에는 남겨서 회색·취소선으로 보여 준다. 이유는 두 가지다.

1. 사용자가 "왜 이 시간대가 후보에 없지" 를 알 수 있다 — 조건 탓인지 매진 탓인지가 갈린다
2. **나중에 "이 자리 감시" 를 붙일 자리가 된다.** 파싱 단계에서 버리면 붙일 데가 없다 (기획 §4.30 다음 단계)

#### 지켜야 할 것

- **진입점에 지원 매장을 적는다.** 범용 `가져오기` 버튼으로 만들지 않는다 — 자기 매장을 못 찾은 사용자가
  고장으로 읽는다 (기획 §4.30)
- **`checkedAt` 을 화면에 띄운다.** `3분 전 기준` 같은 배지. 취소표는 시간에 민감해서 신선도가 곧 신뢰다
- **불러온 뒤에도 손으로 고칠 수 있다.** §4.3 과 같다. 고치면 출처가 `불러옴 → 수정함`
- **서버가 죽어도 화면은 산다.** 실패 시 그 자리에만 오류를 표시하고 끝낸다. F-05·F-06 은 무관하게 동작
- **불러오기를 누르기 전까지 관련 코드를 내려받지 않는다** (§2.4 계정과 같은 지연 로딩)
- **서버가 없는 동안에는 버튼 자체를 감춘다.** 배포된 페이지는 누구나 열기 때문에,
  아직 부를 서버가 없는데 버튼만 떠 있으면 처음 온 사람이 눌러서 오류부터 보게 된다
  (§4.22 "시작 화면은 비워 둔다" 와 같은 결).
  로컬에서 열었거나 `localStorage['flod.server']` 를 지정한 사람에게는 보인다.
  ⚠️ 서버를 배포하면 `index.html` 의 `SERVER_DEPLOYED` 와 `server.js` 의 `DEFAULT_BASE` 를
  **함께** 바꾼다. 하나만 바꾸면 버튼이 없거나 엉뚱한 곳을 부른다
- 서버는 HTTPS 여야 한다 — Pages 가 https 라 http 응답은 브라우저가 차단한다.
  서버 쪽 CORS 는 `/api` 경로에만 열려 있고 허용 출처에 `https://choieuihyun.github.io` 가 들어 있다

---

### 4.5 취소표 감시와 알림 (F-16)  — 미착수

F-15 로 불러온 회차 중 **매진된 자리**에 "이 자리 감시" 를 걸면, 나중에 취소표가 났을 때 알림이 온다.
서버 쪽(판정·쿨다운·발송)은 **이미 다 돼 있다.** 여기서 할 일은 두 가지다 —
**무엇을 감시할지 지목하는 것**과 **알림을 받을 주소를 만들어 올리는 것.**

> **왜 브라우저가 해야 하나.** 서버는 "누구에게 무엇을" 까지 안다. 모르는 건 **"어디로"** 다.
> 그 주소(기기 토큰)는 사용자가 알림을 허락해야 발급되므로 **브라우저만 만들 수 있다.**
> 서버가 대신 만들 방법이 없어서, 서버를 다 만들어도 이 조각이 없으면 알림이 안 간다.

#### ㉠ 감시 걸기 — 어느 경로로 가든 필요하다

- [ ] 회차 칩(매진, 취소선)에 **"이 자리 감시"** → `POST /api/watches { slotId }`
- [ ] 이미 건 자리는 표시가 다르게. `GET /api/watches` 로 내 감시 목록을 받는다
- [ ] 해제 → `DELETE /api/watches/{id}`
- [ ] **로그인이 필요하다.** F-12 의 Firebase 로그인을 그대로 쓰고, ID 토큰을
      `Authorization: Bearer …` 로 보낸다. 서버 계약은 그쪽 저장소 참고
- [ ] 로그인 안 한 사람에게는 버튼 대신 "로그인하면 감시할 수 있어요"
- [ ] **서버가 없어도 앱은 그대로 동작해야 한다** (F-15 와 같은 취급 — §2.4)

#### ㉡ 알림 받을 주소 만들기 — **경로를 먼저 골라야 한다**

서버 쪽 저장소 작업명세서의 "알림을 어디로 받을 것인가" 에 갈림길 셋이 정리돼 있다.
요약하면:

| 받는 곳 | 되나 | 조건 |
|---|---|---|
| 안드로이드 크롬 · 데스크톱 | ✅ | 알림 권한 허용만 |
| **아이폰 사파리** | ⚠️ | **홈 화면에 추가**해야 한다 (추정 — 실기 확인 필요) |
| 디스코드 웹훅 | ✅ | **브라우저 작업이 아예 없다.** 대신 사람별로 다른 알림은 안 된다 |

웹 푸시로 갈 경우:

- [ ] `firebase-messaging-sw.js` 를 **저장소 루트에** — 하위 경로에 두면 그 경로만 관장한다
- [ ] `getToken({ vapidKey })` → `POST /api/devices { token, platform }`
- [ ] **앱을 열 때마다 부른다.** 토큰은 조용히 갱신돼서, "한 번 등록하고 끝" 으로 두면
      어느 날부터 알림이 안 오는데 아무 신호도 안 뜬다
- [ ] 권한 요청은 **버튼을 눌렀을 때만.** 페이지를 열자마자 물으면 대부분 거절하고,
      한 번 거절하면 브라우저가 다시 안 물어본다
- [ ] 아이폰도 지원한다면 `manifest.json` + `<link rel="manifest">` +
      **"아이폰은 홈 화면에 추가해야 알림이 옵니다"** 안내
      — 안 적으면 사용자는 버튼을 눌러도 아무 일이 없는 걸 보고 고장이라고 판단한다

#### 한계

- 알림은 **브라우저·기기에 묶인다.** 다른 기기에서 로그인하면 그 기기도 따로 등록해야 한다
- 아이폰에서 홈 화면 아이콘을 지우면 토큰이 죽는다. 서버가 `UNREGISTERED` 를 받아 알아서 지운다
- **조합 단위 감시**(3연방 중 한 칸이 매진일 때 그 조합을 통째로 지켜보기)는 아직 없다.
  서버 쪽 M3.5 이고, 이 화면이 먼저 있어야 붙는다

---

## 5. 출력 사양

### 5.1 조합별 산출 지표

- 시작 / 종료 시각, 총 소요 시간
- 공백 총합, 공백별 개별 값, 최소 공백
- 공백 표준편차 (균등도)
- 포함된 테마 수

### 5.2 타임라인 표기

세로 순서는 **위: 테마(이름 + 시작–종료) / 아래: 공백 / 그 아래: 시간축** 이다.
사용자가 먼저 보는 것은 "무엇을 언제 하는가" 이고 공백은 그 사이 정보이므로 아래에 둔다.

| 요소 | 규칙 |
|---|---|
| 블록 폭 | **소요시간에 비례.** 이름 길이로 늘리지 않는다 — 늘리면 시간축이 거짓말이 된다 |
| 블록 내용 | 테마 이름 + `13:30 – 14:45` (모든 회차의 시작·종료를 보여준다) |
| 공백 | 블록과 같은 높이의 **빗금 영역**. 10분 미만 붉은색(실패 시 위험, 기획 §4.2), 40분 이상 황동색 |
| 공백 라벨 | 항상 막대 **아래**. 영역이 좁아도 옆 블록을 덮지 않는다 |

**이름이 안 들어갈 때** — 블록을 키우는 대신 타임라인 전체를 필요한 배율만큼 넓히고 가로 스크롤한다.
블록이 `%` 로 배치돼 있어 컨테이너만 넓히면 **시간 비율을 유지한 채** 전부 커진다. 4배에서 끊고,
그래도 넘치면 이름을 좌우로 훑는다 (`prefers-reduced-motion` 이면 말줄임).
시각 표기도 좁으면 `시작–종료` → `시작만` → 생략으로 단계적으로 줄인다 — 잘린 시각(`13:30–1`)은
없느니만 못하기 때문이다.

**대비** — 결과 화면의 보조 텍스트에는 `--muted` 대신 한 단계 밝은 `--muted2` 를 쓰고,
블록 배경·테두리도 밝게 잡았다. 어두운 배경에서 잘 안 읽힌다는 지적을 반영한 것이다.

### 5.3 공유 형태

| 형태 | 내용 |
|---|---|
| 링크 | 테마·회차·조건·정렬을 URL 해시에 직렬화. 서버 없이 상태가 오간다 |
| 텍스트 | 결과 한 건을 사람이 읽는 형태로. 카톡·메모에 그대로 붙여넣는 용도 |

```
3연방 · 13:30 → 17:10 (3시간 40분, 공백 5분)

13:30–14:45  범계 (75분)
     ↓ 공백 5분
14:50–16:00  몽 (70분)
     ↓ 공백 0분
16:00–17:10  왓 어 트립 (70분)
```

링크에는 **회차 배열이 아니라 입력 원문(raw)만** 담는다. 원문만 있으면 회차는 다시 파싱되므로
링크가 짧아지고(실측 330자), 나중에 파싱 규칙이 바뀌어도 링크가 덜 깨진다.

### 5.4 결과 나누어 보기

부분 조합을 켜면 결과가 두 층으로 갈린다.

| 층 | 내용 |
|---|---|
| 개수 탭 | `테마 3개 전부 515` / `테마 2개 235` |
| 묶음 칩 | 개수 탭이 전체보다 적을 때만. `전체` / `몽 + 왓 어 트립 87` / `범계 + 몽 75` … |

**나눠야 하는 이유**: 정렬의 1차 키가 "테마 수 내림차순" 이라, 섞어 놓으면 부분 조합은 전체 조합 아래에
전부 파묻힌다. 상위 12개만 노출하므로 사실상 **보이지 않는다.** 탭으로 나눠야 비로소 눈에 들어온다.

### 5.5 정렬 기준 5종

| 키 | 라벨 | 용도 | 정렬식 (1차 키는 전부 테마 수 내림차순) |
|---|---|---|---|
| `gap` | 공백 적은 순 | 시간 효율 최우선 | 공백 총합 ↑, 종료 ↑ |
| `end` | 빨리 끝나는 순 | 종료 시간 최우선 | 종료 ↑, 공백 총합 ↑ |
| `even` | 공백 고른 순 | 리듬 균등 | 표준편차 ↑, 공백 총합 ↑ |
| `late` | 늦게 시작 순 | 오전 일정이 있을 때 | 시작 ↓, 공백 총합 ↑ |
| `safe` | 여유 있는 순 | 실패 대비 (기획 §4.2) | 최소 공백 ↓, 공백 총합 ↑ |

단일 최적해가 아니라 **상위 12개를 노출**한다. 실사용에서 "효율 1등"과 "체감 1등"이 자주 달라지기 때문.

---

## 6. 아키텍처

### 6.1 모듈 구성 (`index.html`)

| 영역 | 함수 | 역할 |
|---|---|---|
| 시간 유틸 | `parseClock`, `fmt`, `pad` | `"13:30"` ↔ 분 단위 정수 변환 |
| 파싱 | `parseSessions`, `sessionsToText` | 텍스트 ↔ 회차 배열 |
| 탐색 | `search` | DFS 조합 생성, 통계 산출 |
| 정렬 | `SORTS` | 정렬 기준 5종 |
| 인식 | `handlePaste`, `readImage`, `getPaddleService`, `preparePaddleCanvas`, `ocr` | 붙여넣기·드롭·첨부 → 크기만 맞춘 캔버스 → PaddleOCR(ONNX/WASM) |
| 계정 | `cloud.js`, `submitAuth`, `submitSave`, `loadPlans` | Firebase Auth·Firestore. 로그인 눌러야 내려온다 |
| 렌더 | `renderThemes`, `renderResults`, `axisTicks` | 입력 카드 / 결과 타임라인 / 시간축 |

### 6.2 `@core` 구간 규약

`index.html` 의 스크립트는 `@core:start` ~ `@core:end` 주석으로 구분된다.

- **구간 안**: DOM을 참조하지 않는 순수 로직 (시간 유틸·파싱·탐색·정렬)
- **구간 밖**: DOM 렌더링, 이벤트, 네트워크

인수 테스트는 이 구간만 잘라내 Node에서 그대로 실행한다. 로직을 테스트용으로 복사하지 않으므로 **앱과 테스트가 어긋날 수 없다.** 구간 안에 `document`/`window`를 넣으면 테스트가 즉시 깨진다 — 의도된 동작이다.

### 6.3 이미지 인식 경로

```
이미지 첨부 → canvas 크기 조정(색은 그대로) → PaddleOCR 검출+인식(ONNX/WASM) → 텍스트
                                                                             ↓
                                                               parseSessions → 회차 배열
```

전 과정이 브라우저 안에서 끝난다. **이미지가 서버로 나가지 않고, API 키도 과금도 없다.**

- 엔진: PaddleOCR (PP-OCRv6 tiny), `ppu-paddle-ocr` + `onnxruntime-web` — tesseract 를 대체
  (기획 §4.25). 학습된 검출기가 색·배경과 무관하게 글자 영역을 먼저 찾으므로 예전처럼
  손으로 짠 이진화 전처리(회색조·임계값 4종 시도)가 필요 없다
- 브라우저가 받는 양: 약 18MB (onnxruntime-web WASM 11MB + 모델 6.1MB + 패키지 JS). `vendor/`
  는 1년 캐시라 첫 1회만
- 실패 시 사유를 카드에 표시하고 수동 입력으로 대체. 조합 계산은 영향 없음
- 타임아웃(모바일 210초 / PC 120초) 초과 시 서비스를 폐기하고 다음 시도에서 새로 만든다
- 오프라인 원칙(§3)을 지키려고 `<script type="importmap">` 으로 `onnxruntime-web` 등
  맨몸 지정자를 `vendor/` 경로에 매핑했다 — 번들러 없이 동적 `import()` 만으로 붙는다

**숫자 화이트리스트를 쓰지 않는 이유** — 글자 후보를 숫자로 제한하면 "오전/오후" 의 한글 획이
숫자로 억지 매핑돼 시각 앞에 들러붙는다 (실측: `3:00`→`23:00`, `6:00`→`56:00`).
제한 없이 두면 한글은 알파벳 잡음이 되고 시각 정규식이 걸러낸다.

**전처리가 인식률의 전부다** — 예약 화면의 회차 글자는 옅은 회색이라 기본 이진화에서 배경으로
묻힌다. 전처리 없이 **0/9**, 회색조+이진화(임계값 235)+확대 후 **9/9** (§9.4).

### 6.4 탐색 알고리즘

DFS + 가지치기. 각 단계에서 `직전 종료 + 최소공백` 이후이면서 조건 필터를 통과하는 회차만 후보로 둔다.

**제약의 두 종류** — 대부분의 조건은 후보를 **줄이는** 것이라 가지치기로 처리한다.
그러나 식사 공백은 "어딘가에 큰 공백이 **있어야** 한다" 는 존재 조건이라 경로가 완성돼야 판정할 수 있다.
그래서 이것만 가지치기가 아니라 `record()` 에서 거른다.

**순서는 카드 배치 순서로 고정한다** (기획 §4.14 후기, 전면 변경 — 예전엔 "자리 잠금"을 건 카드만 그 자리에 묶고 나머지는 탐색기가 순서까지 찾았다). 탐색은 `list[i]` 를 인덱스 오름차순으로만 훑는다 — 되돌아가지 않으므로 방문 여부를 `used[]` 로 따로 셀 필요가 없고, "이 순서에서 어떤 카드를 건너뛸지" 만 갈린다. 부분 조합(F-08)은 이 순서를 지킨 채 일부 카드만 빼는 것이지, 재배열이 아니다.

순서를 셀 때는 **회차가 있는 카드만** 센다. 회차가 없는 카드는 일정에 들어가지 않기 때문이다.

폭주 방지 상한: 노드 800,000 / 결과 50,000개. 도달하면 결과 개수 옆에 **"탐색 한도 도달"** 을 표시한다 (조용히 자르지 않는다). 순서 탐색이 빠지면서 탐색 공간이 `N!×M^N` 에서 `M^N` 로 줄어, 요구 범위(테마 3~5개)에서는 사실상 상한에 걸리지 않는다 (실측 §8).

---


### 6.5 서버 구조

**우리 코드가 도는 서버는 0대다.** 백엔드를 안 만든 게 아니라, 백엔드가 할 일을
남의 서비스 두 개에 나눠 맡겼다.

```
        ┌──────────── 사용자 브라우저 ────────────┐
        │  index.html   조합 탐색 · 렌더 · 상태    │
        │  PaddleOCR    이미지 인식 (ONNX/WASM)   │
        │  cloud.js     Firebase SDK (지연 로드)   │
        └───┬─────────────────────────┬──────────┘
            │ ① 파일 받기               │ ② 데이터 주고받기
            ▼                          ▼
   ┌──────────────────┐   ┌───────────────────────────────┐
   │  GitHub Pages    │   │  Firebase (구글)               │
   │  정적 파일만 서빙  │   │   Auth       계정 · 비밀번호     │
   │  코드 실행 없음    │   │   Firestore  일정 문서          │
   │  항상 켜져 있음    │   │   Rules      ← 백엔드 로직이 여기 │
   └──────────────────┘   └───────────────────────────────┘
```

**① 파일** — GitHub Pages 는 `main` 브랜치의 파일을 그대로 내보내는 CDN 이다.
푸시하면 갱신되고, 우리가 켜고 끌 것이 없다.

**② 데이터** — 보통이라면 브라우저 → 우리 서버 → DB 인데, 여기서는 **브라우저가 DB 에 직접 붙는다.**
중간에서 "이 사람이 이 문서를 봐도 되는가" 를 판단하던 자리를 `firestore.rules` 가 대신한다.
**규칙이 곧 백엔드다** — 그게 느슨하면 서버 코드가 뚫린 것과 똑같다 (§8.1).

`serve.mjs` 는 **로컬 개발용이고 배포되지 않는다.** 이 그림 어디에도 없다.

#### 이 구조가 못 하는 일

서버가 없으므로 서버에서만 되는 일은 못 한다.

| 못 하는 것 | 왜 |
|---|---|
| 비밀키가 필요한 외부 API 호출 | 브라우저에 키를 두면 공개된다 |
| 정해진 시각에 도는 작업 | 아무도 안 켜져 있다 |
| 전체 사용자 집계·통계 | 각자 자기 문서만 읽을 수 있다 |
| 가입 자체를 막기 | `apiKey` 만으로 REST 가입이 가능하다 (§8.1) |

지금 요구에는 하나도 필요 없다. 필요해지는 순간 Cloud Functions 를 붙이면 되고,
그때가 이 구조를 벗어나는 시점이다.

---

## 7. 결과 데이터 구조

```js
{
  steps: [{ i, name, place, dur, start, end, soldout, move }],  // move = 앞 테마에서 오는 이동시간
  count,          // 포함 테마 수
  start, end,     // 전체 시작 / 종료 (분 단위)
  total,          // 공백 총합
  gaps: [],       // 개별 공백
  varc,           // 공백 표준편차
  minGap,         // 최소 공백 (단일 테마면 Infinity)
  minWait,        // 이동을 뺀 최소 공백 — 화면의 "최소" 는 이 값이다
  moveTotal       // 이동시간 합계
}
```

### 7.05 이동시간 조회

```js
o.moveMin            // 기본 이동시간 (분)
o.moveMap            // { pairKey(A,B): 분 } — 손대서 정한 쌍만 들어 있다
pairKey(a,b)         // 정렬해서 만든 키. A↔B 와 B↔A 가 같은 칸을 쓴다
moveCost(a,b,min,map)
  a 또는 b 가 빈칸  → 0     (모르는 것을 지어내지 않는다)
  a === b          → 0     (같은 매장)
  map 에 있으면     → 그 값
  없으면           → min   (기본값)
```

공유 링크에는 **지금 화면에 있는 매장 쌍만** 담는다 (`m: [[A,B,분], …]`).
지운 매장의 값까지 끌고 다닐 이유가 없다.

---

### 7.1 저장되는 일정 문서 (F-12)

`users/{uid}/plans/{자동id}` — 규칙상 본인 외에는 읽기조차 안 된다.

```js
{
  date:   '2026-08-24',        // 사용자가 고른 날짜
  status: 'wish' | 'done',     // 하고 싶은 / 했던
  memo:   '강남 3연방, 지수랑',

  // ── 사실로 굳힌 부분 ──────────────────────
  steps:  [{ name, dur, start, end }],   // 확정된 시각
  count, start, end, total,
  minGap,                       // Infinity 는 null 로 (Firestore 가 못 받는다)

  // ── 다시 짜 보기용 ────────────────────────
  snapshot: '<serialize() 결과>',
  createdAt,
}
```

**`steps` 와 `snapshot` 을 함께 두는 이유.**
`snapshot` 은 *풀이의 입력값*이라 다시 계산해야 시간표가 나온다.
그런데 `search()` 나 `KEEP_CAP` 이 바뀌면 같은 입력에서 다른 1위가 나올 수 있다 —
**지나간 일정이 조용히 다른 일정으로 바뀌는 것**은 기록으로서 실격이다.
그래서 확정 시각은 사실로 굳혀 두고, `snapshot` 은 "이 조건으로 다시 짜기" 에만 쓴다.
`soldout` 같은 `undefined` 가 섞이면 Firestore 가 문서를 거부하므로 필드를 명시적으로 골라 담는다.

**`snapshot` 은 저장 순간이 아니라 계산 순간의 입력값이다.**
결과 카드는 계산 뒤에도 화면에 남아 있고, 그 사이 회차를 고칠 수 있다.
저장할 때 `serialize()` 를 새로 부르면 **확정 시각은 계산 A 것, 입력값은 수정 B 것**인
문서가 조용히 만들어져 불러와도 그 일정이 재현되지 않는다.
그래서 `go` 시점에 `lastSnapshot` 으로 붙잡아 두고 저장은 그것을 쓴다.
같은 이유로 `불러오기` 는 옛 결과 카드를 지운다 — 남겨두면 그 `저장` 버튼이 이전 계산을 붙들고 있다.

---

## 8. 비기능 요구

| 항목 | 기준 | 결과 |
|---|---|---|
| 빌드 도구 | 없음 | 충족 — 파일을 그대로 연다 |
| 외부 라이브러리 | 없음 (웹폰트 제외) | **포기** — 아래 참조 |
| 모바일 반응형 | 560px 이하 대응 | 충족 |
| 접근성 | `prefers-reduced-motion` 대응 | 충족 |
| 계산 속도 | 3~5개 테마 100ms 이내 | 충족 — 아래 실측 |

원칙 하나를 의도적으로 깼다. **이미지 자동 인식과 "외부 라이브러리 없음" 은 양립하지 않는다.**
브라우저에 표준 OCR 기능이 없어 선택지는 (a) AI 비전 API — 키·과금 필요, (b) 인식 엔진 탑재
둘뿐이다. 과금 없이 어느 기기에서나 돌아야 한다는 요구가 우선이라 (b)를 택했다.
대신 엔진을 저장소에 넣어 **CDN·빌드·설치는 여전히 없고 오프라인에서도 돈다** — 원칙의 의도인
"열면 그냥 돌아간다" 는 지켰다. `npm` 의존성은 여전히 0개다.

탐색 실측 (회차 9~10개 / 테마, 최소 공백 0, 매진 제외 해제):

| 테마 수 | 조합 수 | 시간 | 상한 도달 |
|---|---|---|---|
| 3 | 92 | 0.4ms | 아니오 |
| 4 | 91 | 0.4ms | 아니오 |
| 5 | 91 | 0.7ms | 아니오 |
| 6 | 63 | 1.4ms | 아니오 |
| 7 | 7 | 1.3ms | 아니오 |

**이 표는 기획 §4.14 후기(카드 순서 고정) 이후 값으로 다시 쟀다.** 예전엔(순서까지 탐색) 테마 5개에서 10,664개·10ms, 7개부터 상한(50,000)에 도달했다 — 순열(`N!`)이 빠지면서 조합 수가 자릿수 단위로 줄었고, 테마가 늘수록 오히려 결과가 **줄어드는** 경향까지 보인다(뒤쪽 카드일수록 앞선 고정 순서가 이미 써버린 시간대와 충돌할 여지가 커지기 때문). 요구 범위(3~5개)는 여전히 전수 탐색이면서 100ms 와 비교할 수 없을 만큼 빠르다.

---


### 8.1 접근 통제 (F-12)

| 층 | 무엇을 막는가 |
|---|---|
| `firestore.rules` — `uid` 일치 | 로그인해도 남의 문서는 못 읽고 못 쓴다 |
| `firestore.rules` — 이메일 도메인 | 화면을 안 거친 계정은 **자기 문서에도** 못 쓴다 |
| Firebase Auth | 비밀번호는 우리가 만지지 않고 해시되어 보관된다 |
| 익명 로그인 사용 중지 | 자격증명 없이 세션이 생기는 길을 닫았다 |

**막지 못하는 것: 계정 생성 자체.**
`apiKey` 는 페이지 소스에 그대로 있고, 그것만으로 `accounts:signUp` 을 직접 부를 수 있다.
화면의 아이디 형식 검사는 화면에서만 돈다. 실제로 `attacker.probe@gmail.com` 을 만들어 확인했다.

이건 결함이 아니라 **가입이 열린 모든 Firebase 앱의 기본 상태**다. 구글 로그인으로 바꿔도 같다.
유출 위험은 없고(위 두 층), 남는 것은 **하루 쓰기 한도(2만)를 태워 저장을 막는** 영업방해 쪽이다.
도메인 제한이 그 길을 좁혔고, 근본 대응인 **App Check(reCAPTCHA)는 사람이 붙은 뒤에** 한다 —
지금 붙이면 쓰지도 않는 방어에 설정과 의존성만 늘어난다.

---


### 8.2 가독성 (명도 대비)

디자인을 바꾼 뒤 **실제로 렌더된 화면에서** 글자색과 그 뒤에 실제로 깔린 배경색을 읽어
WCAG 대비를 계산했다 (부모를 거슬러 올라가 불투명한 배경을 찾는 방식 — 눈으로 보는 것과 같다).

| | 1차 (교체 직후) | 2차 (보정 후) |
|---|---|---|
| 측정 항목 | 30 | 30 |
| 기준 미달 | **12** | **0** |
| 아슬 (기준 −1.2 이내) | 4 | **0** |

바꾼 것은 색 값 몇 개뿐이다.

- `--ink3` `#8B95A1` → `#626C7A` — 흐린 회색 하나가 설명문·지표·요약·메모에 전부 쓰이고 있어
  이것만으로 미달 8건이 사라졌다. 페이지 바닥(`#F2F4F6`)에서도 4.5 를 넘는 값으로 잡았다.
- `--blue` `#3182F6` → `#2670EB` — 흰 글자를 얹는 주 버튼이 3.71 이었다. 눈에는 같은 파랑인데 4.57 이 된다.
- 옅은 바탕에 얹는 글자는 따로 진한 값을 뒀다 — `--red-text` · `--green-text` · `--amber-text`.
  태그·경고·공백 라벨이 여기 해당한다. 테두리나 배경에는 원래의 밝은 값을 그대로 쓴다.
- 시간축 눈금 `#B0B8C1` 은 2.01 로 가장 나빴다. 시각은 정보이지 장식이 아니라서 본문 회색으로 올렸다.

**대비는 눈으로 못 잡는다.** 12건 중 어느 것도 화면을 볼 때는 거슬리지 않았다.
색을 바꿀 때마다 이 측정을 다시 돌린다.

---

## 9. 검증

### 9.1 인수 기준

기획 §2.3의 수기 최적해를 기준값으로 사용한다.

입력: 범계 75분 / 몽 70분 / 왓 어 트립 70분, 최소 공백 0, 매진 제외 해제
기대: **13:30 범계 → 14:50 몽 → 16:00 왓 어 트립** (공백 5분, 17:10 종료) 가 "공백 적은 순" 1위

`npm test` 결과 — **44개 검사 전부 통과**:

| 그룹 | 검사 내용 |
|---|---|
| 회차 텍스트 파싱 | 12시간 보정, 오전·오후 + 매진 플래그, 정오/자정 경계, 인접/떨어진 중복, 달력 숫자 배제, 보정 표시, 자정 넘김 |
| 인수 기준 | 조합 515개 생성, 한도 미도달, 1위 = 기준 최적해, 공백 총합·개별값·종료·최소공백 일치 |
| 조건 필터 | 최소 공백, 시간 창, 매진 제외, 부분 조합 |
| 식사 공백·순서 | 창 안에 N분 확보, 최대 공백과의 모순, 첫 타/마지막 고정, 둘 동시 적용 |
| 비기능 | 테마 3개·5개 계산 100ms 이내, 5개 전수 탐색 확인 |
| 정렬 기준 | 5종 존재, 각 1위가 전체 테마 포함, `end`·`safe` 기준의 단조성 |

### 9.2 발견하고 고친 결함

테스트를 붙이는 과정에서 결함 세 건이 드러났다.

#### (1) 중복 회차가 없는 회차로 둔갑

**증상**: `10:30 10:30 12:00` → `10:30, 22:30, 00:00`
**원인**: 중복 제거가 12시간 보정 **뒤에** 있었다. 보정 루프는 "이전보다 작거나 같으면 +12시간"이므로, 중복된 `10:30` 이 같은 값으로 걸려 `22:30` 이 되고, 그 뒤 `12:00` 이 다시 밀려 `00:00` 이 됐다.
**영향**: 존재하지 않는 회차가 조용히 만들어져, 예약 불가능한 조합이 결과에 섞인다.
**수정**: 중복 제거를 보정 앞으로 옮기고, 접을 때 매진 플래그를 OR로 합치도록 했다 (§4.1-4). 회귀 테스트 포함.

#### (2) 결과 상한이 요구 범위 안에서 이미 작동

**증상**: 테마 5개 탐색이 결과 6,000개에서 잘렸다 (`KEEP_CAP`).
**원인**: 상한이 폭주 방지용치고 너무 낮았다. 실제 5개 테마의 전체 공간은 10,664개로, 잘린 양이 44%다.
**영향**: "겹치지 않는 모든 조합" (F-03) 이 요구 범위인 3~5개 테마 안에서 이미 깨진다. DFS는 앞쪽 가지부터 채우므로 잘려나가는 쪽이 한쪽으로 치우친다 — 이번 데이터에서는 최적해가 우연히 살아남았지만 보장은 없다.
**수정**: `KEEP_CAP` 6,000 → 50,000. 테마 5개가 10ms에 전수 탐색되고, 상한은 7개부터 작동한다 (§8). 도달 시 표시는 그대로 유지.

#### (3) 중복 처리가 "오후 한 바퀴" 를 삼킴

**증상**: 숫자만 인식된 `10:30 … 9:00 10:30` 에서 마지막 `10:30`(=22:30)이 사라져 9개 중 8개만 남음.
**원인**: (1)의 수정에서 중복을 **값 기준으로 전부** 접었다. 오전 10:30 과 오후 10:30 은 숫자가 같아 함께 접혔다.
**수정**: **인접한** 중복만 접는다. 같은 칸을 두 번 읽은 노이즈는 붙어서 나오고 오후 한 바퀴는 떨어져서 나온다 — 인접 여부가 둘을 가른다. 두 경우 모두 회귀 테스트에 있다.

#### (4) 공유 링크가 자기 화면을 덮어씀

**증상**: `링크 복사` 를 한 번 누르면 그 뒤로 새로고침할 때마다 기본 데이터가 사라지고 그때의 상태가 되살아난다.
**원인**: 복사할 URL 을 만들려고 `location.hash` 를 직접 바꿨다. 해시는 남아 있고, 로드 때 그 해시를 복원하므로 **공유하려고 한 번 누른 대가로 그 탭이 영영 그 상태에 묶인다.**
**수정**: URL 문자열만 만들어 복사하고 주소창은 건드리지 않는다(클립보드가 막힌 경우에만 대체). 빈 링크로 들어오면 기본 데이터를 유지하고, `기본값 채우기` 버튼으로 언제든 되돌린다.
**교훈**: 주소창은 앱의 상태 저장소이기도 하다. 거기에 쓰는 것은 "지금 화면" 을 바꾸는 일이지 "복사할 문자열" 을 만드는 일이 아니다.

### 9.3 이미지 인식 실측

실제 예약 화면과 같은 구조의 화면을 만들어 측정했다 (범계 8/22, 회차 9개).

| 조건 | 결과 |
|---|---|
| 전처리 없음 | **0/9** — 회색 글자를 통째로 놓침 |
| 전처리(임계값 200) | 9개 인식, 3개 오독 |
| **전처리(임계값 235)** | **9/9 정확** — PSM 3·6·11 모두 동일 |
| 전처리(임계값 250) | 8~9개, 불안정 |
| 숫자 화이트리스트 사용 | `3:00`→`23:00` 등 오독 |

OCR 출력 `10:30 12:00 1:30 3:00 4:30 6:00 7:30 9:00 10:30` 을 `parseSessions` 에 통과시키면
`10:30 12:00 13:30 15:00 16:30 18:00 19:30 21:00 22:30` — 정답과 완전 일치.

### 9.4 브라우저 동작 확인

**실제 예약 화면 캡처로 회차가 채워지는 것을 확인했다** (사용자 확인).

여기까지 오는 데 세 가지를 고쳐야 했다. 헤드리스 브라우저에서 인식이 180초가 지나도 끝나지 않아
원인 후보를 모두 제거한 것이다.

| 고친 것 | 내용 |
|---|---|
| `recognize()` 인자 | 4번째 인자는 로거가 아니라 작업 ID다. 객체를 넘겨 작업 추적이 깨졌다 |
| 상대경로 | 워커 내부에서는 기준 경로가 달라 엔진 파일을 못 찾고 멈출 수 있다 → 절대 URL 고정 |
| 이미지 과대 | 확대가 680만 화소까지 커졌다 → 가로 1200px 목표 + 총 320만 화소 상한 |

더불어 2분 타임아웃과 워커 폐기·재생성을 넣어, 실패해도 무한 대기에 빠지지 않게 했다.
`vendor/` 캐시 정책도 함께 고쳤다 — 서버가 모든 응답에 `no-store` 를 걸고 있어 이미지를 넣을
때마다 5MB를 다시 받고 있었다.

### 9.5 수치화하지 않은 것

정확도 수치(9/9)는 **복제 화면 기준**이다. 실제 캡처는 "동작한다" 까지만 확인했고 회차 단위
정확도를 세어 보지는 않았다. 다른 예약 사이트·다른 배색에서의 인식률도 측정 범위 밖이다.

---

### 9.6 계정 기능 확인 (F-12)

실제 Firebase 프로젝트가 없어 **가짜 키를 넣고 UI 배선까지** 확인했다.
저장·조회 왕복은 §2.4 설정 후에 확인해야 한다 — 아래는 확인한 것과 못 한 것을 나눈 것이다.

**확인함** (헤드리스 크롬):

| 항목 | 결과 |
|---|---|
| 설정 전 `CLOUD_ON` | `false` — 계정 줄에 "일정 저장 꺼짐" |
| 설정 전 카드·계산 | 카드 3개, **467개 조합** — 기능 추가 이전과 동일 |
| 설정 전 저장 버튼 | 0개 (안 뜬다) |
| **설정 전 추가 네트워크 요청** | **0건** — `cloud.js` 도 Firebase SDK 도 안 내려온다 |
| 설정 후 저장 버튼 | 결과 12건 각각에 1개 |
| 로그인 모달 · 가입 전환 | 열림 / 제목·버튼 문구 전환됨 |
| 아이디 형식 반려 | `ab` → "영문·숫자 3~20자…" |
| 비밀번호 길이 반려 | `123` → "6자 이상이어야 합니다" |
| `cloud.js` 지연 로드 | 로그인 버튼을 누른 뒤에야 1건 |
| 로그아웃 상태에서 저장 | 로그인 모달로 유도됨 |
| 콘솔 오류 | 없음 |
| **`snapshot` 이 계산 시점에 고정되는가** | 계산 → 회차 수정 → 저장 시, 저장되는 입력값은 **계산 시점 값** |
| 저장한 것을 되살리면 그 일정이 나오는가 | 되살려 다시 계산한 1위가 저장된 `steps` 와 동일 |

가장 중요한 줄은 **설정 전 추가 네트워크 요청 0건**이다.
계정 기능을 붙이면서 안 쓰는 사람의 앱이 무거워지지 않았다는 뜻이고, 이게 지켜지지 않았다면
기능을 되물렸어야 한다 (기획 §4.18).

**실제 프로젝트(`roomescapescheduler`)에서 확인함** — REST 로 왕복:

| 항목 | 결과 |
|---|---|
| 가입 (아이디 2개) | 성공 |
| 같은 아이디로 다시 로그인 | 성공, `uid` 동일 |
| 일정 저장 | 성공 |
| 목록 조회 | 1건 — 날짜·상태·`steps` 그대로 |
| 상태 전환 (`done`→`wish`) | 성공 |
| 삭제 | 성공, 남은 문서 0건 |
| **B 가 A 의 목록 읽기** | `PERMISSION_DENIED` |
| **B 가 A 에게 쓰기** | `PERMISSION_DENIED` |
| **로그인 없이 읽기** | `PERMISSION_DENIED` |
| **`@gmail.com` 계정이 자기 uid 에 쓰기** | `PERMISSION_DENIED` (도메인 제한, §8.1) |
| 정상 계정(`@user.resched`)이 쓰기 | 성공 — 제한이 정상 사용자를 막지 않는다 |

점검용 계정은 매번 만들고 지웠다.

**검증 순서에서 배운 것.** 규칙을 게시하기 전에 같은 검사를 돌렸을 때 "남의 문서 막힘" 세 줄이
이미 초록이었다. 규칙이 일해서가 아니라 **Firestore 가 전부 거부하고 있었기 때문**이다.
그전에는 데이터베이스 자체가 없어서 또 같은 `PERMISSION_DENIED` 가 왔다.
**한 가지 코드가 세 가지 원인을 가리고 있었다** — 거부를 성공의 증거로 읽으면 안 된다.
음성 결과는 기대한 것이 막혔을 때가 아니라, **기대하지 않은 것이 통과할 때** 비로소 의미를 갖는다.

인수 테스트 44개는 여전히 전부 통과 — `@core` 구간은 손대지 않았다.

---

## 10. 이번 작업 내역

프로토타입(`escape-schedule.html`) → 웹 프로젝트 세팅.

| # | 작업 | 내용 |
|---|---|---|
| 1 | 파일 구성 | `escape-schedule.html` → `index.html` (호스팅 기본 문서) |
| 2 | 한글 텍스트 복원 | 전달 과정에서 인코딩이 깨진 한글 전량 재작성 (§11.1) |
| 3 | OCR 경로 재설계 | 브라우저 직접 호출 → 같은 출처 프록시 경유, 키 서버 보관 (§6.3) |
| 4 | 모델 ID 수정 | 존재하지 않는 `claude-sonnet-4-6` 문자열 → `claude-opus-5`, `effort: "low"` 지정 |
| 5 | 오류 처리 보강 | 프록시 없음 / 키 없음 / 응답 거절 각각의 사유 표시 |
| 6 | 개발 서버 | `serve.mjs` — 정적 서빙, 의존성 0개, 경로 탈출 차단 |
| 7 | 인수 테스트 | `test/acceptance.mjs` — `@core` 구간 직접 실행, 26개 검사 |
| 8 | 결함 수정 | 중복 회차 + 12시간 보정 상호작용 버그, 결과 상한 상향 (§9.2) |
| 9 | 문서화 | `기획.md`, `작업명세서.md` 신규 / `requirements.md` 복원 |
| 10 | 저장소 | `git init`, `.gitignore` (`.env` 포함) |
| 11 | 접속 범위 제한 | 개발 서버를 `127.0.0.1` 로 고정 — 공용 망에서 타인 접속 차단 (§2.1) |
| 12 | 테마명 확정 | 역산으로 좁혀지지 않던 `범계` 를 사용자 확인으로 확정 (§11.1) |
| 13 | 이미지 첨부 | 카드에 "이미지 첨부" 버튼 — 붙여넣기·드롭만으로는 폰에서 불가 |
| 14 | 인식 방식 전환 | AI 비전 API → 브라우저 내 tesseract. 키·과금·서버 없이 동작 (§6.3) |
| 15 | 전처리 도입 | 회색조·이진화·확대. 인식률 0/9 → 9/9 (§9.3) |
| 16 | 파서 보정 | 인접 중복만 접고 떨어진 중복은 보존 (§9.2-3) |
| 17 | 타임라인 개선 | 공백을 빗금 영역으로 강조, 긴 테마명은 자르지 않고 자동 가로 스크롤 |
| 18 | 입력 피드백 | 해석 결과를 입력 즉시 칩으로 표시, 출처 배지, 12시간 보정·자정 넘김 경고 (§4.2) |
| 19 | 연방 수 노출 | 헤더에 `N연방` 배지, 추가 버튼 옆 안내, 빈 상태 문구. 기능은 원래 있었으나 안 보였다 |
| 20 | 결과 가독성 | 블록에 시작–종료 표기, 공백 라벨을 아래로(위아래 교체), 보조 텍스트·블록 대비 상향 |
| 21 | 타임라인 확장 | 이름이 안 들어가면 전체를 넓히고 가로 스크롤 (시간 비율 유지, 4배 상한) |
| 22 | 식사 공백 (F-09) | 지정 시간대에 N분 이상 공백을 요구. 최대 공백과 모순이면 계산 전에 알림 |
| 23 | 순서 제약 (F-10) | 카드 드래그 + 자리 잠금. 성격 가중치 모델을 대체 |
| 24 | 결과 공유 (F-11) | 상태 링크 + 텍스트 복사 |
| 25 | 지금부터 | 시작 하한에 현재 시각을 넣는 버튼 |
| 26 | 매진 옵션 제거 | `매진 회차 제외` 체크박스를 없애고 "빼고 넣어 주세요" 안내로 대체 (기획 §4.13) |
| 27 | 드래그 + 자리 잠금 | `첫 타/마지막` 드롭다운을 걷어내고 드래그·잠금으로 통합 (기획 §4.14) |
| 28 | 결과 탭 (F-08) | 테마 수 탭 + 테마 묶음 칩. 부분 조합이 전체 조합에 파묻히던 문제 해결 (§5.4) |
| 29 | 기본 데이터 | 실제 매장 3개 테마를 채워둔 상태로 시작 — 열자마자 눌러볼 수 있게 |
| 30 | 테마명 재정정 | `에이트립` → **`왓 어 트립`** (사용자 확인). 역산이 두 번째로 틀린 사례 (§11.1) |
| 31 | 링크 복사 결함 수정 | 주소창을 덮어써 기본 데이터가 사라지던 문제. `기본값 채우기` 버튼 추가 (§9.2-4) |
| 32 | 프록시 제거 | 브라우저 인식으로 바뀐 뒤 죽어 있던 `/api/ocr` 과 API 키 처리를 걷어냈다 |
| 33 | 배포 준비 | 목표가 **동호회 공용**으로 분명해짐 (기획 §4.17). `.nojekyll` 추가, README 에 배포·공유 절차, §2.2 에 배포 후 확인 항목 |
| 34 | 계정·일정 보관 (F-12) | 아이디/비번 로그인 + Firestore. Supabase 대신 Firebase 를 고른 근거는 기획 §4.18. 로그아웃 경로는 요청 수까지 예전과 동일 (§9.6) |
| 35 | 저장 정합성 수정 | `snapshot` 을 계산 시점에 고정. 계산 뒤 입력을 고치고 저장하면 확정 시각과 입력값이 어긋나던 결함 (§7.1). `불러오기` 가 옛 결과를 치우도록, 아이디 대소문자 통일 |
| 36 | 계정 기능 개통 | Firebase 프로젝트 연결, Authentication·Firestore·규칙 게시. 실제 왕복 전부 확인 (§9.6). 익명 로그인은 껐고, 이메일 도메인 제한을 규칙에 넣었다 (§8.1) |
| 37 | 디자인 전면 교체 | 어두운 빈티지 → 밝은 현대형(토스 계열). 색·서체·라운드·그림자 전부. 마크업과 클래스명은 그대로 두어 JS 는 손대지 않았다 (기획 §4.19) |
| 38 | 가독성 보정 | 렌더된 화면에서 대비를 측정해 미달 12건 → 0건 (§8.2). 색 값만 바꿨다 |
| 39 | 모바일 인식 보강 | 화소 예산 절반·제한시간 210초, HEIC 안내, 실패 문구에 실제 크기와 소요 초를 남김 (§11.2) |
| 40 | 인식 전처리 다중화 | 실패 원인이 시간 초과가 아니라 "글자를 못 찾음"으로 판명. 화소 예산 축소를 되돌리고(글자만 작아졌다) 전처리 4종을 돌려 **가장 많이 읽은 것**을 채택하도록 바꿨다 |
| 41 | 매장 간 이동시간 (F-13) | 행렬 대신 **매장명 + 기본값**. 같은 매장 연타가 자동으로 0분이 되는 게 결정적이었다 (기획 §4.7). 탐색은 가지치기로, 표시는 공백 막대·라벨·요약줄·복사 텍스트 네 곳에 |
| 42 | 쌍별 이동시간 | 기본값 하나로는 "A↔B 10분, B↔C 7분"을 못 맞춘다. 매장이 둘 이상이면 쌍 표가 자동으로 생기고, 손대지 않은 칸은 기본값을 따라간다. 방향은 구분하지 않는다 |
| 43 | 이동·잔여 구분 | 색이 아니라 **모양**으로 가른다 — 실선=이동, 빗금=잔여. 숫자도 `이동 8분 · 잔여 17분` 으로 쪼개 네 곳에 표시 (기획 §4.7) |
| 44 | 이미지 다장 첨부 | 두 번째 이미지가 첫 번째를 덮어쓰던 것을 고쳤다. 텍스트 이어붙이기 → 재해석 방식으로 1차 구현 |
| 45 | 다장 첨부 순서 버그 수정 | "합치기 처리는 잘 되나?" 질문에 순서를 꼬아 재현: 오후→오전 순으로 첨부하면 오전 시각이 12시간 밀렸고, 같은 이미지를 두 번 넣으면 새벽 시각이 생겼다. 텍스트 병합을 절대시각 세션 배열 병합으로 교체해 해결 (기획 §4.20) |
| 46 | 사진 인식 경고 | 사진에서 읽은 테마는 해석 결과 아래에 항상 경고("인식이 부정확할 수 있습니다")를 띄운다. 손으로 고친 뒤(image-edited)에도 유지, 직접 입력에는 안 뜸 (기획 §4.21) |
| 47 | 이어 붙이기 체크박스 | "테마를 지웠다 다시 넣어야 안 합쳐지는 구조" 지적. 카드마다 켜고 끌 수 있는 체크박스 추가 — 기본 켜짐, 끄면 다음 첨부가 있던 값을 지우고 덮어쓴다. 한 번에 여러 장 고른 경우 그 장들끼리는 여전히 합쳐짐 (기획 §4.20) |
| 48 | 체크박스 이름 변경 · 시작 화면을 비움 | `이어 붙이기` → **`이미지 여러장 넣기`**. 열자마자 예시 매장 3개(29번)가 채워져 있던 것을 없애고 빈 화면으로 시작, `기본값 채우기` 버튼을 **`테스트 데이터 넣기`** 로 이름을 바꿔 눌러야만 채워지게 함. 공유 링크가 깨졌을 때의 안전망도 예시 데이터 대신 빈 화면으로 통일 (기획 §4.22) |
| 49 | 사용법 투어 | 외부 라이브러리 없이 직접 짠 스포트라이트 코치마크. 테스트 데이터 → 테마 추가 → 시간표 → 조건 → 계산 → 결과 → 공유, 7단계. 처음 온 사람에게 자동으로 뜨고(`localStorage`), 헤더의 `사용법 보기` 버튼으로 언제든 다시 볼 수 있다 (기획 §4.23) |
| 50 | 이름 변경 | `방탈출 스케줄러` → **`Floduler`**. `<title>`, `<h1>`, README·기획·요구사항 문서 제목, `package.json` description 까지 전부 바꿨다. 저장소 이름(`RoomEscapeScheduler`) 과 GitHub Pages 주소는 그대로 둔다 — 이미 쓰고 있는 사람이 있으면 링크가 깨진다 |
| 51 | 다크 모드 | 시스템 설정 자동 + 헤더 버튼으로 수동 전환, 선택은 `localStorage` 에 남아 시스템 설정보다 우선한다. 색이 전부 토큰이라 팔레트 하나만 더 정의하면 됐지만, `--ink`/`--blue` 처럼 "글자색"과 "흰 글자용 배경색"을 겸하던 토큰 몇 개는 다크에서 방향이 갈려 `--strong`/`--strong-ink`, `--blue-hover`, `--blue-text` 로 쪼갰다. 새 팔레트 전부 WCAG 대비 실측 후 반영 (기획 §4.24) |
| 52 | 인식 엔진 교체 (tesseract → PaddleOCR) | PP-OCRv6 tiny(6.1MB) — 172MB "server" 모델보다도 정확하고 빨랐다(실측 표는 기획 §4.25). 학습된 검출기가 색·배경과 무관하게 글자를 찾아 예전의 4단계 이진화 전처리가 통째로 필요 없어졌다. `test/fixtures/` 두 장으로 tesseract 와 나란히 검증 — 카톡 캡처에서 tesseract 가 놓친 회차(15:50)까지 잡았다. 번들러 없이 importmap 으로 vendor 경로에 붙였고(GitHub Pages, COOP/COEP 없이 단일 스레드 WASM), tesseract 코드·벤더 파일은 검증 후 삭제 |
| 53 | `localStorage` 재방문 복구 | 공유 링크(`#s=`)가 없을 때만 마지막 자동 저장 상태를 대신 복구한다. `serialize()`/`restore()` 를 그대로 재사용해 포맷을 두 벌 안 만들었다. 카드 추가/삭제/잠금/정렬처럼 `renderThemes()` 를 타는 조작은 거기서, 입력 중 리렌더를 피하는 텍스트 입력칸들은 `document` 위임 `input`/`change` 리스너로 잡는다(400ms 디바운스). 깨진 값은 공유 링크와 같은 방식으로 조용히 빈 화면 (기획 §4.26) |
| 54 | 정렬 칩 카운트 → "더 보기" 로 대체 | 정렬 칩에 결과 개수를 붙였다가(다섯 칩이 항상 같은 숫자라 실효가 없어) 도로 빼고, "결과" 제목 옆(`#resCount`)에 `상위 12개만 표시됩니다 · 전체 N개 중` 안내문 + 목록 아래 **더 보기** 버튼(12개씩 추가, 새로 열린 첫 카드로 자동 스크롤)으로 바꿨다. 탭·정렬·재계산 시 `showCount` 를 12로 리셋. §6 열린 질문("상위 12개가 적정한가")을 이걸로 닫았다. 다크 모드에서 선택된 탭 카운트 글자가 안 보이던 버그(`.tab.on b` 흰색 하드코딩)는 토큰 기반으로 고친 채 유지. 되돌리는 과정에서 정렬 클릭 시 `renderSorts()` 호출을 실수로 지워 선택 표시가 안 바뀌는 버그를 냈다가 바로 수정 (기획 §4.27) |
| 55 | 모바일 점검 · 헤더 줄바꿈 버그 수정 | "모바일도 고려됐나" 질문에 실제로 390px 폭으로 렌더해서 확인. `.hrow` 가 줄바꿈을 안 해서 `라이트 모드` 버튼 글자가 단어 중간에서 잘려 두 줄이 되는 버그 발견·수정 — `.hrow{flex-wrap:wrap}` + 버튼에 `white-space:nowrap`. 데스크톱 레이아웃은 그대로임을 스크린샷으로 확인. 테마 카드 × 버튼 자리, 타임라인 iOS 스크롤 힌트 부재, 복사/저장 버튼 24px 터치 영역은 기능은 멀쩡해 이번엔 보류 (기획 §4.28) |
| 56 | 순차 배정 (F-14) | 타지역 원정 때 여러 팀이 같은 매장을 나눠 도는 경우용. `식사 시간 확보` 와 같은 접힘 패턴(체크박스를 켜야 패널·`팀 확정` 버튼이 나타남)으로 평소 화면은 그대로 둔 채 넣었다. 결과 카드에서 팀을 확정하면 그 회차(테마 id + 시각)가 `search()` 의 새 `taken` 옵션으로 다음 팀 계산에서 빠지고, `runSearch()` 를 그대로 재호출해 `showCount`/`tabCount` 리셋이 #54 때처럼 빠지지 않게 했다. `마지막 팀 취소`/`전체 초기화`, 자동저장(§4.26)·공유 링크에도 팀 상태가 실린다(버전은 그대로 3, `restore()` 의 미지 버전 throw를 다시 건드리지 않으려고). 헤드리스 브라우저로 검증하다가 `테스트 데이터 넣기` 로 테마를 통째로 갈아치우면 옛 id를 가리키던 팀 배정이 조용히 어긋나는(팀2가 팀1과 똑같은 회차로 나오는) 버그를 실제로 재현·수정했다(`teams=[]` 를 같이 리셋). `test/acceptance.mjs` 에 슬롯 단위 제외를 검증하는 테스트 6건 추가. 결과가 0개일 때의 안내문("최소 공백을 줄이거나…")이 팀 모드에서는 원인이 조건이 아니라 앞선 팀의 회차 소진인 경우가 있어 오히려 헷갈린다는 점, 그리고 3팀까지 실제로 겹치지 않는지는 따로 확인하지 않았다는 점을 놓쳤다가 재검토 후 잡았다 — 팀 모드 전용 빈 결과 안내문 추가, 3팀 CDP 시나리오로 쌍마다 겹치지 않음을 재검증. `팀 확정` 을 누르면 카드가 다음 팀 후보로 바뀌어 그 조합의 복사 버튼이 사라진다는 점도 `teamhint` 문구에 "확정 전에 먼저 복사" 로 명시 (기획 §4.29) |
| 57 | 화면 다듬기 5건 | (1) 테마 카드 드래그 시 살짝 커지고 그림자가 뜨도록(`scale+rotate+shadow`, 트랜지션 추가) — 흐려지기만 하던 것보다 "지금 옮기는 중" 이 분명해짐. (2) `테스트 데이터 넣기` 버튼·`seed()`·관련 투어 스텝 삭제 — F-15(회차 불러오기)가 붙은 뒤로 가짜 데이터로 감을 잡을 이유가 없어짐, 투어 첫 스텝은 `#loadBtn` 으로 이동 (기획 §4.22 후기). (3) `회차 불러오기` 모달의 "지원 매장: …" 요약 줄 삭제 — 지점 드롭다운 옵션에 이미 `매장명 + 지점명`이 같이 나와(같은 이름의 지점이 여러 매장에 있을 때를 위한 표기) 중복 정보였다. 이 줄을 채우던 `$('loadStore')` 할당도 같이 제거해 요소가 사라진 뒤 던질 뻔한 에러를 막았다. (4) `.mrow input,.mrow textarea` 규칙에 `select` 가 빠져 있어 `지점`/`날짜` 드롭다운만 다른 입력칸보다 낮았던 것을 발견·수정 — 선택자에 `select` 추가. (5) 테마 섹션 안내문을 "붙여넣거나 직접 입력 · 매진된 회차는 빼고 넣어 주세요" → "붙여넣거나 직접 입력 시 매진된 회차는 빼고 입력해주세요" 로 한 문장으로 다듬음 |
| 58 | 순서 탐색 폐지 — 카드 순서 = 계산 순서 (기획 §4.14 후기, 전면 변경) | "자리고정보다는 테마 배치 순서대로 조합 계산을 도출해줘" 요청. §4.14 가 명시했던 "카드 순서는 결과에 아무 영향이 없다" 는 전제를 뒤집는 요청이라 실행 전에 결과를 짚어(자리 고정 개념 자체가 없어진다는 점 포함) 재확인받고 진행했다. `search()` 의 DFS를 `used[]`+`lockPos` 조건 분기에서 `dfs(curEnd,start)` 가 `start`부터 오름차순으로만 훑는 구조로 바꿨다 — 코드 자체는 3줄 수준의 축소. "자리 고정" 버튼·`lockbtn`·`th.lock`·`.card.locked` 를 전부 걷어냈고, `blank()`에서 `lock` 필드도 뺐다. `serialize()`/`restore()`의 4번째 튜플 자리는 `so` 슬롯과 같은 방식으로 값만 버리고 위치는 남겼다(옛 링크·자동저장의 `place` 자리가 밀리는 걸 막기 위해). 부분 조합(F-08)은 순서를 지킨 채 건너뛰기만 허용하는 것으로 의미가 바뀌었다. `npm test` 를 돌려 예상대로 깨진 곳을 확인했다 — requirements.md §7 의 수기 최적해(범계→몽→왓 어 트립)는 카드 기본 순서(범계,왓어트립,몽)에서 이제 도달 불가능해 새 순서 안에서 나오는 최적해로 교체했고, "자리 잠금" 전용 테스트 블록은 기능 자체가 없어졌으므로 적응이 아니라 삭제했다(대신 "결과가 항상 오름차순 인덱스로만 나온다"는 새 불변식 테스트로 대체). §2.2("순서까지 고려해 `N!×M^N`")·G-1(§2.3 최적해가 결과 1위)·§8 탐색 실측 표·§11.2 탐색 상한 서술까지 전부 지금 동작 기준으로 다시 썼다 — 탐색 공간이 순열 없이 `M^N` 만 남아 실측상 자릿수 단위로 줄었다(테마 5개: 10,664개→91개). 결과 0개 안내문에 "테마 순서를 바꿔보세요" 를 추가했다. |
| 59 | React+TS 이전 1단계 — 스캐폴드 + 핵심 로직/데이터 계층 (기획 §4.31) | Revolt 스타일 리디자인 요청 + 스택 전환 허용을 받고, advisor 자문으로 "디자인 교체"와 "스택 전환"을 분리(스택 먼저·시각 변화 없이)하는 계획을 세워 승인받았다. 스파이크 2건: (1) PaddleOCR — `app/`에 Vite+React+TS 스캐폴드를 만들어 `onnxruntime-web`/`ppu-paddle-ocr`/`ppu-ocv`를 실제 npm 패키지로 설치해 돌려봄. vendor/에 있던 onnxruntime-web 파일 3개로는 최신 버전이 안 뜨는 것, `npm run build`가 wasm 23MB를 두 벌(public/ 패스스루 + Vite 자동 번들) 만드는 것을 실측으로 발견해 `optimizeDeps.exclude`+`rollupOptions.external`+전용 importmap 조합으로 해결. dev·build 양쪽에서 프로덕션과 결과가 글자 하나까지 동일함을 확인. (2) "로그인 전 0바이트"(§4.18) — 더미 지연 로드 모듈로 네트워크 요청을 직접 찍어 Vite 코드 스플리팅에서도 유지됨을 확인. 데이터 호환: `test/fixtures/legacy-links.json`에 v1/v2/v3early/v3current 4개 실제 링크 해시를 만들거나 헤드리스 브라우저로 왕복 검증해 고정. 이 과정에서 실제 버그 발견·수정: 테마 `id`가 공유 링크/저장된 계획에 안 실려 있어서, 페이지를 만진 뒤(id 카운터가 진행된 뒤) 팀이 확정된 저장 일정을 "불러오기"하면 새로 매겨진 id가 저장된 `teams[].steps[].id`와 어긋나 F-14 제외가 조용히 안 먹는 버그를 실측으로 재현·수정(`index.html`도 같이 고침 — `t` 튜플에 id를 6번째 자리로 추가, `nextId`도 같이 당겨 올림). `@core`(709~889행)를 `app/src/core.ts`로 로직 변경 없이 타입만 붙여 이식, `serialize()`/`restore()`는 DOM 의존을 없앤 순수 함수(`app/src/serialize.ts`)로 다시 짜되 JSON 모양은 완전히 유지. `test/acceptance.mjs`의 검사 전부를 Vitest(`app/src/core.test.ts`)로 옮겨 통과(82개), legacy-links 왕복+id 버그 회귀 테스트(`serialize.test.ts`) 추가. `index.html`·원본 테스트는 손대지 않고(id 버그 수정 제외) 계속 그대로 서빙·통과한다. |
| 60 | React+TS 이전 2단계 — 핵심 계산기 화면 (기획 §4.32) | 1단계 원칙(디자인 교체 없이 스택만) 그대로 이어서, index.html의 CSS(31~543행)를 `app/src/floduler.css`로 그대로 옮기고 같은 클래스명으로 JSX를 짜 시각적으로 100% 동일하게 화면을 만들었다. `useScheduler()` 훅 하나에 핵심 상태를 몰아넣고(`ThemeCard`/`ThemeList`/`ConditionsPanel`/`MoveTimeGrid`/`ResultsPanel`/`ResultCard`/`TeamPanel`이 소비), 새 상태 라이브러리는 추가하지 않았다. 실제로 걸린 것 셋: (1) "팀 확정" 직후 곧바로 다음 팀 후보를 계산하는 부분에서 클로저 문제 — `setTeams` 직후 같은 함수에서 `runSearch()`를 부르면 옛 `teams`를 본다. `runSearch(teamsOverride?)`로 바꿔 방금 계산한 새 배열을 직접 넘기는 방식으로 해결(처음엔 `teams`를 감시하는 effect로 시도했다가, `restore()` 직후에도 팀이 바뀐 걸로 오인해 원본에 없던 자동 재계산이 도는 문제를 발견하고 되돌림). (2) 결과 카드 타임라인의 사후 DOM 보정(이름 넘침 시 폭 늘리기, 안 되면 시각 자르기)을 `useLayoutEffect`로 옮기다가, React가 "이전에 자신이 렌더한 값과 같으면" 텍스트 노드를 안 건드리는 특성과 직접 `textContent`를 자르는 이 로직이 어긋나는 걸 발견 — `data-full` 속성에 원본 텍스트를 항상 심어 두고 매 패스 거기서 다시 시작하도록 고쳤다. (3) 검증 스크립트에서 체크박스를 `.checked` 세팅 + `change` dispatch로 흉내 냈더니 "팀 확정" 버튼이 안 나타나 앱 버그인 줄 알았으나, React가 체크박스는 `click` 이벤트로 추적한다는 걸 확인하고 네이티브 `.click()`으로 바꾸자 정상 동작 — 검증 방법 문제였음을 확인. 패리티 검증: 같은 3테마 입력을 index.html·새 앱 양쪽에 헤드리스로 넣어 결과 개수·1위 조합 시퀀스·더보기 페이지네이션이 글자 하나 안 다르게 일치. 정렬 5종·팀 확정/취소·드래그 재정렬(합성 DragEvent)·공유링크 왕복·자동저장 후 새로고침 복원까지 콘솔 예외·경고 0건, 모바일 390px 가로 스크롤 없음, 다크모드 자동 전환 확인. 이미지 인식·Firebase 계정·F-15 서버 불러오기·사용법 투어·다크모드 토글 버튼은 이번에도 비활성화 상태로만 자리를 남기고 범위 밖으로 미뤘다. |
| 61 | CI/CD 파이프라인 (기획 §4.33) | `.github/workflows/deploy.yml` 신설 — `main` 푸시마다 루트 인수 테스트 + `app/` Vitest(82건)를 먼저 돌리고 통과해야 `app/`을 빌드해서 GitHub Pages(`actions/deploy-pages`)에 올리는 구조. 테스트 실패 시 배포 잡 자체가 안 돈다. `app/public/.nojekyll` 추가(방어적, Actions 배포 경로는 원래 Jekyll 안 탐), 스파이크용으로 남아 있던 `app/public/test-fixture.png`는 프로덕션 빌드에 실을 이유가 없어 삭제. **Pages Source 전환(브랜치 배포 → Actions 배포)은 아직 안 함** — 지금 `app/`엔 §4.31~4.32에서 범위 밖으로 미룬 이미지 인식·로그인·F-15·투어가 빠져 있어서, 지금 전환하면 실서비스가 그 기능들을 잃는다. 워크플로 자체는 로컬 빌드로 재검증까지 끝냈고, 전환 시점은 사용자 승인 필요 (§4.33). |
| 62 | React 이전 3단계 — 다크모드 토글 버튼 (기획 §4.34) | §4.33에서 범위 밖으로 미뤘던 다섯 개 중 첫 번째. index.html의 `applyTheme`/`isDarkNow` 로직을 `app/src/useTheme.ts` 훅으로 그대로 이식 — 저장된 선택 없으면 시스템 설정 따름([data-theme] 속성 자체를 안 둠), 버튼 클릭 시 `localStorage`(`resched.theme`)에 남겨 시스템 설정보다 우선. CSS 토큰·FOUC 가드는 2단계(#60) 때 이미 옮겨져 있어 새로 손댈 게 없었다. `SchedulerPage.tsx`의 비활성화된 자리표시자 버튼을 실제 훅에 연결. 헤드리스 브라우저로 초기 상태(시스템 설정 반영)→클릭→속성·버튼 텍스트·`themeColorMeta` 변경→새로고침 후 유지→재클릭 왕복 전부 확인. |

---

## 11. 한계와 미해결

### 11.1 원본 문서 인코딩 손실

전달받은 `escape-schedule.html` / `requirements.md` 의 한글이 UTF-8을 cp1252로 읽은 형태로 깨져 있었고, 이 과정에서 바이트 `0x80~0x9F` 가 **소실**되어 자동 복호가 불가능했다. 남은 바이트로 후보를 역산하고 문맥으로 좁혀 전량 재작성했으며, 같은 손실 변환을 다시 적용해 원본과 대조하는 방식으로 검증했다.

UI 문구와 주석은 코드 문맥이 뜻을 고정하므로 사실상 복구가 확정적이다. **문제는 고유명사인 테마명이었고, 세 개 중 둘을 틀렸다.**

| 이름 | 역산이 내놓은 값 | 실제 | 결과 |
|---|---|---|---|
| `몽` | 몽 | 몽 | 맞음 — 관측치를 만드는 문자열이 이것 하나뿐이었다 |
| `범계` | 벙고 | **범계** | **틀림** — `번고`·`범고`·`법고`·`벌고` 등 다수가 같은 형태로 무너져 후보가 안 좁혀졌다 |
| `왓 어 트립` | 에이트립 | **왓 어 트립** | **틀림** — `왓어트립` 도 관측치와 **완전히 동일하게** 깨진다 |

**교훈**: 손실 변환의 역산은 "내 후보가 관측치를 만든다" 까지만 보증하고 "다른 후보는 못 만든다" 는 보증하지 않는다. `왓 어 트립` 은 특히 뼈아픈 사례다 — 후보를 대조해 **정확히 일치**하는 것을 찾았고 그래서 "확정" 으로 적었는데, 실제로는 같은 형태로 깨지는 다른 이름이 있었다. **일치는 유일성이 아니다.**

고유명사처럼 문맥이 뜻을 고정해 주지 못하는 자리에서는 반드시 사람에게 확인받아야 한다. 두 이름 모두 사용자 확인으로 바로잡았다.

### 11.2 그 외

| 항목 | 내용 |
|---|---|
| 캡처 잘림 | 회차가 많은 화면을 다 못 담으면 OCR로 복구 불가. 사용자가 직접 보완해야 함 |
| 소요시간 | 예약 화면에 없으므로 수동 입력 고정 |
| 테마 식별 | 캡처에 테마명이 없는 경우가 많아 어느 카드에 넣을지 사용자가 지정해야 함 |
| 이동시간 | 사용자가 적는다. 지도로 실제 도보 시간을 가져오는 것은 미지원 (기획 §4.7) |
| 탐색 상한 | §4.14 후기(카드 순서 고정)로 탐색 공간이 `N!×M^N`→`M^N` 로 줄어, 실무 범위(테마 7개·회차 10개까지 실측)에서는 더 이상 걸리지 않는다. 회차를 아주 많이 넣는 극단적인 입력을 위해 상한 자체는 남겨 뒀고, 도달 시 화면에 표시됨 |
| 인식 정확도 수치 | 실제 캡처에서는 동작만 확인. 회차 단위 정확도는 복제 화면 기준 (§9.5) |
| 인식 속도 | 첫 장은 엔진 5MB 다운로드 포함. 이후는 캐시. 큰 이미지일수록 느려 화소 상한을 검 (PC 320만 / 모바일 160만) |
| 모바일 인식 | 실기기에서 1080×2340 을 **4초**에 끝냈다 — 느린 게 문제가 아니었다. 실패 원인은 "글자를 못 찾음" |
| 전처리 다중 시도 | 한 임계값으로 모든 예약 화면을 못 덮는다. 4가지를 돌려 가장 많이 읽은 것을 쓴다. `bright`(밝은 글자)는 **아직 실증 못 함** |
| HEIC | 아이폰이 HEIC 를 그대로 올리면 브라우저가 캔버스로 못 그린다. 막지는 못하고 원인을 알려 준다 |
| 저대비 화면 | 임계값 235 고정. 다른 예약 사이트의 색이 크게 다르면 조정이 필요할 수 있음 |
| 매진 인식 | 숫자만 읽으므로 매진 여부는 이미지에서 못 가져옴 (사용자 요구로 제외) |
| 재방문 저장 | 로그인하면 일정이 남지만, **입력 중인 화면**은 여전히 새로고침하면 사라진다 (§4.16) |
| 비밀번호 복구 | 없음. 메일 주소를 안 받으므로 잊으면 계정을 새로 만들어야 한다 (기획 §4.18) |
| 가입 자체는 못 막는다 | `apiKey` 가 공개라 REST 로 아무 이메일이나 계정을 만들 수 있다. 저장은 도메인 제한이 막지만(§8.1), 근본 대응은 App Check |
| 오프라인 | 계정 기능만 인터넷·CDN 을 탄다. 조합 계산과 이미지 인식은 여전히 오프라인 동작 |
| 배포 | 아직 안 올렸다. 준비는 끝났고 Pages 를 켜는 것만 남았다 (§2.2) |
