# 방탈출 스케줄러 — 요구사항 및 사양 정리

작성일: 2026-08-20
산출물: `index.html` (단일 파일 웹앱, 구 `escape-schedule.html`)

> **보관 문서.** 최초 요구사항 원본이다. 전달 과정에서 깨진 한글을 복원해 두었다
> (경위는 [작업명세서.md](작업명세서.md) §11.1).
> 현재 기준 문서는 [기획.md](기획.md) (배경·목표·설계 근거) 와
> [작업명세서.md](작업명세서.md) (구현·검증) 이다.

---

## 1. 배경

같은 매장에서 방탈출 테마 3개를 하루에 연달아 예약하려 할 때, 각 테마의 회차 시간이 서로 다른 주기로 돌아가 손으로 조합을 짜기 어렵다.

실제 사례 (2026-08-22, 동일 매장):

| 테마 | 소요시간 | 회차 주기 | 회차 시간 |
|---|---|---|---|
| 범계 | 75분 | 90분 | 10:30, 12:00, 13:30, 15:00, 16:30, 18:00, 19:30, 21:00, 22:30 |
| 왓 어 트립 | 70분 | 85분 | 10:20, 11:45, 13:10, 14:35, 16:00, 17:25, 18:50, 20:15, 21:40, 23:05 |
| 몽 | 70분 | 80분 | 10:50, 12:10, 13:30, 14:50, 16:10, 17:30, 18:50, 20:10, 21:30, 22:50 |

> 테마명은 원본 문서의 깨진 인코딩에서 복원한 값이다. 세 개 모두 확정 — 경위는 [작업명세서.md](작업명세서.md) §11.1.

주기가 90 / 85 / 80분으로 모두 달라 최소공배수가 맞지 않는다. 시작 시간 협상이 불가능하고 **정해진 회차에만 들어갈 수 있으므로**, 테마 소요시간이 끝나면 다음 회차까지 대기 시간이 발생한다.

수기로 조합을 하나씩 짚어본 결과, 공백 총합이 **5분부터 165분까지** 벌어졌다. 순서만 바꿔도 종료 시간이 2시간 이상 차이 났다.

### 검증된 최적해 (수기)

```
13:30  범계 (75분)   → 14:45 종료   [공백 5분]
14:50  몽   (70분)   → 16:00 종료   [공백 0분]
16:00  왓 어 트립(70분) → 17:10 종료
```
총 공백 5분, 3시간 40분 소요. 프로그램 검증용 기준값으로 사용.

---

## 2. 요구사항

### 2.1 기능 요구사항

| ID | 항목 | 내용 | 우선순위 |
|---|---|---|---|
| F-01 | 테마 입력 | 이름 / 소요시간(분) / 회차 시간 목록 | 필수 |
| F-02 | 테마 개수 | 제한 없음 (동적 추가·삭제) | 필수 |
| F-03 | 조합 탐색 | 겹치지 않는 모든 순서·회차 조합 계산 | 필수 |
| F-04 | 결과 정렬 | 다중 기준 선택 | 필수 |
| F-05 | 이미지 입력 | 예약 화면 캡처에서 회차 시간 자동 추출 | 필수 |
| F-06 | 텍스트 입력 | 회차 시간 직접 입력 / 수정 | 필수 |
| F-07 | 조건 필터 | 시작 하한, 종료 상한, 최소·최대 공백, 매진 제외 | 필수 |
| F-08 | 부분 조합 | 전체 테마가 안 들어갈 때 일부만 넣은 조합도 표시 | 선택 |

### 2.2 입력 사양

**회차 시간 파싱** — 아래 형태를 모두 처리한다.

- `오전 10:30 매진 오후 12:00 매진 …` (예약 화면 복사 형태)
- `10:30, 12:00, 13:30, 15:00` (오전/오후 표기 없음)
- 오전/오후 표기가 전혀 없으면 **오름차순으로 가정하고 12시간 보정**한다.
  예: `10:30, 12:00, 1:30, 3:00` → `10:30, 12:00, 13:30, 15:00`
- `매진` / `마감` 은 해당 시각에 매진 플래그를 붙인다. `1매`, `2매` 는 예약 가능으로 본다.

**이미지 OCR 추출 항목**
- 회차 시간 (오전/오후 → 24시간제 변환)
- 매진 여부
- 테마명 (화면에 있을 경우)
- 달력 영역의 날짜 숫자는 회차로 오인하지 않을 것

### 2.3 출력 사양

각 조합에 대해 다음을 산출한다.

- 시작 / 종료 시각, 총 소요 시간
- 공백 총합, 공백별 개별 값, 최소 공백
- 공백 표준편차 (균등도)
- 포함된 테마 수

**정렬 기준 5종**

| 기준 | 용도 |
|---|---|
| 공백 적은 순 | 시간 효율 최우선 |
| 빨리 끝나는 순 | 종료 시간 최우선 |
| 공백 고른 순 | 리듬 균등 (표준편차 최소) |
| 늦게 시작 순 | 오전 일정이 있을 때 |
| 여유 있는 순 | 최소 공백이 가장 큰 조합 |

단일 최적해가 아니라 **상위 12개를 노출**한다. 실사용 판단에서 "효율 1등"과 "체감 1등"이 자주 달라지기 때문이다.

### 2.4 비기능 요구사항

- 단일 HTML 파일, 빌드 도구 없음
- 외부 라이브러리 없음 (웹폰트 제외)
- 모바일 반응형
- `prefers-reduced-motion` 대응
- 3~5개 테마 기준 계산 100ms 이내

---

## 3. 설계 결정과 근거

> 상세 근거는 [기획.md](기획.md) §4 로 옮겼다. 아래는 요약이다.

### 3.1 결과를 표가 아닌 타임라인으로

**결정**: 각 조합을 시간축 위 블록 + 공백으로 렌더링.

**근거**: "공백 50분"이라는 숫자만으로는 그 50분이 앞에 몰렸는지 뒤에 몰렸는지 알 수 없다. 실제 검토 과정에서 공백 총합이 같은 두 안(A: 0분+50분 / B: 45분+5분)의 선택이 갈렸는데, 판단 근거는 **배치**였지 총합이 아니었다.

### 3.2 최소 공백을 별도 지표로 노출

**결정**: 최소 공백을 헤더에 표시하고, 10분 미만이면 경고색. 정렬 기준에도 포함.

**근거**: 방탈출은 **탈출 실패 시 제한시간을 꽉 채우고 나온다.** 성공 전제로 짠 공백 0~5분 스케줄은 한 번의 실패로 무너진다.

### 3.3 탐색 알고리즘 — DFS + 가지치기

**결정**: 순열 전수 대신 점진적 DFS. 각 단계에서 `직전 종료 + 최소공백` 이후 회차만 후보로 두고 진행.

**근거**: 테마 N개, 회차 M개일 때 순진한 전수는 `N! × M^N`. 5개 × 10회차면 1200만 조합으로 브라우저가 멈춘다. 시간 겹침 제약이 강해 가지치기 효율이 높다. 노드 80만 / 결과 6000개 상한을 두고, 도달 시 사용자에게 표시한다. *(현재 구현은 결과 상한을 50,000으로 올렸다 — 6,000은 테마 5개에서 이미 걸렸다. 작업명세서 §9.2 참조)*

### 3.4 이미지 OCR이 주 입력, 텍스트를 보조로

**결정**: 두 입력을 모두 지원하되, OCR 결과를 편집 가능한 텍스트 칸에 풀어서 표시.

**근거**: 텍스트만 지원하면 회차 시간을 전부 타이핑해야 해서 손으로 짜는 것과 시간이 비슷해진다 → 도구의 존재 이유가 사라진다. 반대로 OCR만 지원하면 캡처가 잘리거나 오독했을 때 복구할 수단이 없다.

### 3.5 링크 파싱 — 보류

**결정**: 예약 페이지 URL 입력 방식은 미구현.

**근거**: 실제 확인 결과 (키오스케이프 예약 페이지)
- 정적 HTML에는 **회차 시간이 존재하지 않음**. 날짜 선택 시 별도 AJAX 요청으로 로드되는 구조.
- URL 파라미터(`theme_num` 등)가 서버 렌더링에 반영되지 않고 JS가 처리.
- 가져온 것: 테마 설명, 장르, 난이도, **플레이시간**, 가격
- 못 가져온 것: 회차 시간, 매진 여부

또한 순수 프론트엔드에서는 CORS로 외부 도메인 직접 요청이 차단되므로, 링크 방식은 **프록시 서버가 전제**다.

### 3.6 장르·난이도 가중치 — 제외

**결정**: 시간 계산만 수행. 테마 성격은 반영하지 않음.

**근거**: 사용자 요구. 다만 검토 과정에서 **테마 성격이 결론을 뒤집은 사례**가 있었음을 기록해 둔다 — 공백 5분짜리 최적안이 "왓 어 트립은 문제가 많은 방"이라는 정보 하나로 무너졌다. 문제 방은 2시간 뒤 연속 플레이 하한을 두고 앞에 배치하는 구조라야 했기 때문. 향후 확장 시 최우선 후보.

---

## 4. 구현 내역

### 4.1 모듈 구성

| 영역 | 함수 | 역할 |
|---|---|---|
| 시간 유틸 | `parseClock`, `fmt`, `pad` | `"13:30"` ↔ 분 단위 정수 변환 |
| 파싱 | `parseSessions` | 텍스트 → 회차 배열, 오전/오후 및 12시간 보정 |
| OCR | `handlePaste`, `readImage`, `ocr` | 붙여넣기·드롭 → base64 → Vision API → JSON |
| 탐색 | `search` | DFS 조합 생성, 통계 산출 |
| 렌더 | `renderThemes`, `renderResults`, `axisTicks` | 입력 카드 / 결과 타임라인 / 시간축 |

### 4.2 입력 경로

1. 회차 칸에 **Ctrl+V** → 클립보드 이미지 감지 시 OCR, 텍스트면 그대로 파싱
2. 카드에 **이미지 드래그 앤 드롭**
3. 회차 시간 **직접 타이핑**

### 4.3 결과 데이터 구조

```
{
  steps: [{ name, dur, start, end, soldout }],
  count, start, end,
  total,        // 공백 총합
  gaps: [],     // 개별 공백
  varc,         // 공백 표준편차
  minGap        // 최소 공백
}
```

---

## 5. 한계와 미해결 사항

| 항목 | 내용 |
|---|---|
| OCR 인증 | 브라우저에서 API를 직접 부를 수 없음 → 프록시 서버로 해결 (작업명세서 §6.3) |
| 캡처 잘림 | 회차가 많은 화면을 다 못 담으면 OCR로 복구 불가. 사용자가 직접 보완해야 함 |
| 소요시간 | 예약 화면에 없으므로 수동 입력 고정 |
| 테마 식별 | 캡처에 테마명이 없는 경우가 많아 어느 카드에 넣을지 사용자가 지정해야 함 |
| 이동시간 | 동일 매장 전제. 매장이 다를 경우의 이동시간 행렬 미지원 |
| 탐색 상한 | 테마 6개 이상 + 회차 다수 조합에서 한도 도달 가능 |

---

## 6. 확장 후보

우선순위 순.

1. **테마 성격 가중치** — 문제 방은 앞쪽, 쉬운 테마는 뒤쪽 배치에 가점. 3.6 참조
2. **배포용 프록시** — API 키 은닉 + 링크 파싱 동시 해결 (Vercel Functions 등)
3. **매장 간 이동시간** — 매장이 다른 경우의 이동시간 행렬
4. **결과 저장·공유** — URL 쿼리스트링에 상태 직렬화

---

## 7. 검증

입력값 (범계 75분 / 몽 70분 / 왓 어 트립 70분, 최소 공백 0, 매진 제외 해제) 으로 실행 시,
**13:30 범계 → 14:50 몽 → 16:00 왓 어 트립 (공백 5분, 17:10 종료)** 조합이 "공백 적은 순" 1위로 나와야 한다. 수기 결과와 일치 여부로 검증한다.

→ `npm test` 로 자동화. 결과는 [작업명세서.md](작업명세서.md) §9.
