# 배포 아키텍처

프론트 배포 구조가 헷갈린다는 질문에서 시작된 문서. 결론부터 —

**지금 `https://choieuihyun.github.io/RoomEscapeScheduler/` 에서 뜨는 건
저장소 루트의 `index.html`이 아니라 `app/`(React+TypeScript)이다.**
루트 `index.html`은 저장소에 그대로 남아 있지만 더 이상 서빙되지 않는다.

## 1. 저장소 안에 앱이 두 개 있다

```
RoomEscapeScheduler/
├── index.html          ← 원본 vanilla JS 앱. 그대로 남아 있지만 이제 안 뜬다.
├── serve.mjs            (index.html용 로컬 개발 서버 — 배포 안 됨)
├── test/acceptance.mjs  (index.html의 @core 인수 테스트)
│
└── app/                ← 지금 실제로 서빙되는 것
    ├── src/              React+TS 소스
    ├── index.html        Vite 엔트리 (빌드되면 dist/index.html이 됨)
    ├── vite.config.ts     base 등 빌드 설정
    └── dist/             `npm run build` 산출물 (git에 안 올라감, gitignore)
```

React 이전 작업(기획 §4.31~4.35)이 `app/`을 index.html과 기능·최종적으론
시각까지 다르게 완성시켰고, 그 사이 어느 시점에 GitHub Pages 설정이
`app/`을 배포하는 쪽으로 넘어갔다(§3 참고 — 언제·누가 눌렀는지는 저장소
안에 기록이 없어 알 수 없다). **루트 `index.html`을 다시 보려면 로컬에서
`node serve.mjs`로 직접 띄우는 수밖에 없다.**

## 2. 배포 파이프라인 — GitHub Actions

```
 개발자 PC                     GitHub                                      브라우저
┌──────────┐   git push   ┌───────────────────────────────────────┐   요청   ┌─────────┐
│  main에  │ ───────────▶ │  .github/workflows/deploy.yml 실행      │ ◀────── │  방문자  │
│  커밋     │              │                                         │         └─────────┘
└──────────┘              │  ① test  루트 인수 테스트 + app/ Vitest   │
                           │           (82건) — 하나라도 실패하면      │
                           │           ↓ 아래 두 단계가 아예 안 돈다   │
                           │  ② build  app/ 에서 npm run build       │
                           │           → app/dist/ 생성               │
                           │  ③ deploy  actions/deploy-pages 로       │
                           │           dist/ 를 Pages에 publish       │
                           └───────────────────────────────────────┘
```

`main`에 푸시할 때마다 자동으로 돈다(`.github/workflows/deploy.yml`).
**테스트 게이트가 있다** — `test` job이 실패하면 `build`/`deploy` job은
아예 시작도 안 한다(`needs: test`). 배포된 것은 항상 테스트를 통과한
코드다. 보통 푸시 후 1~2분 안에 라이브에 반영된다.

이 파이프라인은 `app/`만 빌드한다. 루트 `index.html`은 이 워크플로가
건드리지 않는다 — 그냥 저장소에 파일로 남아 있을 뿐이다.

## 3. GitHub Pages 설정 — "브랜치 배포"와 무엇이 다른가

GitHub Pages는 소스를 두 가지 중 하나로 설정할 수 있다:

| | 브랜치에서 배포 (예전 방식) | GitHub Actions (지금 상태) |
|---|---|---|
| 서빙되는 것 | 지정한 브랜치의 파일 **그대로** | Actions가 만든 **빌드 산출물** |
| 이 저장소라면 | 루트 `index.html`이 그대로 열림 | `app/dist/`(빌드 결과)가 열림 |
| 파이프라인 필요 여부 | 없음 — 그냥 파일 서버 | `deploy.yml` 같은 워크플로 필요 |

기획.md §4.33은 처음에 `deploy.yml`을 만들면서도 Pages 설정 자체는
"브랜치 배포"로 남겨 뒀다(전환은 사용자 승인 후로 미룸). 그런데 §4.35
작업 중 실제 라이브 URL을 열어 보니 이미 "Actions" 쪽으로 바뀐 상태였다
— `curl`로 받은 `index.html`이 2.2KB짜리(빌드된 `app/`의 셸)였고, 원본
`index.html`(수천 줄짜리 단일 파일)이 아니었다. **언제·누가 바꿨는지는
이 저장소 기록만으로는 알 수 없다.**

### 서브패스 문제 (실제로 겪은 버그)

이 저장소는 GitHub의 **프로젝트 페이지**로 서빙된다 — 즉 도메인 루트가
아니라 `/RoomEscapeScheduler/` 서브패스에서 뜬다. Vite는 기본적으로
자산 경로(`/assets/...`)를 도메인 루트 기준으로 만드는데, 그러면 실제
서빙 경로와 안 맞아 전부 404가 난다. `app/vite.config.ts`에
`base: '/RoomEscapeScheduler/'`(빌드할 때만 — 로컬 `npm run dev`는
영향 없음)를 넣어서 고쳤다(커밋 `16371e0`). **이걸 처음에 안 넣어서
실제로 흰 화면 장애가 났었다** — 이 문서가 나오게 된 계기.

## 4. 런타임에 붙는 외부 서비스 4개

`app/`이 브라우저에서 실행되는 동안 붙는 서비스들. **전부 선택적이다**
— 하나가 죽어도 나머지 기능은 그대로 동작한다.

```
                    ┌──────────────── 사용자 브라우저 ────────────────┐
                    │  app/ (React)  조합 탐색 · 렌더 · 상태           │
                    │  PaddleOCR      이미지 인식 (ONNX/WASM, 로컬 실행) │
                    │  cloud.ts       Firebase SDK (로그인 눌러야 로드) │
                    │  firebase-messaging-sw.js  서비스워커(백그라운드) │
                    └──┬───────────┬───────────────┬───────────┬─────┘
                       │①          │②              │③          │④
                       ▼           ▼               ▼           ▼
              ┌────────────┐ ┌──────────────┐ ┌───────────┐ ┌────────────┐
              │  Firebase  │ │ floduler.    │ │ jsdelivr  │ │  Firebase  │
              │  (구글)     │ │ duckdns.org  │ │ CDN       │ │  Cloud     │
              │  Auth·     │ │ (F-15/F-16,  │ │ Pretendard│ │  Messaging │
              │  Firestore │ │  별도 프로젝트│ │ 폰트      │ │ (F-16 웹   │
              │            │ │  가 운영하는 │ │ (다이나믹 │ │  푸시 발송)│
              │            │ │  크롤러+감시 │ │  서브셋)  │ │            │
              │            │ │  서버)       │ │           │ │            │
              └────────────┘ └──────────────┘ └───────────┘ └────────────┘
```

- **① Firebase Auth/Firestore** — 로그인·저장된 일정. `app/public/firebase-config.js`에
  접속 정보(비밀 아님, 커밋해도 되는 값 — 파일 상단 주석 참고). 브라우저가
  Firestore에 **직접** 붙는다, 중간 서버 없음 — 대신 `firestore.rules`가
  "누가 어떤 문서를 볼 수 있는가"를 결정한다. **규칙이 곧 백엔드**다.
  로그인 버튼을 누르기 전까지 이 SDK 자체가 안 내려온다(코드스플리팅,
  Network 로그로 0바이트 실측 확인 — 기획 §4.34).
- **② F-15/F-16 크롤러+감시 서버** — `floduler.duckdns.org`. 이 저장소가
  아니라 별도 프로젝트(로컬 `~/IdeaProjects/Server`)가 예약 사이트를
  주기적으로 긁어 회차·매진 여부를 `/api/branches`·`/api/schedule`로,
  F-16(빈자리 알림)의 감시 등록·기기 토큰 등록을 `/api/watches`·
  `/api/devices`로 받는다. **인증 없는 GET(F-15)과 인증 필요 엔드포인트
  (F-16)는 CORS 처리가 다르다** — F-16 엔드포인트는 서버가 `Origin` 헤더를
  직접 검사해 프로덕션 origin(`https://choieuihyun.github.io`) 아니면
  `403 Invalid CORS request`로 막는다(GET들은 이 검사가 없다). 로컬
  개발에서는 `vite.config.ts`의 `/api` 프록시가 `changeOrigin`(Host 교체)에
  더해 `headers:{Origin:...}`로 Origin 자체를 덮어써서 우회한다(F-16
  개발 중 실측으로 발견 — `app/src/server.ts`의 `base()`가
  `import.meta.env.DEV`일 때만 상대경로를 씀). 이 서버가 꺼져 있어도
  직접 입력·이미지 인식은 그대로 동작한다.
- **③ Pretendard CDN** — 폰트. `app/index.html`에서 jsdelivr을 통해
  받는다. 이것도 죽으면 시스템 폰트로 자연스럽게 대체된다(`font-family`
  폴백 체인).
- **④ Firebase Cloud Messaging** — F-16 웹 푸시. `app/public/firebase-messaging-sw.js`
  (서비스워커, `app/public/`에 있어야 스코프가 앱 전체를 덮는다 — §3
  서브패스 규칙과 같은 이유)가 백그라운드에서 푸시를 받아 OS 알림으로
  띄운다. 감시를 처음 걸 때만 권한을 요청하고(`cloud.ts`의
  `getMessagingToken()`), 발급된 기기 토큰을 ②의 `/api/devices`에 등록—
  실제 발송은 서버가 Firebase Admin SDK로 한다(이 브라우저 쪽 코드와는
  별개 경로). 서비스워커의 Firebase 설정값은 `window`가 없어
  `firebase-config.js`를 못 읽으므로 하드코딩(비밀 아님) — 값이 바뀌면
  두 파일 다 고쳐야 한다.

**PaddleOCR(이미지 인식)은 외부 서비스가 아니다** — 모델·WASM이
`app/public/vendor/`로 같이 배포돼 브라우저 안에서 전부 실행된다. 서버로
이미지가 나가지 않는다.

## 5. 로컬 개발과 실제 배포, 뭐가 다른가

| | `npm run dev` (로컬) | 실제 배포 |
|---|---|---|
| base path | `/` (루트) | `/RoomEscapeScheduler/` |
| F-15/F-16 서버 호출 | `/api` 프록시 경유, Origin도 덮어씀 (CORS 우회) | `floduler.duckdns.org` 직접 호출 |
| Firebase | 같은 `firebase-config.js` (dev/prod 구분 없음) | 동일 |
| 서비스워커(F-16) | `http://localhost:*/firebase-messaging-sw.js` | `https://.../RoomEscapeScheduler/firebase-messaging-sw.js` |
| 테스트 게이트 | 없음 — 그냥 뜬다 | `npm test`(82건) 통과해야 배포됨 |

## 6. 이 구조가 못 하는 일

서버 코드가 없으므로(①②는 남의 서비스, GitHub Pages는 정적 파일만
서빙) 서버에서만 되는 일은 못 한다.

| 못 하는 것 | 왜 |
|---|---|
| 비밀키가 필요한 외부 API 호출 | 브라우저에 키를 두면 공개된다 |
| 정해진 시각에 도는 작업 | 이 저장소 소유의, 항상 켜져 있는 서버가 없다 |
| 전체 사용자 집계·통계 | Firestore 규칙상 각자 자기 문서만 읽을 수 있다 |
| 가입 자체를 막기 | `apiKey`만으로 REST 가입이 가능하다 |

필요해지면 Firebase Cloud Functions를 붙이면 되고, 그 순간이 이 구조를
벗어나는 시점이다.

## 관련 문서

- 결정 배경: [기획.md](기획.md) §4.31~4.37(React 이전·리디자인·매진 포함
  계산·F-16), §4.33/§4.35(Pages Source 전환 관련)
- 작업 기록: [작업명세서.md](작업명세서.md) #59~#71, §4.5(F-16 감시·알림
  스펙), §6.5(예전 index.html 기준 서버 구조 — 이 문서로 대체)
- 파이프라인 정의: [`.github/workflows/deploy.yml`](.github/workflows/deploy.yml)
