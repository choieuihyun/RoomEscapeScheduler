export class WebPlatformProvider{pathSeparator="/";ort=ort;createCanvas(width,height){let canvas=getPlatform().createCanvas(width,height);let getContext=canvas.getContext.bind(canvas);getContext("2d",{willReadFrequently:true});return canvas}isCanvas(image){return!!image&&typeof image.getContext==="function"}async loadResource(source,defaultUrl){if(source instanceof ArrayBuffer){return source}let sourceToLoad=typeof source==="string"?source:defaultUrl;let response=await fetch(sourceToLoad);if(!response.ok){throw new Error(`Failed to fetch resource from ${sourceToLoad}`)}return response.arrayBuffer()}async saveDebugImage(_canvas,_filename,_outputDir){return Promise.resolve()}canvas={prepareCanvas:(image)=>CanvasProcessor.prepareCanvas(image),createProcessor:(canvas)=>new CanvasProcessor(canvas),getToolkit:()=>CanvasToolkit.getInstance()}}import*as ort from"onnxruntime-web";import{CanvasProcessor,CanvasToolkit,getPlatform}from"ppu-ocv/canvas-web";function defaultWasmPaths(){let version=ort.env.versions.web??ort.env.versions.common;return`https://cdn.jsdelivr.net/npm/onnxruntime-web@${version}/dist/`}export function isWebWorker(){return typeof globalThis.WorkerGlobalScope==="function"}export function applyDefaultWasmPaths(){let inBrowser=typeof window!=="undefined"||isWebWorker();if(!inBrowser||ort.env.wasm.wasmPaths)return;ort.env.wasm.wasmPaths=defaultWasmPaths()}applyDefaultWasmPaths();export async function isWebGpuAvailable(){if(typeof navigator==="undefined")return false;let nav=navigator;if(!nav.gpu||typeof nav.gpu.requestAdapter!=="function")return false;try{let adapter=await nav.gpu.requestAdapter();return adapter!==null&&adapter!==undefined}catch{return false}}export async function getDefaultWebExecutionProviders(){if(await isWebGpuAvailable()){return["webgpu","wasm"]}return["wasm"]}