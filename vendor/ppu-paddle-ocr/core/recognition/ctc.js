export let BLANK_INDEX=0;export let UNK_TOKEN="<unk>";export let MIN_CROP_WIDTH=8;let GAP_QUANTA_CROSS_CLASS=1.5;let GAP_QUANTA_SAME_CLASS=2.5;function charClass(char){if(/\p{L}/u.test(char))return 0;if(/\p{N}/u.test(char))return 1;return 2}export function injectGapSpaces(chars,positions){if(chars.length<4)return;let deltas=[];for(let i=1;i<positions.length;i++){deltas.push((positions[i]??0)-(positions[i-1]??0))}let sorted=[...deltas].sort((a,b)=>a-b);let median=sorted[Math.floor(sorted.length/2)]??0;if(median<=0)return;let quantum=sorted.find((d)=>d>0)??0;if(quantum<=0)return;for(let i=chars.length-1;i>=1;i--){let prev=positions[i-1]??0;let curr=positions[i]??0;let k=charClass(chars[i]??"")===charClass(chars[i-1]??"")?GAP_QUANTA_SAME_CLASS:GAP_QUANTA_CROSS_CLASS;if(curr-prev>median+k*quantum&&chars[i]!==" "&&chars[i-1]!==" "&&chars[i]!==chars[i-1]){chars.splice(i,0," ");positions.splice(i,0,(prev+curr)/2)}}}let FULLWIDTH_OFFSET=65248;let CJK_PATTERN=/[\u2E80-\u9FFF\uAC00-\uD7AF\uF900-\uFAFF]/;export function refineDecodedChars(chars,positions){for(let i=chars.length-1;i>=1;i--){if(chars[i]===" "&&chars[i-1]===" "){chars.splice(i,1);positions.splice(i,1)}}if(CJK_PATTERN.test(chars.join("")))return;for(let i=0;i<chars.length;i++){let code=chars[i]?.codePointAt(0)??0;if(code>=65281&&code<=65374){chars[i]=String.fromCodePoint(code-FULLWIDTH_OFFSET)}else if(code===12288){chars[i]=" "}}}export function ctcGreedyDecode(logits,sequenceLength,numClasses,charDict,spaceRecovery=false){let dictLen=charDict.length;let lastDictIndex=dictLen-1;let emitted=[];let lastCharIndex=-1;let confidenceSum=0;let confidenceCount=0;let positions=[];for(let t=0;t<sequenceLength;t++){let base=t*numClasses;let maxProb=logits[base];let maxIndex=0;for(let c=1;c<numClasses;c++){let prob=logits[base+c];if(prob>maxProb){maxProb=prob;maxIndex=c}}if(maxIndex===BLANK_INDEX||maxIndex===lastCharIndex){lastCharIndex=maxIndex;continue}if(maxIndex>=0&&maxIndex<dictLen){if(spaceRecovery&&maxIndex!==lastDictIndex&&(logits[base+lastDictIndex]??0)>0.001&&emitted[emitted.length-1]!==" "){emitted.push(" ");positions.push((t+0.5)/sequenceLength)}let char=charDict[maxIndex]??"";if(maxIndex===lastDictIndex){if(char!==UNK_TOKEN){emitted.push(" ");confidenceSum+=maxProb;confidenceCount++;positions.push((t+0.5)/sequenceLength)}}else{emitted.push(char);confidenceSum+=maxProb;confidenceCount++;positions.push((t+0.5)/sequenceLength)}}lastCharIndex=maxIndex}injectGapSpaces(emitted,positions);refineDecodedChars(emitted,positions);let confidence=confidenceCount>0?confidenceSum/confidenceCount:0;return{text:emitted.join(""),confidence,positions}}export function decodeResults(outputTensor,charactersDictionary,numClassesFromShape,verbose=false,spaceRecovery=false){let outputData=outputTensor.data;let outputShape=outputTensor.dims;let sequenceLength=outputShape[1];let numClasses=outputShape[2]??numClassesFromShape;if(!charactersDictionary){return{text:"",confidence:0,positions:[]}}let dict=charactersDictionary;if(charactersDictionary.length===numClasses-1){dict=["",...charactersDictionary]}else if(numClasses!==charactersDictionary.length&&verbose){console.warn(`Warning: Model output classes (${numClasses}) does not match dictionary length (${charactersDictionary.length}).
 Consider using our model & dictionary catalogue at https://github.com/PT-Perkasa-Pilar-Utama/ppu-paddle-ocr-models.`)}return ctcGreedyDecode(outputData,sequenceLength,numClasses,dict,spaceRecovery)}export function decodeLogitsRow(rowData,sequenceLength,numClasses,charactersDictionary,spaceRecovery=false){let dict=charactersDictionary;if(charactersDictionary.length===numClasses-1){dict=["",...charactersDictionary]}return ctcGreedyDecode(rowData,sequenceLength,numClasses,dict,spaceRecovery)}