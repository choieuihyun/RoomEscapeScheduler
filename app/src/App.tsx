import './ocr-spike';

// 아직 실제 화면이 없다 — 지금은 핵심 로직(core.ts)과 상태 계층(serialize.ts)
// 이식까지만 끝난 단계다. 컴포넌트는 다음 단계에서 채운다 (README.md 참고).
function App() {
  return (
    <p style={{ fontFamily: 'monospace', padding: 24 }}>
      Floduler — React+TS 이전 진행 중. 아직 화면 없음(app/README.md 참고).
    </p>
  );
}

export default App;
