# 방탈출 스케줄러 — 기획

작성일: 2026-08-20
문서 역할: **왜 만드는가 / 무엇을 만드는가.** 어떻게 만들었는지는 [작업명세서.md](작업명세서.md).

---

## 1. 한 줄 요약

방탈출 테마 여러 개를 하루에 연달아 예약("연타")할 때, **정해진 회차 시간표만으로 가능한 모든 조합을 계산해 대기 시간이 어디에 얼마나 생기는지 보여주는** 단일 페이지 웹 도구.

---

## 2. 배경

### 2.1 문제

같은 매장에서 방탈출 테마 3개를 하루에 연달아 예약하려 할 때, 각 테마의 회차가 서로 다른 주기로 돌아가 손으로 조합을 짜기 어렵다.

실제 사례 (2026-08-22, 동일 매장):

| 테마 | 소요시간 | 회차 주기 | 회차 시간 |
|---|---|---|---|
| 범계 | 75분 | 90분 | 10:30, 12:00, 13:30, 15:00, 16:30, 18:00, 19:30, 21:00, 22:30 |
| 에이트립 | 70분 | 85분 | 10:20, 11:45, 13:10, 14:35, 16:00, 17:25, 18:50, 20:15, 21:40, 23:05 |
| 몽 | 70분 | 80분 | 10:50, 12:10, 13:30, 14:50, 16:10, 17:30, 18:50, 20:10, 21:30, 22:50 |

> 테마명은 원본 문서의 깨진 인코딩에서 복원한 값이다. 세 개 모두 확정 — 경위는 [작업명세서.md](작업명세서.md) §11.1.

주기가 90 / 85 / 80분으로 모두 달라 최소공배수가 맞지 않는다. 시작 시간 협상이 불가능하고 **정해진 회차에만 들어갈 수 있으므로**, 테마 소요시간이 끝나도 다음 회차까지 대기 시간이 발생한다.

### 2.2 왜 도구가 필요한가

손으로 조합을 하나씩 짚어본 결과, 공백 총합이 **5분부터 165분까지** 벌어졌다. 순서만 바꿔도 종료 시간이 2시간 이상 차이 난다. 즉 이 문제는 "대충 짜도 비슷한" 문제가 아니라 **탐색할 가치가 있는 조합 최적화 문제**다.

동시에, 사람이 전부 짚기에는 경우의 수가 많다. 테마 N개·회차 M개면 순서까지 고려해 최대 `N! × M^N`. 3개만 해도 실수 없이 짚기 어렵고, 4~5개면 불가능하다.

### 2.3 검증된 최적해 (수기 계산)

```
13:30  범계 (75분)   → 14:45 종료   [공백 5분]
14:50  몽   (70분)   → 16:00 종료   [공백 0분]
16:00  에이트립(70분) → 17:10 종료
```

총 공백 5분, 3시간 40분 소요. 프로그램이 이 답을 1위로 내놓는지를 인수 기준으로 삼는다 ([작업명세서.md](작업명세서.md) §8).

---

## 3. 목표

| # | 목표 | 성공 판정 |
|---|---|---|
| G-1 | 겹치지 않는 조합을 **빠짐없이** 계산 | §2.3 최적해가 결과에 포함되고 "공백 적은 순" 1위 |
| G-2 | 회차 입력의 마찰 제거 | 예약 화면 캡처 붙여넣기만으로 회차가 채워짐 |
| G-3 | 숫자가 아니라 **배치**를 보고 고를 수 있게 | 각 조합이 시간축 타임라인으로 렌더 |
| G-4 | 하나의 정답이 아니라 **선택지**를 제시 | 5가지 정렬 기준, 상위 12개 노출 |
| G-5 | 설치·빌드 없이 즉시 사용 | 파일 하나 열면 동작, 의존성 0개 |

### 비목표 (명시적으로 하지 않는 것)

- **테마 성격(장르·난이도) 가중치** — 시간 계산만 한다. 근거는 §4.6.
- **매장 간 이동시간** — 동일 매장 전제. 근거는 §4.7.
- **예약 대행** — 조합만 제시하고, 실제 예약은 사용자가 예약 사이트에서 한다.
- **회차 시간 자동 수집(링크 파싱)** — 근거는 §4.5.

---

## 4. 설계 결정과 근거

### 4.1 결과를 표가 아닌 타임라인으로

**결정**: 각 조합을 시간축 위 블록 + 공백으로 렌더링.

**근거**: "공백 50분"이라는 숫자만으로는 그 50분이 앞에 몰렸는지 뒤에 몰렸는지 알 수 없다. 실제 검토 과정에서 공백 총합이 같은 두 안(A: 0분+50분 / B: 45분+5분) 중 선택이 갈렸는데, 판단 근거는 **배치**였지 총합이 아니었다. 숫자 나열로는 이 차이가 드러나지 않는다.

### 4.2 최소 공백을 별도 지표로 노출

**결정**: 최소 공백을 헤더에 표시하고, 10분 미만이면 경고색. 정렬 기준에도 포함("여유 있는 순").

**근거**: 방탈출은 **탈출 실패 시 제한시간을 꽉 채우고 나온다.** 성공 전제로 짠 공백 0~5분 스케줄은 한 번의 실패로 무너진다. 총 공백만 최적화하면 이 리스크가 전혀 반영되지 않는다.

### 4.3 탐색은 DFS + 가지치기, 결과는 전량 보관

**결정**: 순열 전수 대신 점진적 DFS. 각 단계에서 `직전 종료 + 최소공백` 이후 회차만 후보로 두고 진행.

**근거**: 순진한 전수 탐색은 `N! × M^N` — 5개 × 10회차면 1200만 조합으로 브라우저가 멈춘다. 시간 겹침 제약이 매우 강해 가지치기 효율이 높다. 노드 80만 / 결과 5만개 상한을 두고, 도달 시 사용자에게 "탐색 한도 도달"로 알린다(조용히 자르지 않는다). 요구 범위인 테마 3~5개는 상한에 걸리지 않고 전수 탐색된다.

### 4.4 이미지 OCR이 주 입력, 텍스트가 보조

**결정**: 두 입력을 모두 지원하되, OCR 결과를 편집 가능한 텍스트 칸에 풀어서 표시.

**근거**: 텍스트만 지원하면 회차 시간을 전부 타이핑해야 해서 손으로 짜는 것과 시간이 비슷해진다 → 도구의 존재 이유가 사라진다. 반대로 OCR만 지원하면 캡처가 잘리거나 오독했을 때 복구할 수단이 없다. OCR 출력을 편집 가능한 텍스트로 되돌려 두 방식을 연결한다.

### 4.5 링크 파싱 — 보류

**결정**: 예약 페이지 URL 입력 방식은 미구현.

**근거**: 실제 확인 결과 (키오스케이프 예약 페이지)
- 정적 HTML에는 **회차 시간이 존재하지 않음**. 날짜 선택 시 별도 AJAX 요청으로 로드되는 구조.
- URL 파라미터(`theme_num` 등)가 서버 렌더링에 반영되지 않고 JS가 처리.
- 가져올 수 있던 것: 테마 설명, 장르, 난이도, **플레이시간**, 가격
- 못 가져온 것: 회차 시간, 매진 여부

즉 정작 필요한 데이터가 링크로는 오지 않는다. 게다가 순수 프론트엔드에서는 CORS로 외부 도메인 직접 요청이 차단되므로 프록시 서버가 전제된다. 비용 대비 효과가 낮아 보류.

### 4.6 장르·난이도 가중치 — 제외

**결정**: 시간 계산만 수행. 테마 성격은 반영하지 않는다.

**근거**: 사용자 요구. 다만 검토 과정에서 **테마 성격이 결론을 뒤집은 사례**가 있었음을 기록해 둔다 — 공백 5분짜리 최적안이 "에이트립은 문제가 많은 방"이라는 정보 하나로 무너졌다. 문제 방은 2시간 뒤 연속 플레이 하한을 두고 앞에 배치하는 구조라야 했기 때문. 향후 확장 시 최우선 후보(§5-1).

### 4.7 매장 간 이동시간 — 제외

**결정**: 동일 매장 전제.

**근거**: 원래 문제가 한 매장 안에서의 연타였다. 매장이 다르면 이동시간 행렬이 필요한데, 이는 입력 부담을 크게 늘린다. 확장 후보로 남긴다(§5-3).

### 4.8 OCR은 프록시 경유 (이번 웹 세팅에서 확정)

**결정**: 브라우저는 같은 출처의 `/api/ocr` 로만 요청하고, API 키는 서버(`serve.mjs`)에서만 붙인다.

**근거**: 프로토타입은 브라우저에서 `api.anthropic.com` 을 직접 호출했는데, 이 경로는 **원리적으로 동작하지 않는다** — CORS로 차단되고, 뚫더라도 API 키가 프론트엔드에 그대로 노출된다. 웹으로 올리는 이상 프록시는 선택이 아니라 전제다. 대신 프록시는 인증 헤더만 붙이는 통과 계층으로 최소화해, 프롬프트·모델 선택 같은 제품 로직은 앱에 남긴다.

키가 없거나 정적 호스팅(GitHub Pages 등)인 경우 OCR만 비활성화되고 **수동 입력 경로는 그대로 살아 있다.** 기능 저하가 전체 실패가 되지 않도록 한 것이 핵심.

---

## 5. 확장 후보

우선순위 순.

1. **테마 성격 가중치** — 문제 방은 앞쪽, 쉬운 테마는 뒤쪽 배치에 가점. §4.6 참조
2. **배포용 프록시** — Vercel Functions 등으로 `/api/ocr` 을 그대로 옮기면 정적 호스팅에서도 OCR 사용 가능. 링크 파싱도 같은 계층에서 해결
3. **매장 간 이동시간** — 매장이 다른 경우의 이동시간 행렬
4. **결과 저장·공유** — URL 쿼리스트링에 상태 직렬화
5. **회차 주기 추론** — 회차 몇 개만 넣어도 주기를 추정해 나머지를 채움

---

## 6. 열린 질문

- 상위 12개 노출이 적정한가? 실사용에서 스크롤이 길다면 6개로 줄이고 "더 보기"를 두는 안.
- 최소 공백 경고 기준 10분이 적정한가? 매장 규모·이동 동선에 따라 다를 수 있음.
- 부분 조합(F-08)을 기본 켬으로 둘 것인가? 현재는 기본 끔.
