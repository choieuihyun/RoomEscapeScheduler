# Floduler — 기획

작성일: 2026-08-20
문서 역할: **왜 만드는가 / 무엇을 만드는가.** 어떻게 만들었는지는 [작업명세서.md](작업명세서.md).

---

## 1. 한 줄 요약

방탈출 테마 여러 개를 하루에 연달아 예약("연타")할 때, **정해진 회차 시간표만으로 가능한 모든 조합을 계산해 대기 시간이 어디에 얼마나 생기는지 보여주는** 단일 페이지 웹 도구.

---

## 2. 배경

### 2.1 문제

같은 매장에서 방탈출 테마 3개를 하루에 연달아 예약하려 할 때, 각 테마의 회차가 서로 다른 주기로 돌아가 손으로 조합을 짜기 어렵다.

실제 사례 (2026-08-22, 동일 매장):

| 테마 | 소요시간 | 회차 주기 | 회차 시간 |
|---|---|---|---|
| 범계 | 75분 | 90분 | 10:30, 12:00, 13:30, 15:00, 16:30, 18:00, 19:30, 21:00, 22:30 |
| 왓 어 트립 | 70분 | 85분 | 10:20, 11:45, 13:10, 14:35, 16:00, 17:25, 18:50, 20:15, 21:40, 23:05 |
| 몽 | 70분 | 80분 | 10:50, 12:10, 13:30, 14:50, 16:10, 17:30, 18:50, 20:10, 21:30, 22:50 |

> 테마명은 원본 문서의 깨진 인코딩에서 복원한 값이다. 셋 중 둘은 역산이 틀려 사용자 확인으로 바로잡았다 — 경위는 [작업명세서.md](작업명세서.md) §11.1.

주기가 90 / 85 / 80분으로 모두 달라 최소공배수가 맞지 않는다. 시작 시간 협상이 불가능하고 **정해진 회차에만 들어갈 수 있으므로**, 테마 소요시간이 끝나도 다음 회차까지 대기 시간이 발생한다.

### 2.2 왜 도구가 필요한가

손으로 조합을 하나씩 짚어본 결과, 공백 총합이 **5분부터 165분까지** 벌어졌다. 순서만 바꿔도 종료 시간이 2시간 이상 차이 난다. 즉 이 문제는 "대충 짜도 비슷한" 문제가 아니라 **탐색할 가치가 있는 조합 최적화 문제**다.

동시에, 사람이 전부 짚기에는 경우의 수가 많다. 테마 N개·회차 M개면 순서까지 고려해 최대 `N! × M^N`. 3개만 해도 실수 없이 짚기 어렵고, 4~5개면 불가능하다.

### 2.3 검증된 최적해 (수기 계산)

```
13:30  범계 (75분)   → 14:45 종료   [공백 5분]
14:50  몽   (70분)   → 16:00 종료   [공백 0분]
16:00  왓 어 트립(70분) → 17:10 종료
```

총 공백 5분, 3시간 40분 소요. 프로그램이 이 답을 1위로 내놓는지를 인수 기준으로 삼는다 ([작업명세서.md](작업명세서.md) §8).

> **§4.14 후기로 이 판정 기준이 바뀌었다.** 도구는 더 이상 순서를 탐색하지 않고
> 카드 배치 순서를 그대로 따른다 — 위 최적해(범계→몽→왓 어 트립)는 카드를 그
> 순서로 직접 옮겨야만 나온다. §2.2 의 "순서까지 고려해 `N!×M^N`" 도 지금은
> `M^N`(회차 선택만)로 줄었다. 자세한 경위는 §4.14 후기.

---

## 3. 목표

| # | 목표 | 성공 판정 |
|---|---|---|
| G-1 | 겹치지 않는 조합을 **빠짐없이** 계산 | (카드 배치 순서 안에서) 매 회차 선택의 모든 경우가 결과에 포함 — §4.14 후기로 순서 자체는 더 이상 도구가 탐색하지 않는다 |
| G-2 | 회차 입력의 마찰 제거 | 예약 화면 캡처 붙여넣기만으로 회차가 채워짐 |
| G-3 | 숫자가 아니라 **배치**를 보고 고를 수 있게 | 각 조합이 시간축 타임라인으로 렌더 |
| G-4 | 하나의 정답이 아니라 **선택지**를 제시 | 5가지 정렬 기준, 상위 12개 노출 |
| G-5 | 설치·빌드 없이 즉시 사용 | 파일 하나 열면 동작, 의존성 0개 |

### 비목표 (명시적으로 하지 않는 것)

- **테마 성격(장르·난이도) 가중치** — 시간 계산만 한다. 근거는 §4.6.
- **매장 간 이동시간** — 동일 매장 전제. 근거는 §4.7.
- **예약 대행** — 조합만 제시하고, 실제 예약은 사용자가 예약 사이트에서 한다.
- **회차 시간 자동 수집(링크 파싱)** — 근거는 §4.5.
- **매진 여부 자동 인식** — 이미지에서는 시간만 읽는다 (§4.9). 매진 회차는 사용자가 빼고 넣는다 (§4.13).
- **테마 데이터 저장·재사용, 신테마 감시** — 근거는 §4.10.

---

## 4. 설계 결정과 근거

### 4.1 결과를 표가 아닌 타임라인으로

**결정**: 각 조합을 시간축 위 블록 + 공백으로 렌더링.

**근거**: "공백 50분"이라는 숫자만으로는 그 50분이 앞에 몰렸는지 뒤에 몰렸는지 알 수 없다. 실제 검토 과정에서 공백 총합이 같은 두 안(A: 0분+50분 / B: 45분+5분) 중 선택이 갈렸는데, 판단 근거는 **배치**였지 총합이 아니었다. 숫자 나열로는 이 차이가 드러나지 않는다.

### 4.2 최소 공백을 별도 지표로 노출

**결정**: 최소 공백을 헤더에 표시하고, 10분 미만이면 경고색. 정렬 기준에도 포함("여유 있는 순").

**근거**: 방탈출은 **탈출 실패 시 제한시간을 꽉 채우고 나온다.** 성공 전제로 짠 공백 0~5분 스케줄은 한 번의 실패로 무너진다. 총 공백만 최적화하면 이 리스크가 전혀 반영되지 않는다.

### 4.3 탐색은 DFS + 가지치기, 결과는 전량 보관

**결정**: 순열 전수 대신 점진적 DFS. 각 단계에서 `직전 종료 + 최소공백` 이후 회차만 후보로 두고 진행.

**근거**: 순진한 전수 탐색은 `N! × M^N` — 5개 × 10회차면 1200만 조합으로 브라우저가 멈춘다. 시간 겹침 제약이 매우 강해 가지치기 효율이 높다. 노드 80만 / 결과 5만개 상한을 두고, 도달 시 사용자에게 "탐색 한도 도달"로 알린다(조용히 자르지 않는다). 요구 범위인 테마 3~5개는 상한에 걸리지 않고 전수 탐색된다.

### 4.4 이미지 OCR이 주 입력, 텍스트가 보조

**결정**: 두 입력을 모두 지원하되, OCR 결과를 편집 가능한 텍스트 칸에 풀어서 표시.

**근거**: 텍스트만 지원하면 회차 시간을 전부 타이핑해야 해서 손으로 짜는 것과 시간이 비슷해진다 → 도구의 존재 이유가 사라진다. 반대로 OCR만 지원하면 캡처가 잘리거나 오독했을 때 복구할 수단이 없다. OCR 출력을 편집 가능한 텍스트로 되돌려 두 방식을 연결한다.

### 4.5 링크 파싱 — 보류

**결정**: 예약 페이지 URL 입력 방식은 미구현.

**근거**: 실제 확인 결과 (키오스케이프 예약 페이지)
- 정적 HTML에는 **회차 시간이 존재하지 않음**. 날짜 선택 시 별도 AJAX 요청으로 로드되는 구조.
- URL 파라미터(`theme_num` 등)가 서버 렌더링에 반영되지 않고 JS가 처리.
- 가져올 수 있던 것: 테마 설명, 장르, 난이도, **플레이시간**, 가격
- 못 가져온 것: 회차 시간, 매진 여부

즉 정작 필요한 데이터가 링크로는 오지 않는다. 게다가 순수 프론트엔드에서는 CORS로 외부 도메인 직접 요청이 차단되므로 프록시 서버가 전제된다. 비용 대비 효과가 낮아 보류.

### 4.6 장르·난이도 가중치 — 제외

**결정**: 시간 계산만 수행. 테마 성격은 반영하지 않는다.

**근거**: 사용자 요구. 다만 검토 과정에서 **테마 성격이 결론을 뒤집은 사례**가 있었음을 기록해 둔다 — 공백 5분짜리 최적안이 "왓 어 트립은 문제가 많은 방"이라는 정보 하나로 무너졌다. 문제 방은 2시간 뒤 연속 플레이 하한을 두고 앞에 배치하는 구조라야 했기 때문. 향후 확장 시 최우선 후보(§5-1).

### 4.7 매장 간 이동시간 — 행렬을 피해서 넣었다

**처음 결정**: 동일 매장 전제. 매장이 다르면 이동시간 **행렬**이 필요한데 입력 부담이 크다고 봤다.

**뒤집은 이유**: 부담이 큰 것은 이동시간이 아니라 **행렬**이었다.
행렬을 안 만들면 되는 문제였는데, 그걸 이동시간 자체의 비용으로 착각했다.

**대신 한 것**: 테마마다 **매장 이름**을 받고, 조건에 **기본 이동시간** 하나를 둔다.

| 안 | 입력 칸 | 판정 |
|---|---|---|
| 매장 쌍 행렬만 | 매장 3곳 = 3칸, 4곳 = 6칸 | 정확하지만 전부 채워야 한다 |
| 테마마다 "오는 데 N분" | N칸 | 같은 매장 연타에도 0을 일일이 넣어야 한다 |
| **매장명 + 기본값** | **N+1칸** | 채택 |

**결정적이었던 것은 지배적인 경우다.** 방탈출 연타는 대부분 **같은 매장**이고,
매장명만 있으면 그 경우가 **자동으로 0분**이 된다. 가장 흔한 입력이 공짜가 되는 셈이다.

**그다음 바로 부족해졌다** — *"A↔B 는 10분, B↔C 는 7분인데"*.
기본값 하나로는 당연히 못 맞춘다. 그래서 쌍 표를 **얹었다**:

- 매장이 둘 이상이면 **쌍 목록이 자동으로 생긴다** (사용자가 만들지 않는다)
- 손대지 않은 칸은 **기본값을 따라간다** — 실제로 다른 쌍만 고치면 된다
- 고친 칸은 색이 달라져 *"여긴 내가 정한 값"* 이 보인다

처음 안을 버린 게 아니라 **기본값이 바닥이 되고 쌍 표가 예외가 된 것**이다.
매장 3곳이어도 실제로 손대는 칸은 보통 한둘이다.

**방향은 구분하지 않는다.** A→B 와 B→A 는 걷는 시간이 같다.
두 번 적게 하면 입력만 두 배가 되고 서로 어긋날 여지가 생긴다.

**지도 API 는 쓰지 않는다.** 매장명으로 좌표를 얻는 것까지는 브라우저에서 되지만,
**실제 도보 시간은 못 얻는다** — 카카오 도보 길찾기는 제휴사 전용이고,
TMAP 은 CORS 로 브라우저 호출이 막혀 있어 중계 서버가 필요하다.
직선거리로 추정하는 안도 검토했으나 접었다: **걸어서 가는 게 기본인데 직선거리는 말이 안 된다.**
틀린 숫자는 없는 숫자보다 나쁘고(§4.10 과 같은 논리), 사용자는 자기 동네를 API 보다 잘 안다.

**빈칸은 제약이 아니다.** 매장을 안 적은 테마에는 이동시간을 붙이지 않는다.
모르는 것을 지어내면 조합이 조용히 사라진다. 대신 매장을 몇 곳 적었는지,
지금 이동시간이 실제로 걸리는지를 조건 아래 한 줄로 계속 알려 준다.

**이동과 잔여는 색이 아니라 모양으로 가른다.**
처음엔 이동도 주황, 넓은 공백도 주황이라 같은 색이 두 가지를 뜻했다.
색은 이미 *빠듯함*(빨강·회색·주황)을 나타내는 데 쓰고 있어서, 거기에 종류까지 얹으면 겹친다.

| | 막대 | 라벨 |
|---|---|---|
| **이동** | **채운 주황** | 채운 알약 `이동 8분` |
| **잔여** | **빗금** | 맨 글자 `잔여 17분` (빠듯하면 빨강) |

**실선이면 걷는 시간, 빗금이면 노는 시간.** 색맹이어도 구분되고, 색은 빠듯함만 맡는다.
숫자도 합쳐서 보여주지 않는다 — `공백 25분` 은 8분이 이동인지 알려주지 않는다.
머리·타임라인·요약줄·복사 텍스트 네 곳 모두 `이동 N분 · 잔여 N분` 으로 쪼개 적는다.

**최소 공백에 더한다, 대체하지 않는다.** 최소 공백은 정리·여유이고 이동은 그 위의 실제 소요다.
그래서 결과 머리의 *최소* 는 **이동을 뺀 값**(`minWait`)으로 보여 준다 —
이동 10분이 낀 15분 공백을 "최소 15분" 이라 하면 거짓말이다.
좁음/넓음 색 구분도 같은 기준으로 바뀌었다.

### 4.8 이미지 인식은 브라우저 안에서 (AI·과금 없이)

**결정**: 인식 엔진(tesseract)을 저장소에 넣고 브라우저에서 직접 돌린다. AI 비전 API는 쓰지 않는다.

**근거**: 프로토타입은 브라우저에서 `api.anthropic.com` 을 직접 호출했는데 이 경로는 원리적으로 동작하지 않는다 — CORS로 차단되고, 뚫더라도 API 키가 노출된다. 프록시를 세우면 해결되지만 그러면 **키 발급 + 배포 + 이미지 1장당 과금**이 전제가 된다.

요구는 "윈도우든 폰이든 이미지 넣으면 시간표가 나온다" 였고, 여기에 과금과 배포를 얹지 않는 쪽을 택했다. 대신 §3의 원칙 하나("외부 라이브러리 없음")를 포기했다. 이미지 자동 인식과는 양립할 수 없는 조건이라, 도구의 존재 이유(입력 마찰 제거, G-2) 쪽을 남겼다.

**대가**: 첫 사용 시 약 5MB 다운로드, 그리고 저대비 화면에 대한 전처리 의존. 전처리 없이는 인식률이 0에 수렴한다 (작업명세서 §9.3).

**결과**: 실제 예약 화면 캡처로 동작을 확인했다 (작업명세서 §9.4). 이로써 §4.10이 정한 두 입력 경로가 모두 살아 있다.

### 4.9 인식을 쉽게 만드는 대신 파서가 떠받친다

**결정**: OCR에 배치 해석을 요구하지 않는다. 숫자와 콜론만 읽게 하고 나머지는 텍스트 규칙으로 처리한다.

**근거**: 예약 화면은 시간과 표시가 격자로 흩어져 있어 "어느 표시가 어느 시간에 붙었나" 를 푸는 게 어려워 보인다. 그런데 실제 화면을 뜯어보니 세 가지가 성립했다 — **시간에는 콜론이 있고, 달력 숫자에는 없으며, 회차는 항상 시간 순으로 나온다.** 이 셋만으로 배치 해석이 통째로 대체된다.

그래서 인식기에 남는 요구는 "레이아웃 이해" 가 아니라 "글자를 순서대로 읽기" 뿐이고, 그건 값싼 엔진도 하는 일이다. 실제로 오전/오후조차 읽을 필요가 없어져 한글 데이터 12MB가 불필요해졌다.

**어려운 요구를 없애는 쪽이 좋은 인식기를 구하는 것보다 효과가 컸다.** 이 판단이 AI 없이 가는 선택을 성립시켰다.

### 4.10 입력 경로는 두 가지로 고정한다

**결정**: **시간 직접 입력**과 **사진 넣기**, 둘만 지원한다. 테마 데이터를 미리 채워 저장해 두는 방향은 가지 않는다.

**검토한 것**: 회차 시간표는 날마다 바뀌지 않고(바뀌는 건 매진 여부인데 그건 안 쓴다), 주기도 일정하다(90/85/80분). 그래서 한 번 채워두면 계속 쓰는 데이터셋이 성립한다 — 테마 저장소, 주기로 회차 생성, 내보내기/가져오기까지 묶음으로 검토했다.

**그럼에도 안 하는 이유**: 데이터셋은 **채워져 있을 때만** 값어치가 있는데, 채워져 있으려면 계속 관리해야 한다. 그리고 관리의 핵심인 **"신테마가 나왔는지" 는 이 구조에서 알 방법이 없다** — 예약 사이트를 주기적으로 긁어야 하고 그건 서버가 필요하다(§4.5와 같은 벽). 즉 저장소를 만들어도 조용히 낡아가고, 낡은 데이터는 없는 것보다 나쁘다.

반면 그때그때 넣는 방식은 **항상 최신이다.** 사용자가 지금 보고 있는 예약 화면이 곧 진실이기 때문이다.

**따라오는 결론**: 매번 넣어야 하므로 **넣는 마찰이 이 도구의 생명**이다. 사진 인식(§4.8)이 부가 기능이 아니라 핵심인 이유가 여기 있다. 기능을 늘리는 대신 이 두 경로를 빠르고 정확하게 만드는 데 집중한다.

### 4.11 공백은 줄이기만 하는 것이 아니다

**결정**: 지정한 시간대에 N분 이상 공백을 **요구**하는 조건을 넣는다 (식사 시간 확보).

**근거**: 이 도구는 공백을 최소화하도록 만들어졌는데, 4~5연방이면 5~7시간짜리 일정이 된다. 그 사이에 밥 먹을 시간이 없으면 **공백 0분짜리 최적해가 실제로는 못 쓰는 답**이다. 기존 조건(최소·최대 공백)은 전부 공백을 억제하는 방향뿐이라 이 요구를 표현할 방법이 없었다.

성격이 반대인 조건이라 구현 위치도 다르다 — 억제 조건은 가지치기로 처리되지만, "어딘가에 있어야 한다" 는 경로가 완성돼야 판정된다 (작업명세서 §6.4).

### 4.12 순서 제약이 성격 가중치를 대체한다

**결정**: 사용자가 순서를 직접 묶을 수 있게 한다. 장르·난이도 가중치 모델(기존 확장 후보 1순위)은 만들지 않는다.

**근거**: §4.6에 기록해 둔 실패 사례 — 공백 5분짜리 최적안이 "왓 어 트립은 문제가 많은 방" 이라는 정보 하나로 무너졌고, 그 방은 앞에 배치해야 했다. 이걸 풀려고 성격을 수치화하는 모델을 떠올렸지만, 실제로 필요했던 것은 **"이 방을 앞에 둬라" 한 마디**였다.

난이도·장르를 점수로 만드는 일은 근거 없는 숫자를 만들기 쉽고, 만들어도 사용자가 이미 아는 결론을 우회해서 표현하는 것에 지나지 않는다. 사용자가 아는 것을 그대로 입력하게 하는 편이 정확하고 훨씬 싸다.

### 4.13 매진은 옵션이 아니라 안내로

**결정**: `매진 회차 제외` 체크박스를 없앤다. 대신 **"매진된 회차는 빼고 넣어 주세요"** 라고 화면에 적는다. 매진으로 표시되어 들어온 회차는 조용히 제외한다.

**근거**: 체크박스가 성립하려면 매진 여부가 데이터에 들어와 있어야 하는데, 두 입력 경로 어느 쪽에서도 안 들어온다.

- **사진**: 숫자와 콜론만 읽으므로(§4.9) 매진 표시는 애초에 인식 대상이 아니다
- **직접 입력**: 매진 회차 뒤에 `매진` 을 타이핑하는 것보다 그 줄을 안 쓰는 게 빠르다

즉 체크박스는 **한 번도 켜질 일이 없는 데이터에 대한 옵션**이었다. 켜져 있어도 아무것도 걸러내지 않으면서, 사용자에게는 "이 도구가 매진을 알아서 처리한다" 는 잘못된 인상을 준다. 그 인상이 제일 위험하다 — 예약할 수 없는 조합을 1위로 믿게 만든다.

옵션을 지우고 **책임이 어디 있는지 말로 적는 편**이 정확하다. 다만 예약 페이지를 통째로 복사해 붙이면 `매진` 이 텍스트에 딸려 오므로, 그 경우에만 자동으로 걸러내고 걸러냈다는 사실을 취소선으로 보여 준다.

### 4.14 순서는 드래그 + 자리 잠금으로

**결정**: 카드 순서 드롭다운(`첫 타로`/`마지막으로`)을 걷어내고, **카드를 끌어 옮기고 잠그는** 방식으로 바꾼다.

**계기**: "첫 타/마지막은 왜 넣었냐, 유저가 순서대로 잘 넣으면 되는 거 아니냐" 는 질문. 이 질문이 드러낸 것은 기능의 문제가 아니라 **UI가 전제를 전달하지 못했다**는 사실이다.

**카드 순서는 결과에 아무 영향이 없다.** 탐색기가 모든 순서를 다 시도하기 때문이다(실측: 카드를 어떻게 늘어놓아도 조합 84개, 1위 동일). 그런데 화면 어디에도 그렇게 적혀 있지 않으니, 카드 순서가 곧 진행 순서라고 읽히는 게 당연하다. 그래서 **"순서는 도구가 정합니다"** 를 화면에 명시했다.

**설계**: 잠근 카드는 그 자리에, 잠그지 않은 카드는 탐색기가 배치한다. 개념이 하나인데 표현력은 드롭다운보다 넓다 — 맨 위 잠금은 첫 타, 맨 아래 잠금은 마지막, **가운데 잠금은 드롭다운으로는 표현할 수 없던 것**, 전부 잠금은 "이 순서 그대로 두고 회차만 최적화" 다.

**전부 잠그는 선택을 굳이 막지 않는 이유**: 순서 탐색은 이 도구의 핵심 가치지만(§2.2 — 순서만 바꿔도 공백 5분↔165분), 이미 순서를 정해 놓고 회차만 맞추고 싶은 때도 있다. 그건 사용자가 정보를 더 갖고 있다는 뜻이므로 막을 이유가 없다. 다만 **기본값은 전부 자유**다.

**후기 — 이 결정을 통째로 뒤집었다.** "자리고정보다는 테마 배치 순서대로 조합 계산을 도출해줘" 라는 요청. 위에서 "카드 순서는 결과에 아무 영향이 없다" 고 못 박아 뒀던 바로 그 전제를 뒤집는 요청이라, 결과를 명확히 하고 진행했다(설계 결정 자체를 실행 전에 사용자에게 재확인) —

> 지금은 카드를 끌어 옮겨도 그건 화면 표시 순서일 뿐, 계산은 여전히 모든 순서를 다 탐색합니다(자리 고정을 건 카드만 그 자리에 묶임). **"카드 순서 = 계산 순서"로 전면 변경**하면, 자리 고정 개념 자체가 의미를 잃어(모든 카드가 항상 자기 위치에 고정되므로) 그 버튼도 같이 없앨 수 있는데, 그래도 이 방향이 맞습니까?

→ **"카드 순서 = 계산 순서 (전면 변경)"** 로 확정.

**바뀐 것**: `search()` 는 이제 순서를 탐색하지 않는다. `list[i]` 를 인덱스 오름차순으로만 방문한다(`dfs(curEnd, start)` 가 `start`부터 `i<n` 까지만 훑고 `dfs(end, i+1)` 로 내려간다 — `used[]` 도 `lockPos` 도 필요 없어졌다). "자리 고정" 버튼·`lockbtn`·`th.lock`·카드의 파란 잠금 표시를 전부 걷어냈다 — 모든 카드가 항상 자기 자리에 "고정"돼 있는 셈이니, 그중 일부만 골라 잠근다는 개념이 성립하지 않는다. 부분 조합(F-08)은 그대로 살아있지만 의미가 바뀌었다 — 예전엔 "일부 테마만 넣은 **모든 순열**", 지금은 "카드 순서를 유지한 채 일부만 **건너뛰기**" 다(재배열은 여전히 없다).

**이것으로 §2.2·G-1 이 뒤집힌다.** §2.2 는 "순서까지 고려해 최대 `N!×M^N`" 을 이 도구가 존재하는 핵심 근거로 들었고, G-1 은 "§2.3 최적해(범계→몽→왓 어 트립 순서)가 결과에 포함" 을 성공 판정으로 삼았다. 카드 기본 배치 순서(범계, 왓 어 트립, 몽)에서는 이 순서 자체가 이제 **도달 불가능**하다 — 사용자가 카드를 그 순서로 직접 끌어 옮겨야만 나온다. §2.3 의 수기 계산 결과 자체는 여전히 사실(공백 5분짜리 조합이 존재한다는 것)이지만, "도구가 순서까지 자동으로 찾아준다" 는 G-1 의 판정 방식은 더는 맞지 않는다. 탐색 공간도 순열이 빠지며 `N!×M^N` → `M^N` 로 줄었다 — `test/acceptance.mjs` [2] 실측 기준 92개(범계→왓어트립→몽 고정 순서, 이전엔 순열까지 합쳐 몇 배 더 많았다).

**결과가 0개일 때의 안내문에도 "테마 순서를 바꿔보세요" 를 추가했다** — 순서가 고정된 뒤로는 "이 조건으로는 이 순서에서 안 나온다" 는 경우가 늘 수 있는데, 최소 공백·시간 범위만 건드리라는 조언은 이제 가장 흔한 해법(순서를 바꿔본다)을 빼먹은 셈이라서다.

**결정**: 정적 호스팅(GitHub Pages)으로 공개한다. 서버·DB·로그인은 넣지 않는다.

**근거**: "여러 사람이 쓰게 하고 싶다" 를 뜯어 보면 대부분 **이미 되고 있다.** 정적 파일이라 접속자끼리 서로 간섭하지 않고, 공유는 링크에 상태를 담아 이미 해결돼 있다(§5.3). 남는 것은 **저장** 하나이고, 그것만이 서버를 부르는 이유다.

그런데 저장에도 층이 있다. 브라우저에 남기는 것(`localStorage`)과 파일로 빼는 것은 서버 없이 되고, **기기를 넘나드는 "내 것"** 부터가 DB + 로그인이다.

**DB 를 지금 넣지 않는 이유**: 코드보다 따라오는 것이 비싸다 — 로그인 강제, 보안 규칙(브라우저가 DB 에 직접 붙으므로 규칙이 곧 방어선이다), 남의 데이터를 보관하는 책임, 그리고 "파일만 올리면 끝" 이던 것이 유지보수 대상이 된다. 아직 쓰는 사람이 없는 상태에서 이걸 먼저 지으면 **아무도 안 쓰는 DB 를 관리하게 된다.**

**DB 가 값을 하기 시작하는 지점**은 따로 있다 — §4.10 에서 접었던 **공용 테마 시간표**다. 혼자서는 신테마를 알 수 없어 데이터가 낡는다는 것이 접은 이유였는데, **사람이 모이면 그 전제가 무너진다.** 새 테마에 간 사람이 넣고, 시간표가 바뀌면 누군가 고친다. 사람 수가 신선도를 유지해 준다.

**따라서 순서는**: ① 정적 배포 → ② `localStorage` 로 재방문 편의 → ③ 사용자가 붙고 "남들 시간표도 보고 싶다" 가 실제로 생기면 그때 DB. ③을 먼저 하는 것은 수요를 짐작해서 짓는 일이다.

### 4.16 localStorage 는 저장소가 아니라 편의다

**결정**: 넣더라도 "마지막 상태 자동 복구" 로만 기대하고, **보관은 링크가 맡는다.**

**근거**: `localStorage` 는 경고 없이 사라진다 — 브라우저 데이터 삭제, 시크릿 모드 종료, 기기 변경, 저장공간 부족 시 정리. 특히 **iOS 사파리는 7일간 방문이 없으면 지운다.** 폰으로 쓸 일이 많은 도구에서 이건 작은 문제가 아니다.

반면 **공유 링크는 이미 백업 수단이다.** 카톡 "나에게 보내기" 나 메모에 붙여 두면 브라우저를 밀어도 복원된다. 그래서 역할을 나눈다 — `localStorage` 는 매번 링크를 누르지 않아도 되게 하는 편의, 링크는 실제 보관. 이 구분을 흐리면 사용자가 날아갈 것을 믿게 된다.

**구현은 §4.26.**

### 4.17 "동호회가 같이 쓴다" 는 접속의 문제였다

목표가 늦게 분명해졌다 — **방탈출 동호회 사람들이 이 페이지를 같이 쓰는 것.**

"같이 쓴다" 를 뜯어 보면 실제로 필요한 것은 세 가지인데, 두 개는 이미 되어 있었다.

| 필요한 것 | 지금 상태 |
|---|---|
| 각자 열어서 쓴다 | **주소 하나면 된다** — 정적 배포로 끝 |
| 짠 결과를 단톡방에 넘긴다 | 일정 복사(사람이 읽는 글) · 링크 복사(입력값 통째) 이미 있음 |
| 남의 시간표를 내 계정에 모아 둔다 | **없음.** DB 가 필요한 유일한 항목 (§4.15) |

세 번째는 아직 실제로 생기지 않은 요구다. 그래서 이번에 하는 일은 **GitHub Pages 켜기 하나**다.
동시 접속·공동 편집은 애초에 이 도구의 성격이 아니다.
시간표를 짜는 일은 혼자 30초 걸리는 작업이고, 공유되는 것은 과정이 아니라 **결과**다.

기본 데이터(범계·몽·왓 어 트립)는 **공개 배포에서도 그대로 둔다.**
동호회 사람들에게 저건 남의 데이터가 아니라 아는 테마이고, 첫 화면에서 "이렇게 넣으면 이렇게 나온다" 를
설명 없이 보여 준다. 지우는 비용은 카드 삭제 몇 번으로 낮다.

### 4.18 계정을 붙이되, 도구는 로그인 없이 완결된다

§4.15 에서 "DB 가 값을 하는 경계" 를 적어 뒀는데, 그 요구가 도착했다 —
**내가 했던 일정과 하고 싶은 일정을 계정에 쌓아 두는 것.** 링크로는 안 되는 일이다.
링크는 보관이 아니라 전달이고, 잃어버리면 그걸로 끝이기 때문이다.

**Firebase 를 고른 이유는 Supabase 가 잠들기 때문이다.**
기능만 보면 Supabase 도 충분했지만, 무료 등급은 **7일 동안 요청이 없으면 프로젝트를 정지**시킨다.
동호회 도구는 원래 띄엄띄엄 쓴다 — 2주 쉬었다가 누가 들어오면 그 사람은 오류를 본다.
Firebase Spark 는 정지가 없고, 한도를 넘겨도 청구가 아니라 그날치 차단이라 카드도 필요 없다.

**로그인은 아이디/비밀번호다.** 구글 로그인이 기술적으로는 더 편하지만
"중요 정보는 안 들어가고 단순 공유용" 이라는 성격에 맞춰 가장 가벼운 쪽을 골랐다.
다만 *가볍게* 는 UX 의 이야기지 보안의 이야기가 아니다 —
비밀번호는 우리가 만지지 않고 Firebase Auth 가 해시해 보관한다.
직접 만들면 반드시 틀리는 부분이라 남에게 맡긴다.
대신 메일 주소를 안 받으므로 **재설정 수단이 없고**, 그 사실을 로그인 화면에 적어 둔다.

**저장 단위는 "확정된 시간표" 다.**
공유 링크의 `snapshot` 은 *풀이의 입력값*이라 다시 계산하면 결과가 나온다.
그런데 계산 로직이나 상한(`KEEP_CAP`)이 바뀌면 순위가 달라진다 —
**"내가 했던 일정" 이 나중에 조용히 다른 일정으로 바뀌는 것**은 기록으로서 실격이다.
그래서 확정된 시각을 사실로 굳혀 저장하고, `snapshot` 은 "다시 짜 보기" 용으로 곁에 둔다.
지나간 일은 사실이고, 앞으로 할 일은 입력값이다.

**로그아웃 경로는 예전과 바이트 단위로 같다.**
`cloud.js` 와 Firebase SDK 는 로그인 버튼을 눌러야 처음 내려온다.
설정(`firebase-config.js`)이 비어 있으면 계정 기능만 조용히 꺼지고 나머지는 그대로 돈다.
이미지 인식을 지연 로드로 붙였던 것과 같은 규칙이다 — **안 쓰는 사람에게 짐을 지우지 않는다.**
다만 정직하게 적어 두면, 이 기능은 "CDN 없이 오프라인" 이라는 성질을 **로그인에 한해** 깬다.

### 4.19 어두운 빈티지에서 밝은 현대형으로

처음 색은 방탈출의 분위기(어두운 방, 황동 자물쇠)에서 가져왔다. 쓰다 보니 **칙칙하다**는 판단이 나왔고,
"토스처럼" 이라는 기준이 주어졌다. 밝은 배경 · 파란 포인트 · 큰 라운드 · 테두리 대신 그림자.

**마크업은 한 줄도 안 건드렸다.** 클래스명이 전부 JS 가 붙잡는 손잡이라서다 —
`.blk`, `.gap.tight`, `.tab.on` 같은 것들은 렌더 코드가 직접 만들어 낸다.
색만 바꾸는 작업에서 구조를 흔들면 고장 지점이 CSS 밖으로 번진다.

**두 가지가 색을 바꾸는 것만으로 깨졌다.**

*공백이 안 보이게 됐다.* 원래 공백 띠는 `rgba(...,.18)` 짜리 옅은 빗금이었다.
어두운 바닥에서는 충분했지만 흰 바닥에서는 사실상 사라진다.
**"공백을 더 눈에 띄게" 는 사용자가 두 번 요구했던 것**이라, 불투명한 색으로 다시 칠하고
세 단계(좁음 빨강 · 보통 회색 · 넓음 주황)를 유지했다.

*시각이 잘렸다.* `10:20 – 11:35` 가 `10:20` 으로 줄어들었다.
타임라인을 넓히는 계산에 `+18` 이라는 상수가 박혀 있었는데, 그게 옛 블록 여백이었기 때문이다.
여백을 CSS 에서 읽도록 바꿔 상수를 없앴다 — **디자인 값이 로직에 복사되어 있으면 언젠가 어긋난다.**

두 결함 모두 테스트가 못 잡는다. `@core` 는 순수 로직이라 CSS 를 모른다.
그래서 **바꾸기 전 화면을 찍어 두고 같은 화면을 다시 찍어 비교**했고, 이름 훑기·시각 잘림 개수를
숫자로 재서 0 으로 되돌린 뒤에 끝냈다. 눈으로만 봤으면 둘 다 놓쳤을 것이다.

### 4.20 이미지를 여러 장 이어 붙인다

**문제**: 회차가 많은 매장은 캡처 한 장에 다 안 들어간다. 오전·오후로 나눠 두 장을 찍는 경우가 흔하다.

**전에는**: 이미지를 첨부할 때마다 `th.raw` 를 통째로 덮어썼다. 두 번째 캡처를 넣으면 첫 번째가 사라졌다.

**1차 시도(폐기)**: 이어 붙이고 **통째로 다시 해석**했다. 화면상 잘 돼 보였는데,
"합치기 처리는 잘 되나?" 라는 질문에 순서를 꼬아 재현하자 바로 깨졌다:

- 오후 캡처(14:00)를 먼저 첨부하고 오전 캡처(09:00)를 나중에 첨부 → `09:00`이 `21:00`으로 둔갑
- 같은 이미지를 실수로 두 번 첨부 → 두 번째 사본이 12시간 밀려 **새벽 시각이 새로 생김**

원인은 `parseSessions` 의 **오름차순 12시간 보정**이다. "직전 값보다 작으면 시간이 지난 것"
이라는 가정은 **한 장 안에서**는 맞지만, 이어 붙인 문자열을 통째로 재해석하면 그 가정이
이미지 "사이" 에도 걸린다. 두 번째 이미지가 첫 번째보다 이른 시각으로 시작하는 순간 —
오전/오후 순서로 찍었거나, 같은 이미지를 두 번 넣었거나 — 조용히 틀린다.

**최종 결정**: 텍스트를 합치지 않는다. **이미 절대시각으로 확정된 세션 배열**을 합친다.
각 이미지는 자기 안에서만 `parseSessions` 를 거치므로 보정이 그 계약(한 장 안에서만 유효) 을
벗어나지 않는다. 합칠 때는 같은 시각이 겹치면 매진 여부만 OR 로 합친다.

- 파일 선택창에서 **여러 장을 한 번에** 고를 수도, 나중에 **또 첨부**할 수도 있다
- 워커가 하나뿐이라 동시에 돌리면 진행 표시가 서로를 덮어쓴다 — **차례로** 처리한다
- 겹치는 시각은 자동으로 하나가 된다 — 순서와 무관하게, 텍스트가 아니라 절대분 값으로 비교하므로
- 버튼 문구가 `이미지 첨부` → `이미지 추가` 로 바뀌어 "더 넣을 수 있다" 는 걸 알려준다
- 칩에 `사진 N장에서 읽음` 으로 몇 장을 모았는지 남는다

**손으로 고친 뒤 이미지를 추가해도 같은 길을 탄다.** 직접 입력한 값(이미 절대시각으로 파싱된
`th.sessions`) 도 같은 병합에 들어간다 — 사용자가 넣은 것을 조용히 지우지 않는다.

**교훈**: "잘 되나?" 라는 되물음이 없었으면 순서 의존 버그를 그대로 커밋했을 것이다.
가짜로 만든 시나리오(순서 뒤섞기·중복 첨부)로 실제 재현해 보기 전에는 병합이 맞는지 알 수 없었다.

**그다음 문제**: 병합을 항상 켜 두니, 정말 덮어쓰고 싶을 때(잘못 읽힌 이미지를 다시 넣는 경우)
길이 카드를 지웠다가 다시 만드는 것뿐이었다. **`이미지 여러장 넣기` 체크박스**를 카드마다 둔다
(처음 이름은 `이어 붙이기` 였으나 더 직관적인 이름으로 바꿨다).
기본은 켜짐(지금까지의 동작) — 끄면 다음 첨부부터 **있던 값을 지우고 새로 시작**한다.

**여러 장을 한 번에 고른 경우의 경계**: 체크를 끈 상태에서 두 장을 한 번에 고르면,
지워지는 것은 "이전에 있던 값" 이지 "이번에 고른 여러 장" 이 아니다 — 그 두 장끼리는 평소처럼
합쳐진다. 그렇지 않으면 여러 장 첨부(§4.20)가 이 스위치 하나로 무력화된다.
### 4.21 사진에서 온 값은 항상 한 번 보게 한다

**결정**: `th.source` 가 `image`/`image-edited` 인 테마는 해석 결과 아래에 항상 경고를 띄운다 —
"사진에서 읽은 값입니다 — 시간이 제대로 맞는지 한 번 확인해 주세요. 인식이 부정확할 수 있습니다."

**왜 조용히 넘기면 안 되는가**: 계산은 입력이 틀렸는지 모른다. 형식만 맞으면 존재하지 않는
회차도 똑같이 시각 하나일 뿐이라 아무 저항 없이 조합에 들어간다. §4.20 에서 실제로 겪었듯
인식은 순서·중복 같은 조건에 따라 틀릴 수 있고, 사람이 한 번은 봐야 잡힌다.

**손으로 고쳐도 계속 띄운다.** `image-edited` 로 바뀐 뒤에도 경고를 유지한다 — 한 글자를 고쳤다고
나머지 회차까지 전부 확인했다는 보장은 없다. 직접 입력(`manual`)에는 뜨지 않는다 —
사람이 직접 친 값을 두고 "정확한지 확인하라"는 건 의미가 없다.

### 4.22 시작 화면은 비워 둔다

**전에는**: 페이지를 열면 예시 매장 3곳(범계·몽·왓 어 트립)이 이미 채워져 있었다.
"손으로 안 넣고 바로 눌러볼 수 있게" 라는 의도였지만, 실사용자에게는 자기 데이터를 넣기 전에
남의 매장 이름부터 지워야 하는 화면이었다.

**결정**: 시작 화면은 빈 화면(`테마가 없습니다`)이다. 예시 데이터는 `기본값 채우기` →
**`테스트 데이터 넣기`** 로 이름을 바꾼 버튼을 눌러야만 들어간다. 공유 링크가 깨졌을 때의
안전망도 같은 이유로 예시 데이터 대신 빈 화면으로 바꿨다 — 예시 데이터는 이제
"눌러야 나오는 데모" 이지 "항상 있는 안전한 기본값" 이 아니다.

**후기 (F-15 이후)**: `회차 불러오기`(F-15, §4.30 근처 작업명세서 참고)가 붙은 뒤로는
**`테스트 데이터 넣기` 버튼을 없앴다.** 처음 온 사람이 감을 잡는 방법이 가짜 예시
데이터 대신 **진짜 예약 사이트 데이터를 불러와 보는 것**으로 대체됐기 때문이다 —
가짜 매장 이름을 지우고 시작하던 문제(위 "전에는")를 다시 만들지 않으면서도, 이제는
가짜 데이터 자체가 필요 없어졌다. 사용법 투어의 첫 스텝도 `#seedBtn` → `#loadBtn`
으로 옮겼다.

### 4.23 사용법 투어 (스포트라이트)

**결정**: 화면을 어둡게 덮고 설명할 요소 하나만 구멍을 뚫어 보여주는 방식(코치마크)으로
전체 사용법을 짚어준다. 외부 라이브러리(`driver.js` 등) 대신 직접 짰다 — 서버·CDN 없이
정적 파일만으로 돌아가는 이 프로젝트의 구조를 새 의존성 하나 때문에 깨고 싶지 않았다.

**7단계**: 테스트 데이터 넣기 → 테마 추가 → 시간표 넣기(직접 입력/캡처 붙여넣기/이동시간) →
조건 정하기 → 조합 계산 → 결과 확인 → 공유하기. 테마 카드처럼 시작 화면(§4.22)에 없을 수도
있는 요소 대신, 항상 존재하는 버튼·섹션만 짚는다 — 없는 단계는 자동으로 건너뛴다.

**언제 뜨는가**: `localStorage` 에 처음 봤다는 표시가 없는 사람에게만 자동으로, 화면이 자리
잡은 뒤(0.5초) 뜬다. 헤더의 **`사용법 보기`** 버튼으로 언제든 다시 볼 수 있다.

**구현**: 대상 요소의 `getBoundingClientRect()` 위에 테두리만 있는 스팟을 놓고, 그 스팟에
`box-shadow: 0 0 0 9999px rgba(...)` 를 걸어 화면 전체를 어둡게 칠하되 스팟 자리만 뚫리게 한다.
전체를 덮는 투명한 `.tour-dim` 레이어가 클릭을 가로채 — 투어 도중에는 버튼으로만 진행하고,
뒤 화면을 실수로 건드리지 않는다. 단계가 바뀌면 대상을 `scrollIntoView` 로 가운데로 옮긴 뒤
자리를 다시 잰다.

### 4.24 다크 모드

**결정**: **시스템 설정을 자동으로 따르되, 헤더의 `다크 모드`/`라이트 모드` 버튼으로 수동 전환**할
수 있게 한다. 수동으로 고른 값은 `localStorage` 에 남아 이후엔 시스템 설정보다 우선한다 — 버튼을
누른 적이 없으면 계속 시스템을 따라가고, 한 번 누르면 그때부터는 그 선택이 고정된다.

**색은 전부 CSS 변수 토큰이라 값만 하나 더 정의하면 됐다.** `:root` 에 라이트 팔레트가 이미
역할별로(`--bg`, `--surface`, `--ink`...) 분리돼 있어서, 다크 팔레트를 `@media
(prefers-color-scheme: dark)` 와 `[data-theme="dark"]` 두 자리에 얹는 것으로 끝났다 — 컴포넌트
CSS(카드·버튼·타임라인...)는 전부 토큰만 참조하므로 단 한 줄도 건드리지 않았다.

**그런데 토큰 몇 개는 "글자색"과 "흰 글자를 얹는 배경색"을 겸하고 있었다.** 예를 들어 선택된
탭/정렬 칩은 `background:var(--ink)` 로 채웠는데, 라이트 모드에서 `--ink` 는 거의 검정이라 흰
글자가 잘 보이지만, 다크 모드에서 `--ink` 를 거의 흰색으로 뒤집으면 그 칩은 흰 배경에 흰 글자가
되어 사라진다. 파랑도 마찬가지: 버튼 배경(흰 글자를 얹어야 하니 진해야 함)과 `h1`/`.busy` 같은
본문 글자색(어두운 배경 위에 얹어야 하니 밝아야 함)이 같은 `--blue` 토큰을 쓰고 있어서, 다크
모드에서는 두 요구가 정반대 방향으로 갈렸다. **역할을 토큰으로 쪼갰다** — `--strong`/`--strong-ink`
(선택 칩 전용, 다크에서는 반대로 밝은 배경+어두운 글자로 뒤집는다), `--blue`(버튼 배경)
`--blue-hover`(버튼 hover) `--blue-press`(틴트 배경 위 글자) `--blue-text`(카드/페이지 배경 위
글자) 로 분리. 라이트 모드 값은 전부 기존 색 그대로 넣어서, 라이트 모드 화면은 픽셀 하나 안
바뀐다.

**공백 빗금·이동 알약도 토큰화했다.** `.gap`(잔여)의 세 단계 빗금 색, `.mv`(이동)의 채운 알약
색은 지금까지 컴포넌트 CSS 안에 직접 박혀 있었다(§4.7, §4.19) — 다크 모드에서 그대로 쓰면 밝은
파스텔이 어두운 카드 위에서 너무 튄다. `--gap-a/b`, `--gap-big-a/b`, `--gap-tight-a/b`,
`--mv-line/fill/text` 로 뽑아내 다크 전용 값을 따로 정의했다.

**대비는 라이트 모드 때와 같은 방식으로 실측했다** — 최종 팔레트의 모든 글자/배경 쌍(본문,
칩, 경고, 이동 알약, 선택된 탭...) 을 WCAG 공식으로 계산해 4.5:1(작은 설명 문구는 완화해 3:1)
을 넘기는지 먼저 확인한 뒤에 코드에 넣었고, 실제 렌더된 화면에서 `getComputedStyle` 로 한 번
더 재서 이론값과 같은지 확인했다.

**전환 순간 화면이 번쩍이지 않게** — 저장된 선택이 있으면 `<style>` 이 파싱되기 전에 아주 작은
인라인 스크립트가 먼저 `data-theme` 속성을 붙인다. CSS 가 처음 칠할 때부터 맞는 색으로 칠하므로,
밝았다가 어두워지는 깜빡임(FOUC)이 없다.

### 4.25 인식 엔진을 tesseract → PaddleOCR 로 바꿨다

**전에는**: tesseract.js + 손으로 짠 이진화 전처리(§4.8, §11.2)를 썼다. 배경마다 다른 임계값이
필요해서 auto/adapt/bright/raw 네 가지를 순서대로 시도하고 가장 많이 읽힌 것을 채택했다 —
그래도 매진 회차가 회색이 아닌 경우처럼 글자·배경 관계가 이질적인 화면은 계속 놓쳤다.

**결정**: **PaddleOCR (PP-OCRv6 tiny, ONNX Runtime Web)** 로 바꿨다. 이 엔진은 학습된
검출기가 색·배경과 무관하게 글자 영역을 먼저 찾아낸 다음 그 영역만 읽으므로, 네 가지
이진화 전처리가 통째로 필요 없어졌다 — 크기만 맞추고 색은 그대로 넘긴다.

**모델 크기가 클수록 좋지 않았다.** 여러 등급을 실측 비교했다:

| 모델 | 크기 | 정확도(카톡 캡처 9~10회차) | 소요 |
|---|---|---|---|
| PP-OCRv5 server | 172MB | 4/9 | 9.7초 |
| **PP-OCRv6 tiny (채택)** | **6.1MB** | **9~10/9~10** | **~0.4~0.6초** |

server 모델은 문서 스캔처럼 정갈한 이미지에 맞춰져 있어 오히려 카톡 캡처류에는 약했다.
tiny 가 크기·속도·정확도 세 축 전부에서 이겼다.

**실제 두 픽스처로 tesseract 와 나란히 검증**(`test/fixtures/`):

| 픽스처 | tesseract(전) | PaddleOCR(후) |
|---|---|---|
| `booking-sample.png` | 9개 정확 | 9개 정확 (동일) |
| `KakaoTalk_..png` (실제 카톡 캡처) | **9개** (15:50 놓침) | **10개** — 놓쳤던 회차까지 잡았다 |

**서버·CDN 없이 오프라인으로 돌아가는 원칙(§3)은 그대로 지켰다.** `onnxruntime-web`,
`ppu-paddle-ocr`, `ppu-ocv`(캔버스 전용 빌드 — OpenCV.js 는 안 끌어온다) 를 전부
`vendor/` 에 넣고, `<script type="importmap">` 으로 맨몸 지정자(`onnxruntime-web` 등)를
그 경로에 매핑했다. 번들러 없이 index.html 을 그대로 열어도 동적 `import()` 만으로 붙는다 —
패키지 안의 상대경로 import 는 vendor 아래 같은 폴더 구조를 그대로 옮겨 놨으므로 손대지 않아도
풀린다. GitHub Pages 는 COOP/COEP 헤더를 못 붙이므로 WASM 은 처음부터 단일 스레드로 고정했다
(`numThreads=1`) — 별도의 서비스워커나 헤더 우회 없이 그냥 동작한다.

**용량은 조금 늘었다.** tesseract 벤더(13MB, 3개 wasm 변형 포함)를 지우고 onnxruntime-web
WASM(11MB) + PaddleOCR 모델(6.1MB) + 패키지 JS(150KB) 로 바꿔 순증 약 5MB(13MB → 18MB).

**tesseract 관련 코드는 검증 후 완전히 지웠다** — 전처리 함수(`preprocess`, `adaptiveInk`),
워커 로딩(`getOcrWorker`, `loadScript`, `OCR_STEP`) 전부. 절반만 지우고 죽은 코드를 남겨
두면 다음에 이 파일을 읽는 사람(나 포함)이 어느 쪽이 실제로 도는지 헷갈린다.

### 4.26 `localStorage` 재방문 복구를 §4.16 그대로 구현했다

**결정**: §4.16 이 정한 대로 — 링크가 진짜 보관, `localStorage` 는 "매번 링크를 누르지
않아도 되는" 편의. 공유 링크(`#s=`)가 없을 때만 마지막 자동 저장 상태를 대신 복구한다.

**형식을 새로 만들지 않았다.** 공유 링크와 똑같이 `serialize()`/`restore()` 를 그대로
쓴다 — 테마·매장·이동시간·조건·정렬 기준까지 이미 링크에 담던 것과 동일하다. 저장 위치만
URL 대신 `localStorage`(`resched.laststate` 키)로 바뀐 것뿐이라, 포맷을 두 벌 관리할
필요가 없고 링크 버전이 바뀌면 자동 저장도 같이 따라간다.

**언제 저장하는가**: 카드 추가·삭제·잠금·순서 변경·이미지 인식 완료·정렬 기준 변경처럼
`renderThemes()`/정렬 클릭을 타는 조작은 거기서 걸어 둔다. 반면 테마 이름·매장·시간표
입력칸은 타이핑 중 리렌더를 피하려고 `renderThemes()` 를 안 거치고 값만 바로 바꾸는
구조라(입력 중 커서가 날아가면 안 되니까) 따로 잡을 곳이 없다 — 그래서 `document` 에
`input`/`change` 를 위임으로 걸어 모든 입력칸(조건 칸 포함)을 한 번에 잡았다.
400ms 디바운스로 묶어 타이핑마다 저장이 튀지 않게 한다.

**깨진 값은 조용히 무시한다.** 형식이 안 맞거나 파싱이 실패하면 공유 링크 복원 실패와
똑같이 콘솔에 경고만 남기고 빈 화면(§4.22)으로 시작한다 — 사용자에게 에러를 보여주지
않는다, 어차피 "편의" 이지 실패해도 잃는 게 없다.

### 4.27 정렬 칩에 개수를 붙였다가, 도로 빼고 "더 보기" 로 바꿨다

**1차 시도(폐기)**: `공백 적은 순` 같은 정렬 칩 라벨 오른쪽에 지금 보고 있는 결과
개수를 붙였다. 그런데 **정렬은 필터가 아니라 같은 목록의 순서만 바꾸는 것**이라
다섯 칩이 항상 같은 숫자를 가진다 — "왜 467개 계산했는데 화면엔 12개만 나오냐" 는
질문을 받고서야, 정작 사용자가 궁금했던 건 "정렬별로 몇 개냐" 가 아니라 "왜 상위
12개만 보이냐" 였다는 걸 알았다. 다섯 칩에 같은 숫자를 반복해서 박아 두는 건 그
질문에 대한 답이 아니었다.

**결정**: 칩에서 숫자를 걷어내고, **"결과" 제목 바로 옆(`#resCount`)에 "왜 이만큼만
보이는지" 를 문장으로 적는다** — `상위 12개만 표시됩니다 · 전체 467개 중`. 처음엔
이 문장을 목록 맨 아래 더 보기 버튼 위에 뒀는데, 스크롤을 다 내려야 보였다 —
"화면엔 12개만 뜨던데" 라는 질문 자체가 목록 맨 위에서 나온 것이므로 답도 그
자리에 있어야 했다. 목록 아래에는 **더 보기** 버튼만 남긴다(같은 문장을 두 번 안
적는다). 누르면 12개씩 더 열리고, 방금까지 보이던 마지막 카드 바로 다음 것으로
화면이 스르륵 넘어간다(`scrollIntoView`) — 새로 열린 줄 어딘지 몰라 눈으로 찾게
하지 않는다.

**한 번에 다 그리지 않는 이유는 그대로다.** 조합이 수천 개(§4 실측: 테마 5개에서
10,664개)까지 나올 수 있어서, 처음부터 다 그리면 스크롤이 끝없이 길어지고 렌더도
무거워진다. `showCount`(지금까지 연 개수)를 12부터 시작해 탭·정렬·재계산처럼 목록
자체가 바뀌는 지점마다 다시 12로 되돌린다 — 안 그러면 "여유 있는 순" 으로 24개를
펼쳐 놓고 "빨리 끝나는 순" 으로 바꿨을 때도 24개가 나와, 새 1위를 보려고 스크롤을
더 내려야 하는 이상한 경험이 된다.

**§6 열린 질문 하나를 닫았다.** "상위 12개 노출이 적정한가, 6개로 줄이고 더 보기를
둘까" 라고 미결로 남아 있던 항목 — 답은 "12개는 유지하되 더 보기로 계속 열 수 있게"
로 정했다.

**곁다리로 다크 모드 버그 하나를 고쳤다.** 이번 작업 중 선택된 탭의 카운트 글자색
(`.tab.on b`)이 `rgba(255,255,255,.62)` 로 박혀 있는 걸 발견했다 — 라이트 모드에서
어두운 알약 위 흰 글자를 흐리게 하려던 값인데, 다크 모드에서는 알약(`--strong`)이
밝은색으로 뒤집히면서 그 위에 거의 안 보이는 흰 글자가 됐다. `color:var(--strong-ink);
opacity:.62` 로 바꿔 테마가 뒤집힐 때 같이 뒤집히게 했다 — 다크 모드 작업(§4.24) 때
결과 카드까지는 실측했지만 탭 카운트 숫자는 당시 테마가 비어 있어 실제로 렌더된 적이
없어 놓쳤던 자리다. 이 버그 수정은 정렬 칩 숫자를 도로 뺀 뒤에도 그대로 남긴다 —
탭 카운트는 여전히 쓰고 있으니까.

**정렬 칩을 되돌리면서 진짜 버그 하나를 냈다가 바로 잡았다.** 숫자를 붙일 때
`sortKey=s.k;renderSorts();renderResults();` 였던 클릭 핸들러를, 되돌리는 김에
`showCount=SHOW;` 를 끼워 넣으면서 실수로 `renderSorts()` 호출을 **지워버렸다** —
`sortKey` 값 자체는 바뀌지만 버튼의 `on` 클래스를 다시 그리는 함수를 안 불렀으니,
칩을 눌러도 결과 순서는 바뀌는데 어느 칩이 선택됐는지 화면엔 계속 예전 것이 켜진
채로 남았다. "여유 있는 순 흰색이 안 바뀐다" 는 지적으로 바로 잡았다 — `renderSorts()`
를 다시 넣어 `sortKey=s.k;showCount=SHOW;renderSorts();renderResults();` 로 확정.

### 4.28 모바일 점검 — 헤더 버튼 줄바꿈 버그

**"모바일도 고려가 잘 되어있나" 라는 질문에 실제로 좁은 화면에서 렌더해서 확인했다.**
390px 폭에서 `.hrow`(사용법 보기·다크모드 버튼 + 로그인 영역)가 줄바꿈 없이 한 줄을
고집하다 보니, 정작 버튼 안의 글자(`라이트 모드`)가 단어 중간에서 잘려 두 줄이 됐다
(`라이트 모` / `드`).

**결정**: `.hrow` 에 `flex-wrap:wrap` 을 줘서 공간이 부족하면 줄 자체를 내리고,
버튼에는 `white-space:nowrap` 을 줘서 글자가 단어 중간에 잘리는 일이 다시는 없게
한다 — 버튼 라벨은 줄이 바뀌더라도 통째로 다음 줄로 넘어가야지, 글자가 쪼개지면
안 된다는 원칙.

**같이 확인했지만 이번엔 손대지 않은 것들**: 테마 카드의 × 삭제 버튼이 좁은
화면에서 자기 혼자 다음 줄로 내려가 옆에 빈 공간이 생기는 것(기능은 멀쩡, 자리만
어색함), 결과 카드 안 타임라인이 가로로 넘칠 때 커스텀 스크롤바가 iOS 사파리에서는
안 보이는 것(실제로 가로 스크롤은 되지만 처음 쓰는 사람은 손대 보기 전엔 모를 수
있음), 복사·저장 버튼이 24px 높이로 터치하기엔 다소 빡빡한 것 — 전부 기능이
망가진 건 아니라서, 지적된 것부터 먼저 고치고 이건 필요하면 다음에 손본다.

### 4.29 순차 배정 (F-14) — 팀을 나눠 원정 갈 때

**계기**: "방탈출 동호회에서 타지역 원정을 가면, 2~3개 팀이 하루에 A·B·C 매장을
순회하면서 서로 안 겹치게 일정을 짜는 경우가 있다" 는 질문. 두 가지 방안을
검토했다 — (A) 팀1 조합을 확정하면 그 회차를 팀2 계산에서 자동으로 빼는
**순차 배정**, (B) 전체 팀을 한 번에 고려하는 **동시 최적 배분**. B는 탐색 공간이
팀 수만큼 곱으로 커져 DFS 엔진을 크게 뜯어고쳐야 한다. 기존 계산 엔진을 그대로
재사용할 수 있는 A로 결정했다 — 먼저 고른 팀이 유리해지는 순서 의존성은 있지만,
"편하게 하려면" 이라는 요구에는 A가 낫다.

**"자주 쓰는 기능이 아니니 UI/UX를 잘 빼둬야 한다"** 는 요구가 명시적으로
있었다. 그래서 §4.24 식사 시간 확보와 완전히 같은 접힘 패턴을 그대로 재사용했다
— 체크박스 하나(`여러 팀으로 나눠 배정`)를 켜야만 패널이 펼쳐지고, 결과 카드의
`팀 확정` 버튼도 이 체크가 켜져 있을 때만 나타난다. 평소 1인 사용자 화면은
글자 한 줄(체크박스) 빼고는 전혀 달라지지 않는다.

**동작**: 결과에서 `팀 확정` 을 누르면 그 조합의 회차들이 "확정된 팀" 목록에
쌓이고, 곧바로 다음 팀의 후보를 다시 계산한다(같은 `runSearch()` 경로를 타므로
`showCount`/`tabCount` 초기화가 §4.27 때처럼 빠질 일이 없다). 팀 수만큼 이어서
누르면 된다. `마지막 팀 취소`(하나만 되돌림)와 `전체 초기화` 를 따로 뒀다.

**제외는 테마 id + 회차 시각으로 키를 잡는다** (`search()`의 `o.taken`).
테마 이름으로 잡으면 이름이 같은 테마가 둘 있을 때 잘못 걸리고, 배열 인덱스로
잡으면 테마를 지우거나 순서를 바꾸는 순간 팀마다 가리키는 게 어긋난다. id는
`blank()`가 매기는, 카드가 살아있는 한 안 바뀌는 값이다. 매장·시간이 바뀌어도
같은 회차 슬롯을 계속 가리킨다.

**버그 하나를 실제로 냈다가 헤드리스 브라우저 검증 중에 잡았다**: `테스트 데이터
넣기` 버튼은 `themes` 배열을 통째로 새 `blank()` 객체로 갈아치운다 — 이 순간
옛 테마의 id가 사라진다. 확정해 둔 팀은 그 id로 회차를 가리키고 있었으므로,
갈아치운 뒤에는 (a) 새로 매겨진 id가 우연히 옛 id와 겹치면 엉뚱한 회차가
빠지거나, (b) 안 겹치면 조용히 아무 효과도 없어진다 — 둘 다 화면에 에러가 안
뜨고, 실제로 "팀2가 팀1과 똑같은 회차로 나온다" 는 증상으로만 드러났다. 재현
시나리오를 CDP로 직접 돌려서 잡았다: 팀을 하나 확정한 상태에서 재시딩하면
`teams` 가 리셋되지 않아 다음 확정이 "팀 2" 로 잘못 매겨지고, 회차도 팀1과
동일했다. 고침: `테스트 데이터 넣기` 핸들러에서 `teams=[]` 를 같이 실행한다 —
테마를 통째로 갈아치우는 건 그 자체로 "새로 시작" 이므로, 남아 있던 팀 배정도
같이 비우는 게 맞다.

**팀 상태는 자동저장(§4.26)에도 실린다.** `serialize()`의 `tm` 필드에 팀
목록을, `o` 배열 끝에 팀 모드 체크 여부를 추가했다 — `restore()`의 "알 수 없는
버전" throw(§4.26이 문 것)를 다시 건드리지 않으려고 `v` 는 그대로 3으로 뒀다.
옛 링크·옛 저장값은 이 필드가 없으므로 `restore()`에서 빈 배열/꺼짐으로
떨어진다.

**공유 링크에도 그대로 실린다** — 원정 갈 때는 결과를 다른 사람에게도 보여줘야
하므로, 자동저장뿐 아니라 `링크 복사` 로도 팀 배정 상태가 넘어가는 편이 낫다고
판단했다.

**범위를 일부러 좁혔다**: 팀마다 다른 테마 목록을 고르는 기능, 여러 팀을 한
화면에 동시에 최적화하는 기능(방안 B)은 만들지 않았다. "A·B·C 를 여러 팀이
나눠 돈다" 는 요청을 그대로 읽으면 테마 목록은 팀들이 공유하고 시간만 갈리는
것이므로, 이 이상은 요청받기 전엔 손대지 않는다.

**결과가 0개일 때의 안내문도 팀 모드에서는 갈렸다.** 평소 문구("최소 공백을
줄이거나 시간 범위를 넓혀보세요")는 팀 모드에서 앞선 팀들이 회차를 다 가져가
남은 회차만으로는 조합이 안 나오는 경우엔 오히려 엉뚱한 방향을 가리킨다. 팀
모드 + 확정된 팀이 있을 때는 원인("앞선 팀이 너무 많은 회차를 가져갔을 수
있다")과 대응(`마지막 팀 취소`/`전체 초기화`)을 짚어 주는 문구로 갈랐다. 3팀
시나리오를 CDP로 재현해 세 팀의 회차 집합이 서로 겹치지 않는지, 그리고 실제로
회차가 바닥나면 이 문구가 뜨는지 둘 다 확인했다.

**`팀 확정`을 누르면 카드 목록이 다음 팀 후보로 바뀐다** — 방금 확정한 조합의
`복사` 버튼도 같이 사라진다. 단톡방에 보낼 일정은 확정하기 전에 먼저 복사해
두라고 `teamhint` 문구에 명시했다. (내용은 팀 패널의 칩에도 남아 있고
`마지막 팀 취소`로 카드를 되돌릴 수 있어 데이터가 사라지진 않지만, 되돌리는
수고를 없애는 편이 나아서 문구로 먼저 안내한다.)

### 4.30 세 번째 입력 경로 — 예약 사이트에서 직접 불러온다 (§4.10·§4.13 번복)

**결정**: 입력 경로에 **불러오기**를 더한다. 매장·날짜를 고르면 회차 시간과 **매진 여부**가 한 번에 들어온다.
직접 입력과 사진(§4.8)은 그대로 둔다.

**왜 지금 번복하나**: §4.10 과 §4.13 은 둘 다 **같은 하나의 이유**로 접은 결정이었다.

> §4.10 — 관리의 핵심인 "신테마가 나왔는지" 는 이 구조에서 알 방법이 없다 —
> **예약 사이트를 주기적으로 긁어야 하고 그건 서버가 필요하다.**

> §4.13 — 체크박스가 성립하려면 매진 여부가 데이터에 들어와 있어야 하는데,
> 두 입력 경로 어느 쪽에서도 안 들어온다.

**그 서버가 생겼다.** 별도 프로젝트(로컬 `~/IdeaProjects/Server`)가 예약 페이지를 주기적으로
조회해 테마·회차·예약 가능 여부를 보관한다. 전제가 사라졌으므로 두 결정을 번복한다.
결정이 틀렸던 것이 아니라 **조건이 바뀐 것이다.**

**§4.15 의 순서와도 맞는다**: `①정적 배포 → ②localStorage → ③DB` 로 정해 두었고 ①② 는 완료다.
③ 이 "값을 하는 지점" 을 §4.15 는 *사람이 모여 데이터를 채워 줄 때* 로 봤는데,
크롤러는 사람 없이 그 일을 한다. 상상했던 것보다 이른 형태로 조건이 충족됐다.

**가장 큰 소득은 회차가 아니라 매진이다.** `session.soldout` 은 모델에 이미 있고 화면도 이미 그린다.
§4.13 에서 *채울 방법이 없어* 비워 둔 필드였다. 이게 채워지면 §4.13 이 경고한
**"예약할 수 없는 조합을 1위로 믿게 만든다"** 가 구조적으로 사라진다.
`매진 회차 제외` 옵션을 되살리는 것은 아니다. 다만 **§4.13 의 "조용히" 는 바꾼다** —
계산에서는 그대로 빼되 **타임라인에는 회색으로 남긴다.**

계산에서 빼는 것과 화면에서 지우는 것은 다르다. 남겨야 사용자가 *"왜 이 시간대가 후보에 없지"* 를
조건 탓인지 매진 탓인지 구분할 수 있고, 무엇보다 **나중에 "이 자리 감시" 를 붙일 자리가 된다.**
파싱 단계에서 버리면 붙일 데가 없다. 사양은 작업명세서 §4.4.

**서버는 선택이지 전제가 아니다.** §4.18 에서 계정에 적용한 규칙을 그대로 따른다 —
서버가 없거나 멎어도 직접 입력과 사진은 그대로 동작하고, 불러오기를 누르기 전까지
관련 코드는 내려오지 않는다. 정적 배포(§4.15)와 "빌드 없음, 의존성 0" 은 유지된다.

**소요시간은 서버 값(홈페이지에 적힌 시간)을 그대로 쓴다.** 실측하면 회차 간격(75~80분)이
광고 시간(60~65분)보다 길지만, 그 여유는 매장 간 이동시간(§4.7)과 최소 공백 경고가 이미 흡수한다.
따라서 `dur` 에 별도 보정을 넣지 않는다. 다만 수동 입력 기본값 70 보다 짧은 값이 들어오므로
서버에서 불러온 조합은 지금보다 다소 빡빡하게 나온다.

**범위를 눈에 보이게 한다**: 서버가 아는 매장은 전부가 아니다
(2026-08-31 기준 **4브랜드 19지점** — 플레이33·키이스케이프·토끼굴·지구별. 계속 는다).
불러오기를 범용 기능처럼 두면 자기 매장을 찾다 못 찾고 고장으로 읽는다 —
§4.10 이 경계한 *"낡은 데이터는 없는 것보다 나쁘다"* 가 이번엔 **비어 있음**의 형태로 온다.
그래서 진입점 자체에 지원 매장을 적는다. 범용 `가져오기` 버튼으로 만들지 않는다.

> 매장이 계속 늘기 때문에 **버튼 문구에 브랜드명을 박지 않는다.** 지점 목록은 서버가 준다
> (`GET /api/branches`). 문구를 박으면 매장이 늘 때마다 이쪽도 고쳐야 하고, 안 고치면 거짓말이 된다.

**다음 단계 (F-16 · 아직 안 함)**: 짜여진 조합 안에 매진 회차가 있으면 그 자리를 서버에
감시 등록하고, 취소표가 나오면 알린다. 두 프로젝트를 합쳐야만 나오는 기능이다.
**서버 쪽은 2026-08-28 에 발송까지 끝났다** — 남은 건 이쪽에서 알림 받을 주소를
만들어 올리는 것과 감시 버튼이다 (작업명세서 §4.5).

### 4.31 React + TypeScript 이전 — 1단계 (스캐폴드 + 핵심 로직/데이터 계층)

**계기**: Revolt(자동차 마켓 앱) 스타일 참고 링크를 보여주며 비슷한 룩을 Floduler에
적용하고 싶다는 요청. "전체 리디자인"에 동의했고, "TS나 리액트 써도 돼, 아예
바꿔도 돼" 라고 스택 전환까지 허용받았다.

**결정**: "디자인 교체" 와 "스택 전환" 을 **동시에 하지 않는다**. 둘 다 하면
뭐가 깨져도 포팅 버그인지 의도된 디자인 변경인지 구분할 수 없다 — advisor
자문 결과. 그래서 **스택 전환을 먼저, 시각적으로 100% 동일하게** 끝내고
(패리티를 기존 테스트로 증명), 리디자인은 실제 디자인 앵커(색상·타이포·간격
구체값 또는 참고 스크린샷)를 받은 뒤 별도 단계로 미룬다. 계획 전문은
사용자 승인을 거쳐 세션 로컬에 저장했다.

**스파이크 두 개를 먼저 돌려 계획을 뒤집을 수 있는 리스크를 먼저 걷어냈다**:

- **PaddleOCR가 Vite 번들 하에서 되는가** — `npm create vite`로 스캐폴드(`app/`)를
  만들고 `onnxruntime-web`·`ppu-paddle-ocr`·`ppu-ocv`를 npm 패키지로 설치해
  돌려봤다(전부 실제 npm에 있었다 — vendor/는 이 패키지들을 손으로 복사해 둔
  것이었다). 실측으로 두 가지를 발견·해결했다:
  1. vendor/에 있던 `onnxruntime-web` 파일 3개(비-jsep, WASM-only)로는 최신
     패키지가 못 뜬다 — `.jsep.mjs`/`.jsep.wasm` 조합이 추가로 필요했다(23MB,
     비-jsep의 2배).
  2. `npm run build`가 이 23MB 파일을 **두 벌**(우리가 `public/vendor/`로 둔
     패스스루 사본 + Vite가 패키지 내부 참조를 정적 분석해 자동으로 번들에
     넣은 사본)로 만들었다. `vite.config.ts`에서 `onnxruntime-web`을
     `optimizeDeps.exclude` + `build.rollupOptions.external` 로 완전히
     빼고, `index.html`에 그 한 패키지만을 위한 importmap(`onnxruntime-web`
     → `/vendor/onnxruntime-web/ort.wasm.min.mjs`)을 넣어 해결 — 지금 원본
     index.html이 쓰는 것과 같은 메커니즘을 Vite `index.html` 템플릿 안에
     그대로 옮긴 것이다. `npm run dev`와 `npm run build && npm run preview`
     양쪽에서 같은 테스트 이미지를 돌려 프로덕션 결과와 **글자 하나까지
     동일**함을 확인했다.
- **"로그인 전 0바이트"(§4.18)가 Vite 코드 스플리팅에서도 유지되는가** — 더미
  지연 로드 모듈을 만들어 네트워크 요청을 직접 찍어봤다. 첫 로드 때 요청
  0건, 로그인 관련 함수를 실제로 호출한 뒤에야 청크 요청 1건 — 그대로
  유지됨을 확인.

**데이터 호환성을 먼저 고정했다**: `test/fixtures/legacy-links.json` 에 v1(최초
공유 링크, `pin` 문자열)·v2(`lock` 0/1)·v3early(F-13 도입 직후, `place`+`moveMin`
추가)·v3current(오늘 배포된 코드가 실제로 `serialize()`해서 만든 값) 4개를
실제 코드로 만들거나 헤드리스 브라우저로 현재 `restore()`에 왕복 검증해 박아
뒀다. 새 상태 계층은 이 픽스처들과 **정확히 같은 결과**를 내야 통과한다.

**이 과정에서 실제 버그를 하나 발견·수정했다** (index.html도 같이 고쳤다,
포팅이 아니라 정정이라서): 테마의 `id`는 지금까지 공유 링크·저장된 계획에
**실려 있지 않았다** — `restore()`가 매번 `blank()`로 새 id를 매겼다. 페이지를
갓 새로고침한 직후라면 id 카운터가 1부터 다시 시작해 우연히 맞아떨어지지만,
**이미 카드를 만진 뒤(카운터가 진행된 뒤) "불러오기"로 팀이 확정된 저장
일정을 열면** 새로 매겨진 id가 그 계획에 저장된 `teams[].steps[].id` 와
어긋나 F-14(순차 배정)의 회차 제외가 조용히 안 먹는다 — 실측으로 재현했다
(팀2 계산에 팀1과 완전히 같은 조합이 다시 나옴). `t` 튜플에 6번째 자리로
`id`를 추가하고, `restore()`가 있으면 그대로 쓰고 없으면(옛 링크) 새로
매기도록 고쳤다. 다음 카드가 복원된 id와 겹치지 않도록 `nextId`도 같이
당겨 올린다. 새 TS 포팅에서는 이 계약을 `restore(hash, startId)` 형태의
순수 함수로 못박아 뒀다 — `startId`는 "지금 세션에서 이미 쓰인 가장 큰
id + 1"을 호출부가 넘긴다.

**옮긴 것**: `@core` 구간(709~889행) 전체를 `app/src/core.ts`로 타입만
붙여 그대로 이식(`parseSessions`의 AM/PM·인접중복·12시간보정 규칙,
`search()`의 순서고정·이동시간·식사공백·F-14 제외 규칙 모두 재작성
없이 옮김). `serialize()`/`restore()`는 DOM 의존을 없애고 상태 객체를
받고 돌려주는 순수 함수(`app/src/serialize.ts`)로 다시 짰지만, 밖으로
나가는 JSON 모양(`v:3`, 위치 기반 튜플, 죽은 `lock` 자리)은 완전히
동일하게 유지했다. `test/acceptance.mjs`의 90여 개 검사를 전부
Vitest(`app/src/core.test.ts`)로 옮겨 통과시켰고, `serialize.test.ts`가
legacy-links 픽스처 왕복 + id 버그 회귀 테스트를 추가로 검증한다.
**`index.html`은 이 과정에서 한 줄도 지우지 않았다** — 원본
`test/acceptance.mjs`도 그대로 남아 계속 통과한다. 신구 두 구현이 같은
검사를 각자 통과하는 것 자체가 "이식이 행동을 안 바꿨다"는 증명이다.

**다음 계획 범위 밖**(디자인 앵커를 받거나, 컴포넌트 이식을 시작할 때
별도로 계획): React 컴포넌트 트리 구축(테마 카드·조건 패널·결과 카드·팀
패널·로드/저장 모달·계정 위젯·투어)과 기능별 패리티 확인, GitHub Actions
배포 전환(현재는 "브랜치에서 배포" — Actions 워크플로 없음, Pages Source
설정 변경은 사용자 확인 필요), Revolt 스타일 리디자인 자체.

### 4.32 React + TypeScript 이전 — 2단계 (핵심 계산기 화면)

§4.31 바로 다음 단계. 화면 전체가 아니라 **핵심 계산기 플로우** — 테마 카드
(수동/붙여넣기 입력만) → 조건 패널 → 결과 패널(정렬 5종·탭·페이지네이션) →
팀 배정(F-14) → 공유 링크·자동저장 — 만 옮겼다. Firebase 계정, F-15 서버
불러오기, 이미지 인식(OCR), 사용법 투어, 다크모드 수동 토글은 이번에도
범위 밖(버튼은 그리되 비활성화 + "다음 단계에서 연결됩니다" 안내만).

**원칙은 1단계와 동일하게 유지**: index.html의 CSS(31~543행)를 그대로
`app/src/floduler.css`로 옮기고, 같은 클래스명으로 JSX를 짜서 시각적으로
100% 동일하게 만들었다 — 이번에도 "디자인 교체"는 하지 않았다.

**컴포넌트 구조**: `useScheduler()` 커스텀 훅 하나에 핵심 상태(`themes`,
`found`, `sortKey`, `tabCount`/`tabSet`, `showCount`, `teams`, `moveMap`)를
몰아넣고, `ThemeCard`/`ThemeList`/`ConditionsPanel`/`MoveTimeGrid`/
`ResultsPanel`/`ResultCard`/`TeamPanel`이 props로 받아 쓴다. 새 상태
라이브러리(Redux/Zustand)는 추가하지 않았다.

**포팅하며 실제로 걸린 것들**:
- **드래그 재정렬 상태**를 index.html처럼 DOM 클래스 조작(`classList.add`)이
  아니라 컴포넌트 로컬 `useState`(`dragState`)로 표현했다 — React 관용구에
  맞추되 `dragging`/`over`/`drag` 클래스명 자체는 그대로 유지해 CSS를 안
  건드렸다.
- **"팀 확정" 직후 다음 팀 후보를 곧바로 다시 계산하는 부분**에서 클로저
  문제를 만났다: `setTeams(next)` 직후 같은 함수 안에서 `runSearch()`를
  부르면 `runSearch`가 참조하는 `teams`는 아직 리액트가 반영하기 전의 옛
  값이다. `runSearch(teamsOverride?)`로 바꿔 팀 액션이 방금 계산한 새
  `teams` 배열을 직접 넘기게 해서 해결 — index.html의 `confirmTeam`이
  `runSearch`를 직접 불러 `tabCount`/`showCount` 리셋이 안 빠지게 했던 것과
  같은 목적을, effect 감시가 아니라 명시적 인자 전달로 달성했다(effect로
  하면 `restore()` 직후에도 팀이 바뀌었다고 오인해 원본에 없던 자동 재계산이
  실행되는 문제가 있었다 — 실제로 겪고 되돌렸다).
- **결과 카드 타임라인의 사후 DOM 보정**(이름 넘침 시 타임라인 폭 늘리기,
  그래도 좁으면 "13:30–14:45"를 "13:30"으로 자르고 그래도 안 되면
  생략)은 `useLayoutEffect` + `querySelectorAll`로 그대로 옮겼는데, 여기서
  React 특유의 함정을 하나 만났다 — React는 "이전에 자신이 렌더한 값과
  같으면" 텍스트 노드를 안 건드리는데, 이 효과가 `textContent`를 React
  모르게 직접 잘라내므로 둘의 상태가 어긋난다. `data-full` 속성에 잘리지
  않은 원본 텍스트를 항상 같이 심어 두고, 매 패스 시작 시 거기서부터
  다시 계산하도록 해서 index.html처럼 "매번 처음부터 새로 그리고 필요할
  때만 자른다"를 유지했다.
- **체크박스 시뮬레이션**: 검증 스크립트에서 `.checked`를 직접 세팅하고
  `change` 이벤트를 dispatch하는 방식은 React의 체크박스 처리가 내부적으로
  `click` 이벤트를 보므로 안 먹혔다 — 실제 앱 버그가 아니라 검증 방법
  문제였음을 네이티브 `.click()`으로 바꿔서 확인.

**검증**: 같은 3테마 입력값(범계/왓 어 트립/몽, requirements.md §7)을
index.html과 새 앱 양쪽에 헤드리스 브라우저로 넣어 대조 — 결과 개수(62개),
1위 조합 시퀀스, "더 보기" 페이지네이션 개수까지 **글자 하나 다르지
않게** 일치했다. 이어서 정렬 5종 전환, 팀 확정/취소, 드래그 재정렬(합성
DragEvent로 카드 0을 2번 자리로 옮겨 배열이 정확히 바뀌는지), 공유 링크
왕복(복사→해시 디코드→새 탭에서 열어 이름·소요시간 복원), 자동저장 후
새로고침 복원(팀 배정 포함)까지 전 구간에서 콘솔 예외·경고 0건. 모바일
390px 폭에서 가로 스크롤 없음, 다크 모드(`prefers-color-scheme`) 자동
전환 확인.

**다음 계획 범위 밖**: 위에서 비활성화해 둔 것들(이미지 인식, Firebase
계정, F-15 서버 불러오기, 사용법 투어, 다크모드 토글) 연결, GitHub Actions
배포 전환, Revolt 리디자인.

### 4.33 CI/CD 파이프라인을 만들었다 — 아직 전환은 안 했다

**계기**: "배포된 사이트 주소로 자동으로 반영되게 하려면?" 이라는 질문에
GitHub Actions 얘기가 나왔고, "그럼 테스트도 자동으로 돌게 하자"로 이어져
CI(빌드·테스트 자동 확인)와 CD(그걸 통과해야 배포)를 한 워크플로로 묶기로
했다.

**만든 것**: `.github/workflows/deploy.yml`. `main` 에 푸시될 때마다 —

1. `test` 잡: 루트 인수 테스트(`npm test`, index.html의 `@core`) +
   `app/` Vitest(`npm test`, 82건) 둘 다 통과해야 다음 단계로 간다.
2. `build` 잡: `app/` 를 `npm run build` 로 빌드.
3. `deploy` 잡: 빌드 결과물을 GitHub Pages 공식 액션(`actions/deploy-pages`)
   으로 올린다.

테스트가 깨지면 `build`/`deploy` 잡 자체가 안 돈다 — 깨진 코드가 실서비스에
올라갈 수 없는 구조. `app/public/.nojekyll` 도 미리 넣어 뒀다(Actions 배포
경로는 원래 Jekyll을 안 타지만, `vendor/` 가 통째로 빠졌던 §4.15/README의
전례가 있어 확인차 남겨 둠). 스파이크 검증용으로 남아 있던
`app/public/test-fixture.png` 는 프로덕션 빌드에 실릴 이유가 없어 지웠다.

**아직 전환하지 않은 이유 — 중요**: GitHub Pages의 "Source" 를 지금의
"브랜치에서 배포"(=지금 배포된 것은 `index.html`)에서 "GitHub Actions"
(=이 워크플로가 배포한 `app/` 빌드)로 바꾸는 순간, **실서비스가 새 앱으로
전환된다.** 그런데 지금 `app/` 는 §4.31~4.32에서 의도적으로 범위 밖에 둔
기능들 — **이미지 인식(OCR), Firebase 로그인·저장된 일정, F-15 서버
불러오기, 사용법 투어** — 이 전부 빠져 있다. 핵심 계산기 화면은
index.html과 100% 동일하지만, **그 외 기능은 지금 전환하면 일시적으로
사라진다.** 이건 "눈에 안 보이는 변화" 가 아니라 실제 동호회원이 쓰는
기능의 후퇴이므로, 워크플로 자체는 만들어서 검증(로컬에서 `npm run
build`까지 재확인)해 뒀지만 **Pages Source 전환은 사용자 승인 없이 하지
않는다.** 언제 전환할지는 다음 중 하나를 사용자가 정해야 한다 — (a) 빠진
기능들을 다 이식할 때까지 기다렸다가 한 번에 전환, (b) 지금 당장 "핵심
계산기만 되는 버전"으로 전환하는 걸 감수(빠진 기능은 곧 이식될 예정임을
알고).

**정정 (§4.35 작업 중 발견)**: §4.34에서 5개 기능을 전부 채운 뒤 실제
운영 URL(`choieuihyun.github.io/RoomEscapeScheduler`)에 접속해 보니 흰
화면만 떴다. 확인해 보니 **Pages Source가 이미 "GitHub Actions"로
전환돼 있었다** — 언제·어떻게 바뀌었는지는 이 세션에서 확인할 방법이
없었지만(GitHub API가 인증 없이는 Pages 설정을 안 보여준다), 위 문단이
말하는 "사용자 승인 후 전환" 절차를 거치지 않고도 이미 `app/`이
라이브로 서빙되고 있었다는 뜻이다. 흰 화면의 실제 원인은 전환 여부와는
별개 문제였다 — `vite.config.ts`에 `base` 설정이 없어 `/assets/...`
같은 절대경로가 도메인 루트 기준으로 나갔는데, 실제 서빙 경로는
`/RoomEscapeScheduler/` 서브패스라 전부 404였다(§4.35, 커밋
`16371e0`으로 수정). 이 문서가 이후로도 "전환은 사용자 승인 필요"라고
말하는 부분은 **그 결정 자체가 틀렸다는 뜻이 아니라, 실제로는 그 결정
지점을 이미 지나쳐 있었다는 뜻**으로 읽어야 한다 — 앞으로는 실제
서비스 상태를 코드 안 가정이 아니라 라이브 URL로 먼저 확인한다.

---

## 5. 확장 후보

우선순위 순.

1. **JSON 내보내기·가져오기** — 계정 밖으로 데이터를 꺼내 두는 길
2. **조합 감시 알림** — 조합 속 매진 회차에 취소표가 나면 알림 (§4.30 의 다음 단계, 서버 알림 기능 선행)

완료된 것:
- ~~`localStorage` 재방문 복구~~ — **완료.** §4.26
- ~~공용 테마 시간표 (DB)~~ — **착수.** 사람이 채우는 대신 서버가 긁는 형태로 성립 (§4.30)

완료된 것:
- ~~정적 배포~~ — **완료.** GitHub Pages (작업명세서 §2.2, #33)
- ~~계정 설정 마무리~~ — **완료.** Firebase 연결, Auth·Firestore·규칙 (작업명세서 #36)
- ~~매장 간 이동시간~~ — **완료.** 매장명 + 기본값 + 쌍별 예외 (§4.7, 작업명세서 #41~42)
- ~~테마 성격 가중치~~ — 취소. 순서 제약(§4.12)이 같은 문제를 더 싸게 푼다
- ~~배포용 프록시~~ — 취소. 인식이 브라우저로 들어와(§4.8) 프록시 자체가 없어졌다
- ~~결과 저장·공유~~ — **완료.** 링크 + 텍스트 복사 (작업명세서 §5.3)
- ~~계정별 일정 보관~~ — **완료.** 아이디/비번 + Firestore (§4.18, 작업명세서 F-12)

### 4.34 React 이전 3단계 — 범위 밖으로 미룬 것들, 하나씩 채워 넣기

§4.33에서 "지금 전환하면 잃는 기능"으로 꼽은 다섯 — 다크모드 토글 버튼,
사용법 투어, 이미지 인식(OCR), F-15 회차 불러오기, Firebase 로그인·저장된
일정 — 을 다시 하나씩 이식하기 시작했다. §4.31~4.32와 같은 원칙: 한 번에
다 옮기면 뭐가 깨져도 원인을 못 찾으니, 작은 것부터 하나씩 계획 없이도
바로 검증 가능한 단위로 끊어서 간다. 순서는 복잡도순 — 다크모드 토글 →
사용법 투어 → 이미지 인식(OCR) → F-15 불러오기 → 로그인/저장된 일정.

**다크모드 토글 (완료)**: index.html의 로직(`applyTheme`/`isDarkNow`,
`resched.theme` 키)을 `app/src/useTheme.ts` 훅으로 그대로 이식 — 저장된
선택이 없으면 시스템 설정을 따르고([data-theme] 속성 자체를 안 둠), 버튼을
누르면 그 선택을 `localStorage`에 남겨 시스템 설정보다 우선시킨다. CSS
토큰과 FOUC 방지 가드(`app/index.html`의 인라인 스크립트)는 2단계 때 이미
옮겨져 있어 손댈 게 없었다. 헤드리스 브라우저로 클릭→속성 변경→새로고침
후 유지→재클릭 왕복까지 확인.

**사용법 투어 (완료)**: index.html의 스포트라이트 로직(`TOUR_STEPS`,
`tourMount`/`showTourStep`/`positionTour`)을 `app/src/useTour.ts` 훅으로
이식했다. React 트리 안에 넣기보다 원본처럼 `document.body`에 dim/spot/tip
DOM을 직접 붙이고 떼는 방식을 그대로 가져왔다 — 스크롤·리사이즈에 맞춰
지오메트리를 다시 계산하는 로직이라 명령형 쪽이 원본과 동작이 확실히
같다. 이 과정에서 이식 대상 컴포넌트들에 원본에만 있던 `id`(`#themes`,
`#addTheme`, `#loadBtn`, `#go`, `#shareBtn`)가 2단계 때 빠져 있던 걸
발견 — 투어가 가리킬 요소를 못 찾아 스텝을 전부 건너뛰는 문제였다. CSS는
2단계 때 이미 옮겨져 있어 새로 손댈 게 없었다. 헤드리스 브라우저로 첫
방문 자동 시작(500ms 지연)→다음/이전 이동→건너뛰기(`localStorage` 플래그
저장 후 재방문 시 자동 시작 안 함)→헤더 버튼으로 재실행→마지막 스텝
"확인" 버튼 라벨·종료까지 전부 확인, 콘솔 예외 0건.

**이미지 인식(OCR) (완료)**: index.html의 PaddleOCR 경로(§6.3, §4.25) —
`getPaddleService`/`preparePaddleCanvas`/`ocr()` — 를 세 조각으로 나눠
이식했다. (1) `app/src/ocr.ts` — 엔진 로드·전처리·인식 자체는 `th`를
건드리지 않는 순수 함수 `recognizeSessions(file, onStep)`으로, 실패 원인과
진단 정보(`_dim`)를 실어 보내는 `OcrError`도 함께. (2)
`useScheduler.ts`의 `attachImages(id, files)`/`processImage` — 세션 병합
(같은 시각은 매진 여부만 OR로 합치기), `imgCount`/`source` 전이 규칙
("이어 붙이기"를 껐으면 새 배치 전에 지우기, 이미 손으로 고친 뒤 새
이미지가 오면 `image-edited`→`image`로 되돌아가는 것까지) 전부 원본과
동일하게. (3) `ThemeCard.tsx` — 파일 입력·붙여넣기(Ctrl+V)·카드 위
드래그드롭 세 입력 경로를 전부 연결. 1단계 스파이크(#59)에서 이미 검증한
Vite 번들링(모델·wasm은 `app/public/vendor/`)을 그대로 재사용해 새로
손댈 게 없었다. 헤드리스 브라우저 + `DOM.setFileInputFiles`(파일 선택)·
합성 `ClipboardEvent`(붙여넣기) 두 경로로 실제 픽스처
(`test/fixtures/booking-sample.png`)를 넣어 9/9 회차 인식을 확인, 같은
이미지를 두 번 붙여도 중복 없이 9개로 유지되는 것(병합)과 "이미지 여러장
넣기"를 끄면 새 첨부가 이전 값을 지우고 대신하는 것도 확인. 콘솔에는
onnxruntime의 CPU 벤더 인식 경고 한 줄만 뜨는데(원본에도 있던 무해한
엔진 로그), 그 외 예외 0건.

**F-15 회차 불러오기 (완료)**: index.html의 `openLoad`/`ldFetch`/
`ldFillDates`/`ldAddPicked`(§4.30, 작업명세서 §4.4)를 세 조각으로 이식했다.
(1) `app/src/server.ts` — `server.js`의 `branches()`/`schedule()`/`say()`/
`ago()`를 그대로 옮긴 순수 네트워크 함수. (2) `app/src/useLoadModal.ts` —
지점·날짜·인원 필터·후보 목록·선택 상태를 관리하는 훅. (3)
`app/src/components/LoadModal.tsx` — `#loadModal` 마크업 이식, CSS는
2단계 때 이미 옮겨져 있어 손댈 게 없었다. `useScheduler.ts`에
`addServerThemes`를 추가해 고른 항목을 카드로 밀어 넣는다(매진 회차도
그대로 담아 계산에서만 빠지는 것은 원본과 동일).

**로컬 개발에서 걸린 것**: F-15 서버(`floduler.duckdns.org`)의 CORS
허용 출처가 실제 배포 주소(`choieuihyun.github.io`) 하나뿐이라 —
`vite dev`의 `localhost:*`에서 직접 fetch하면 preflight가 403으로
막힌다(`curl`로 프리플라이트를 직접 쏴서 확인). `vite.config.ts`에
`server.proxy`로 `/api`를 서버로 우회하는 dev 전용 프록시를 추가하고,
`server.ts`의 `base()`가 `import.meta.env.DEV`일 때만 상대경로(`''`)를
쓰도록 갈랐다 — 프로덕션 빌드는 원래대로 절대경로로 직접 부른다(배포되면
같은 GitHub Pages 출처이므로 index.html이 지금 하는 것과 동일하게 통과
한다). `localStorage['flod.server']` 수동 오버라이드는 그대로 유지.

실제 배포된 서버(`22개 지점` 실 데이터)로 헤드리스 브라우저 검증: 모달
열기→지점 목록 로드→지점/날짜 전환 시 재조회→인원 필터로 안 맞는
테마 흐리게 표시→항목 선택→카드로 추가(칩에 "불러옴", 신선도 "N분 전
기준" 노출)→불러온 뒤 손으로 고치면 "불러옴 → 수정함"으로 전이→서버
테마와 수동 테마를 섞어 조합 계산까지 콘솔 예외 0건.

**로그인/저장된 일정 (완료)**: index.html의 `cloud.js`(Firebase Auth·
Firestore, §4.18)와 계정·저장 UI 배선(`renderAcct`/`openAuth`/
`submitAuth`/`openSave`/`submitSave`/`loadPlans`/`renderPlans`)을
이식했다. (1) `app/src/cloud.ts` — `cloud.js` 그대로, 로그인 함수를 실제
부르기 전까지 Firebase SDK를 CDN에서 한 바이트도 안 받는다. (2)
`app/src/useAuth.ts` — 계정 상태 + 로그인/가입 모달 상태. `cloud.ts`
자체도 `useAuth.ts`가 지연 `import()`로 불러온다 — 로그인 버튼을 누르기
전까지 5MB짜리 SDK는 물론 3KB짜리 `cloud.ts` 청크조차 안 내려간다.
`resched.signedin`(예전에 로그인한 적 있는가) 가드도 그대로 옮겨, 처음
온 사람에게는 자동 로그인 복귀 시도조차 안 한다. (3) `app/src/usePlans.ts`
— "내 일정" 목록(불러오기/전환/삭제). (4) `app/src/useSaveModal.ts` —
결과 카드의 "저장" 모달, `snapshot`은 **저장 시점이 아니라 계산 시점의
입력값**(`s.lastSnapshot.current`)을 쓴다 — 작업명세서 §7.1이 명시한
불변식. (5) 컴포넌트 4개(`AcctWidget`/`AuthModal`/`SaveModal`/
`PlanSection`) + `ResultCard`에 "저장" 버튼 추가. `useScheduler.ts`에
`loadSnapshot(snapshot, date)` 추가 — "불러오기"가 옛 결과 카드를
지우는 것(그 "저장" 버튼이 이전 계산을 붙들지 않도록)까지 원본과 동일.

**검증은 두 단계로 나눴다.** 코드만으로 되는 것 — 코드 분할이 "로그인
전 0바이트"를 지키는지(빌드 결과에 `cloud.ts`가 별도 청크로 분리됨을
확인, 헤드리스 브라우저의 Network 로그로 로그인 모달을 여는 것만으로는
Firebase/CDN 요청이 0건임을 실측)와 `restore()`가 실제 v3 링크를
정확히 복원하는지(기존 `serialize.test.ts`의 legacy-links 픽스처
왕복 검증, `loadSnapshot`이 감싸는 바로 그 함수)는 이미 커버돼 있었다.
계정이 필요한 나머지 절반은 사용자 승인을 받아 테스트 계정으로 확인:
가입→계산→저장→새로고침 후 자동 로그인 복귀→내 일정 목록→불러오기
(결과 카드 비워짐, 입력값 복원)→전환→삭제→로그아웃까지 콘솔 예외 0건,
끝나고 Firebase Auth 계정 삭제로 정리. **가장 중요한 확인은 따로 했다**
— "로그인 되나"가 아니라 "index.html이 저장한 일정을 새 앱이 실제로
불러올 수 있나": 루트 `index.html`(별도 포트)에서 같은 테스트 계정으로
로그인해 일정을 저장하고, `app/`에서 그 계정으로 로그인해 "내 일정"에
뜬 그 문서를 "불러오기"로 열어 테마 이름·회차가 글자 하나 안 다르게
복원됨을 실측 확인 — `serialize()`/`restore()`가 두 구현 사이에서도
바이트 호환임을 코드가 아니라 실제 Firestore 문서로 증명했다.

이것으로 §4.33에서 범위 밖으로 미뤘던 다섯 가지가 모두 끝났다 —
다크모드 토글, 사용법 투어, 이미지 인식(OCR), F-15 회차 불러오기,
로그인/저장된 일정. (당시엔 배포 전환 여부가 다음 결정 사항이라고
적었지만, §4.35에서 발견했듯 Pages Source는 이미 Actions로 전환돼
있었다 — 위 §4.33 정정 참고.)

### 4.35 `app/` 리디자인 — Linear + Notion Calendar 톤 스킨

§4.31에서 "디자인 교체와 스택 전환을 동시에 하지 않는다"며 미뤄뒀던
리디자인 차례. Linear와 Notion Calendar를 디자인 앵커로 받았다. 처음엔
헤더를 완전한 톱바(로고+메뉴 나란히, 지금의 큰 제목·설명 문장을 작은
워드마크로 축소)로 바꾸는 안을 제안했으나, 사용자가 "지금 화면들에
그 느낌만 주는 깔끔한 디자인"을 원한 것이었다고 정정 — 논의 끝에
**구조 개편이 아니라 표면 재도색(스킨)**으로 범위를 좁혔다. "포팅과
리디자인을 동시에 하지 않는다"는 이 프로젝트의 원칙과 같은 논리로,
이번엔 "리디자인과 IA 개편을 동시에 하지 않는다"를 지켰다 — 제목/설명
문장은 그대로 두고 팔레트·타이포·그림자→보더 전환만 다시 입혔다.

**결정**: 폰트는 Pretendard 유지. Linear의 Inter 선택은 라틴 문자
지오메트리 최적화가 이유이고, 한국어가 주 언어인 이 제품엔 이미 잘
튜닝된 Pretendard + tabular-nums 처리가 맞다 — "Linear처럼"이 폰트
교체까지 요구하는 게 아니라 색상·밀도·그림자 처리 같은 표면 언어를
가리킨다.

**바뀐 것**: 배경/서피스를 중성 회색조로, 액센트를 인디고(`#5E6AD2`,
다크 `#7B85E8`)로, 카드류의 `border:transparent`+그림자로 뜬 느낌을
얇은 1px 보더로, 라운드를 16/10/8px→12/8/6px로 축소. 다크모드 값이
두 블록(`@media(prefers-color-scheme:dark)`와 `[data-theme="dark"]`)에
중복 정의돼 있어(§4.24) 하나만 고치면 "수동은 맞는데 OS다크는 틀림"이
나는 지점이라 advisor가 미리 짚었고 둘 다 갱신했다. 탭/정렬 칩의 검정
반전 채움을 인디고 소프트 채움으로, 모달을 즉시 전환에서 opacity+scale
트랜지션으로 바꿨다.

**테마별 컬러 코딩** — Notion Calendar가 캘린더마다 색을 주는 것에서
착안해 6색 순환 팔레트(`--accent-0`~`--accent-5`)를 신설, `ThemeCard`
왼쪽 보더 띠(테마 index 기반)와 결과 타임라인 `.blk`(스텝의 테마 index
`s.i` 기반) 양쪽에 `color-mix()`로 적용 — 같은 테마는 카드에서도 결과
타임라인에서도 항상 같은 색으로 보인다. advisor 자문으로 "`.blk`는
`ResultsPanel`의 `useLayoutEffect`가 `scrollWidth`/`clientWidth`로 라벨
말줄임을 계산하므로 padding·border 두께를 조금이라도 바꾸면 그 계측이
깨진다"는 걸 미리 못박고, 색은 background/color만 바꾸도록 범위를
제한했다.

**"캘린더" 메뉴**: 사용자가 요청한 대로, 나중에 실제 캘린더 기능을 붙일
자리만 헤더 오른쪽에 만들었다 — `<button>`도 `onClick`도 `href`도 없는
순수 `<span aria-disabled="true" tabIndex={-1}>` 표시 요소. 라우트도
상태도 없다.

헤드리스 브라우저로 데스크톱/모바일(390px) × 라이트/다크 4가지
스크린샷, 3테마 실제 데이터로 결과 타임라인의 테마별 컬러가 카드와
정확히 일치하는 것, 투어 스포트라이트가 새 헤더 레이아웃에서도 정확한
위치를 잡는 것과 Escape로 닫히는 것, "캘린더" 메뉴가 완전 비활성임을
전부 실측 확인. 빌드+테스트(82건) 통과.

**커밋 직후 실제 운영 URL에서 흰 화면 발견 → 원인은 배포 경로 문제
(Pages Source는 이미 Actions로 전환된 상태였다, 위 §4.33 정정 참고)**:
`vite.config.ts`에 `base` 설정이 없어 빌드 산출물의 `/assets/...` 등
절대경로가 도메인 루트 기준으로 나갔는데, 실제 서빙 경로는
`/RoomEscapeScheduler/` 서브패스라 JS·CSS·`firebase-config.js`가 전부
404 나서 React가 마운트를 못 했다. `command === 'build'`일 때만
`base: '/RoomEscapeScheduler/'`를 적용(dev 서버는 그대로 루트)하고,
`importmap`의 `onnxruntime-web` 경로는 Vite가 자동으로 재작성하는
`src`/`href` 속성이 아니라서 `%BASE_URL%` 플레이스홀더로 직접 처리했다.
로컬에서 `dist/`를 실제와 같은 서브패스 구조(정적 서버 + 심볼릭 링크)로
띄워 헤드리스 브라우저로 콘솔 에러·요청 실패 0건을 먼저 확인한 뒤 배포,
배포 완료 후 실제 운영 URL에서도 같은 방식으로 재확인했다(커밋
`16371e0`).

### 4.36 "매진 회차도 포함해서 계산" — §4.13/§4.30 번복 (app/ 전용)

**사용자 시나리오**: 특정 요일에 원하는 테마 3개를 짜려는데 그 요일엔
셋 다 회차가 전부 매진이다. `excludeSoldout`이 계산 입력에서 매진
회차를 통째로 빼버리므로(§4.13), 그 테마는 어떤 조합에도 낄 수 없고
결과가 0개로 나온다 — "그나마 이 조합이면 되겠다"조차 안 보인다.

**왜 다시 번복하나**: §4.13은 "매진 회차 제외" 체크박스를 없앴다 —
당시엔 두 입력 경로(직접 입력·사진) 어느 쪽도 매진 여부를 못 채웠으니
체크박스가 "한 번도 켜질 일이 없는 옵션"이었고, 무엇보다 **"예약할 수
없는 조합을 1위로 믿게 만드는" 게 제일 위험**하다고 판단했다. §4.30은
F-15로 매진 여부가 실제로 들어오기 시작한 뒤에도 "체크박스를 되살리는
것은 아니다"라고 재확인했다 — 대신 계산에서는 계속 빼되 타임라인엔
회색으로 남기는 절충안을 택하고, 진짜 해법(F-16, 조합 속 매진 회차를
서버에 감시 등록해 취소표 알림)은 아직 손 안 댄 채 미뤄뒀다.

**그런데 F-16 없이도 지금 당장 막힌 지점이 있다**: 후보 테마가 전부
매진인 요일처럼, "계산에서 완전히 빠짐"과 "화면에 회색으로 남음"
사이에 **아무 조합도 안 나오는 경우**가 있다. §4.13의 원래 우려("예약
불가 조합을 1위로 믿는다")는 그 조합이 **눈에 안 띄게** 나올 때
위험한 것이지, 사용자가 **직접 켠 옵션으로, 매진 회차가 눈에 뚜렷하게
표시된 채** 나오는 것과는 다르다. F-15 덕분에 이제 매진 회차가 결과에
섞여 나와도 타임라인에서 확실히 구분된다(빨간 점선 `.blk.so` — §4.30이
이미 만들어 둔 스타일, 그동안 실제로 렌더될 데이터가 없어 써본 적이
없었을 뿐). 그래서 §4.13/§4.30의 우려를 지키는 조건 — **기본 꺼짐 +
매진 회차가 결과에서 시각적으로 명백히 구분됨** — 을 만족시키는
범위에서 체크박스를 되살렸다. F-16(서버 감시·알림)은 여전히 범위
밖이다 — 이번 것은 그 앞단계, 클라이언트만으로 되는 절반이다.

**구현**: `core.ts`의 `search()`는 이미 `excludeSoldout` 옵션을 갖고
있었다(§4.30에서 만들어 두고 한 번도 `false`로 호출된 적이 없었다) —
`useScheduler.ts`에서 하드코딩된 `excludeSoldout: true`를
`!options.oIncludeSoldout`로 바꾸기만 하면 됐다. `ConditionsPanel.tsx`에
"매진 회차도 포함해서 계산" 체크박스 추가(기본 꺼짐, 결과가 안 나오는
게 조건 탓인지 매진 탓인지 헷갈리지 않게 툴팁으로 설명). 공유
링크·자동저장 포맷(`serialize.ts`)의 `o` 배열 끝에 새 자리를 추가 —
`oMove`/`oTeam`이 그랬듯 옛 링크는 그 값이 없어(`undefined`) 자동으로
꺼짐으로 떨어진다(레거시 픽스처 회귀 테스트로 이미 확인됨). §4.13이
남겨 둔 죽은 5번째 슬롯(`so`, 옛 "매진 제외" 체크박스 자리)은 건드리지
않았다 — 그 자리는 이미 배포된 모든 링크·자동저장에 하드코딩된 값
`1`이 깔려 있어서, 의미를 뒤집어 재활용하면 옛 링크를 열 때마다 새
옵션이 조용히 켜진 것처럼 복원될 뻔했다.

헤드리스 브라우저로: 토글 꺼짐(기본값)에서 3테마 중 1개가 전부 매진이면
결과 0개("조건을 만족하는 조합이 없습니다")로 사용자가 겪은 문제를
그대로 재현 → 토글 켜면 결과 1개가 나오고 그 매진 테마의 블록에
`.blk.so` 스타일이 실제로 적용됨 → 링크 복사 후 새로고침해도 체크
상태가 그대로 복원됨(직렬화 왕복)까지 확인. 빌드+테스트(82건) 통과.

### 4.37 F-16 완성 — 감시·알림 (작업명세서 §4.5)

**서버는 이미 다 돼 있었다** — 판정·쿨다운·3개 제한까지. 이쪽에서 할 일은
§4.5가 적어 둔 그대로 둘이었다: 무엇을 감시할지 지목하는 것(회차 칩·결과
타임라인의 벨 토글)과 알림 받을 주소를 만드는 것(웹 푸시).

**실측으로 문서와 달랐던 지점 셋**:
1. `GET /api/watches`는 배열이 아니라 `{limit, watches}` 객체로 온다.
2. 3개 제한 초과는 `409` `{error:"WATCH_LIMIT_EXCEEDED", message:"..."}` —
   메시지가 이미 완성된 한국어라 그대로 썼다(`sayWatch()`).
3. `GET /api/watches`는 **회차 시각이 이미 지난 것도 뺀다** — 문서의 "지난
   날짜는 빠진다"가 날짜 단위가 아니라 날짜+시각 단위였다. 테스트 중 방금
   막 만든 감시가 GET에 안 잡혀 서버 버그로 오인했다가, 그 회차 시각이
   테스트하던 그 순간 기준으로 이미 지났다는 걸 확인하고서야 이해했다 —
   지나간 회차를 감시해 봐야 취소표를 받을 수 없으니 맞는 동작이다.

**slotId가 파이프라인 전체에서 빠져 있었다**: `Session`/`ServerSession`/
`toSessions()`/`PathStep`/`search()`의 DFS 어디에도 서버 세션 id가 없었다
(Explore 조사로 확인). `Session.id`/`PathStep.sessionId`를 새로 추가하고
`dfs()`의 `path.push()`에 `sessionId: s.id`를 얹어 관통시켰다 — 수동
입력·이미지 인식은 원래 id가 없으니 `undefined`로 자연히 떨어지고, 그 칩엔
벨이 안 뜬다.

**감시 목록(`GET /api/watches`)이 slotId를 안 돌려준다**는 것도 실측으로
발견했다 — 그래서 "이 칩이 지금 감시 중인지"는 서버 목록과 대조할 방법이
없다. 이번 세션에서 직접 건 것만 `slotId→watchId`로 클라이언트에 기억해
칩/타임라인 토글 상태에 쓰고("빈자리 알림" 목록 모달이 진짜 소스), §4.13이
남긴 죽은 5번째 슬롯처럼 여기서도 정직하게 한계를 코드 주석에 남겼다.

**웹 푸시는 이 저장소에 완전히 없던 것**(manifest, service worker,
firebase-messaging 전부 그린필드)이라 실제로 두 가지가 걸렸다:
1. **`messagingSenderId` 누락** — `firebase-config.js`엔 이 필드가
   없었다(로그인/Firestore만 쓸 땐 필요 없어서). 서비스워커 콘솔(페이지
   콘솔엔 안 뜨고 서비스워커 자체의 콘솔에만 뜬다 — CDP로 서비스워커
   타깃에 직접 붙어서 찾았다)에 "Missing App configuration value:
   messagingSenderId"로 실패하고 있었다. `appId`의 두 번째 세그먼트가
   곧 프로젝트 번호(=messagingSenderId)라는 걸 이용해 값을 채웠다.
2. **인증이 필요한 API는 로컬 개발에서 CORS로 막힌다는 걸 새로 발견** —
   F-15의 `/api` 프록시(`vite.config.ts`)는 `changeOrigin`으로 Host만
   바꾸고 `Origin` 헤더는 그대로 브라우저 값(`localhost:*`)이 나간다.
   `GET /api/branches`·`/api/schedule`는 서버가 Origin을 안 봐서 지금까지
   문제가 없었지만, `/api/watches`·`/api/devices`처럼 인증이 필요한
   엔드포인트는 서버가 Origin을 직접 검사해 `403 Invalid CORS request`로
   막는다. 프록시 설정에 `headers:{Origin:'https://choieuihyun.github.io'}`를
   추가해 해결 — F-16 개발 중 처음으로 인증 API를 실제 브라우저에서 두드려
   보다가 드러난, F-15 때부터 있었을 잠재적 구멍이다.

**검증**: 테스트 계정으로 가입 → F-15로 실제 매진 회차 로드 → 칩의 벨 클릭
→ `POST /api/watches` 성공(201) + 헤더 뱃지 갱신 + 푸시 권한 요청(CDP
`Browser.grantPermissions`로 자동 허용) → 서비스워커 등록 성공 →
`POST /api/devices` 성공(콘솔 에러 0건) → "빈자리 알림" 모달에서 목록 확인
→ 삭제까지 전부 실측. 테스트 계정·서버 쪽 감시 기록 정리, 빌드+테스트
(82건) 통과.

---

## 6. 열린 질문

- ~~상위 12개 노출이 적정한가?~~ → **결정**: 12개 유지 + "더 보기" 로 계속 열람 (§4.27).
- 최소 공백 경고 기준 10분이 적정한가? 매장 규모·이동 동선에 따라 다를 수 있음.
- 부분 조합(F-08)을 기본 켬으로 둘 것인가? 현재는 기본 끔.
- ~~공개 배포 시 기본 데이터를 그대로 둘 것인가?~~ → **번복**: 처음엔 둔다고 정했으나(§4.17),
  실사용자에게는 "자기 데이터 넣기 전에 남의 매장부터 지워야 하는 화면"이었다.
  지금은 **비워 두고, 버튼을 눌러야 나오는 데모**로 바꿨다 (§4.22).
- ~~"내 시간표를 저장해 두고 싶다" 가 나오는가?~~ → **나왔다.** 계정 기능 착수 (§4.18).
- 비밀번호 재설정 수단이 없는 것이 실제로 문제가 되는가? 문의가 나오면 복구용 메일 주소를 선택 입력으로 받는 안.
- 남의 일정을 **보는** 요구가 생길 것인가? 지금은 각자 자기 것만 본다. 생기면 공개 플래그 하나로 시작한다.
